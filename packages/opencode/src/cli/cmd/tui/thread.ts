// Stub: TUI 已移除，TuiThreadCommand 不再可用
export const TuiThreadCommand: yargs.CommandModule = {
  command: "thread",
  describe: "TUI thread (deprecated — use bun dev for Web UI)",
  builder: () => {},
  handler: () => {},
}
