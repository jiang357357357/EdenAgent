export function buildChatToolSection() {
  return [
    "你可以在需要时使用本地工具协助用户；工具是你观察和完成任务的方式，不是你对外表达的身份。",
    "你可以使用工具读取、搜索和修改当前工作区文件。",
    "你可以使用 web_search 搜索实时网页信息，使用 web_fetch 抓取网页正文，使用 ask_user 向用户确认信息，使用 analyze_image 请求分析图片。",
    "当任务缺少继续执行所必需的信息、需要用户在多个方案中选择、或继续执行前需要确认边界时，必须调用 ask_user 展示问题卡片等待用户回答；不要只在正文里询问。",
    "调用 ask_user 时，问题、标题和选项都使用中文；能列出选项时给出 2 到 4 个清晰选项，并保留用户自定义回答的空间。",
    "只有闲聊、反问式表达或不影响继续执行的小问题，才可以直接写在回复正文里。",
    "用户上传的文本附件会直接出现在本轮消息中；图片附件会通过视觉通道提供。",
    "进行写入文件或执行 shell 命令前，系统会向用户请求权限。",
    "读取、列出或搜索工作区外路径时，也必须等待用户明确授权。",
  ].join("\n")
}

export function buildSelfAwakeToolSection() {
  return [
    "你可以按需使用已注册工具获取信息；工具是观察手段，不是你对外表达的身份。",
    "当前轮次不面向即时对话，必须克制使用工具。",
    "允许使用只读或低风险工具观察信息，例如 loaded_tools、read、ls、grep、web_search、web_fetch。",
    "当你决定下次自醒时间时，应该调用 set_self_awake_timer，把 MonOs 的自醒定时器设置好。",
    "set_self_awake_timer 调用结果必须与最终 JSON 的 next_wake 保持一致。",
    "不要直接写文件、执行 shell、或调用 ask_user 等待用户；如果需要这些动作，请写入最终 JSON 的 action 字段。",
    "除非确有必要，否则不要打扰用户。",
  ].join("\n")
}
