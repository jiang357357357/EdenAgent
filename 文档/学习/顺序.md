# Opencode 源码学习顺序

## 原则

按理解成本排序——读 A 模块前，必须已经理解 A 所依赖的所有概念。反了就要回头重读。

## 第一层：地基

| 顺序 | 包/模块 | 文件 | 行数 | 要点 |
|------|---------|------|------|------|
| 1.1 | `core` | `src/global.ts` | ~120 | 全局路径 `Global.Path`（data/cache/config/state/tmp），`Flag.OPENCODE_CONFIG_DIR` 覆盖 |
| 1.2 | `core` | `src/flag/flag.ts` | ~200 | 60+ `Flag.*` 环境开关，`truthy()`/`falsy()`/`number()`，`OPENCODE_EXPERIMENTAL*` 通道感知 |
| 1.3 | `core` | `src/util/log.ts` | ~150 | `Log.init()` / `Log.create()` 结构化文件日志，自动清理保留最近 10 个 |
| 1.4 | `core` | `src/filesystem.ts` | ~200 | `AppFileSystem` —— `read/write/list/findUp/glob/globUp/ensureDir` |
| 1.5 | `core` | `src/util/encode.ts` | ~50 | Base64 / SHA256 / FNV 校验和 |
| 1.6 | `core` | `src/util/identifier.ts` | ~50 | 时间戳 ID 生成器 `ascending()`/`descending()` |
| 1.7 | `core` | `src/util/lazy.ts` | ~10 | `lazy(fn)` 延迟初始化 |
| 1.8 | `core` | `src/schema.ts` | ~50 | `PositiveInt` / `Newtype<T>()` / `DeepMutable<T>` |
| 1.9 | `core` | `src/effect-zod.ts` | ~200 | Effect Schema → Zod 转换 `zod()` / `toJsonSchema()` |

> **检查点**：读完能答出——全局数据目录在哪？`Flag.OPENCODE_DISABLE_PROJECT_CONFIG` 是干什么的？日志写到哪个文件？

---

## 第二层：神经系统

| 顺序 | 模块 | 文件 | 行数 | 要点 |
|------|------|------|------|------|
| 2.1 | `bus` | `src/bus/bus-event.ts` | ~80 | `BusEvent.define(type, schema)` —— 事件定义注册 |
| 2.2 | `bus` | `src/bus/index.ts` | ~200 | 实例级 `PubSub`（按类型 + 通配），`publish()` → 同步到 `GlobalBus` |
| 2.3 | `bus` | `src/bus/global.ts` | ~50 | 进程级 `EventEmitter`，跨实例通信 |

> **检查点**：`publish(def, payload)` 的执行路径？实例级 bus 和 GlobalBus 的关系？

---

## 第三层：配置骨架

| 顺序 | 模块 | 文件 | 行数 | 要点 |
|------|------|------|------|------|
| 3.1 | `config` | `src/config/paths.ts` | 45 | `ConfigPaths.files()` / `ConfigPaths.directories()` —— 向上搜索配置文件 |
| 3.2 | `config` | `src/config/parse.ts` | 88 | JSONC 解析 + Effect Schema 验证 + Zod 回退 |
| 3.3 | `config` | `src/config/variable.ts` | 90 | `{env:VAR}` / `{file:path}` 模板替换 |
| 3.4 | `config` | `src/config/agent.ts` | 175 | Agent MD 文件加载（frontmatter + body） |
| 3.5 | `config` | `src/config/provider.ts` | 114 | Provider/Model 配置 Schema |
| 3.6 | `config` | `src/config/command.ts` | 62 | Command MD 加载 |
| 3.7 | `config` | `src/config/permission.ts` | 73 | Permission 配置 Schema（`*` → `allow/deny/ask`） |
| 3.8 | `config` | `src/config/config.ts` | **847** | **核心：9 层合并 `loadInstanceState()`**。按序阅读合并逻辑：well-known → global → env → project → `.mon-agent/` → envContent → console → managed → MDM |

> **检查点**：`mon-agent.jsonc` 和 `.mon-agent/mon-agent.json` 谁的优先级高？`instructions` 数组是覆盖还是拼接？

---

## 第四层：运行时容器

| 顺序 | 模块 | 文件 | 行数 | 要点 |
|------|------|------|------|------|
| 4.1 | `project` | `src/project/project.sql.ts` | ~30 | Drizzle 表定义 |
| 4.2 | `project` | `src/project/project.ts` | ~200 | `fromDirectory()` —— 发现 `.git`，生成 ProjectID，upsert DB |
| 4.3 | `project` | `src/project/instance-context.ts` | ~50 | `ALS` 上下文 `{ directory, worktree, project }`，`Instance.bind(fn)` |
| 4.4 | `project` | `src/project/instance-store.ts` | ~150 | `load()` 按目录缓存 + `Deferred` 去重，`reload()`，`dispose(ctx)` |
| 4.5 | `project` | `src/project/bootstrap.ts` | ~100 | 启动序列：`config → plugin → 并行 init 所有服务` |

> **检查点**：为什么用 `Deferred` 而不是简单的 Map？bootstrap 里为什么用 `forkDetach`？

---

## 第五层：AI 的大脑

| 顺序 | 模块 | 文件 | 行数 | 要点 |
|------|------|------|------|------|
| 5.1 | `agent` | `src/agent/agent.ts` | **449** | 8 个内置 agent（build/plan/general/explore/scout/compaction/title/summary），权限合并 `Permission.merge(defaults, specialized, user)`，自定义 agent 覆盖逻辑 |

> **检查点**：`build` 和 `plan` agent 的权限差异？`explore` 有哪些只读工具？用户如何在 mon-agent.json 中定义自定义 agent？

---

## 第六层：心脏 —— 对话循环

| 顺序 | 模块 | 文件 | 行数 | 要点 |
|------|------|------|------|------|
| 6.1 | `session` | `src/session/session.sql.ts` | ~60 | session/message/part Drizzle 表 |
| 6.2 | `session` | `src/session/session.ts` | **936** | Session CRUD、fork、revert、permission、summary、diff |
| 6.3 | `session` | `src/session/status.ts` | 98 | 状态机 `idle → busy → retry` |
| 6.4 | `session` | `src/session/retry.ts` | 199 | 指数退避 `attempts×3 delay×500 factor×2 maxDelay×10s` |
| 6.5 | `session` | `src/session/system.ts` | 84 | System prompt 组装（环境信息 + skills 列表） |
| 6.6 | `session` | `src/session/instruction.ts` | 232 | AGENTS.md / CLAUDE.md 发现与加载，去重 |
| 6.7 | `session` | `src/session/message-v2.ts` | **1235** | 消息模型 V2 —— User/Assistant Part 类型体系，`toModelMessages()` |
| 6.8 | `session` | `src/session/compaction.ts` | 650 | 自动/手动压缩，token 预算分配，调用 compaction agent 生成结构化摘要 |
| 6.9 | `session` | `src/session/llm.ts` | 470 | `LLM.run()` —— system prompt 组装、工具解析、provider 选择、`streamText()` 调用 |
| 6.10 | `session` | `src/session/processor.ts` | 828 | SSE 事件流处理器，`start/delta/end` 事件 → updatePart |
| 6.11 | `session` | `src/session/prompt.ts` | **2101** | **核心循环 `loop()` —— 整个系统的 while(true) 主循环** |

> **检查点**：从用户发送消息到 AI 返回完整回复，经过哪些步骤？compaction 触发条件是什么？`tool-execution` finishReason 后会发生什么？

---

## 第七层：双手 —— 工具系统

| 顺序 | 模块 | 文件 | 行数 | 要点 |
|------|------|------|------|------|
| 7.1 | `tool` | `src/tool/tool.ts` | 162 | `Tool.define()` / `Tool.init()` / `Tool.wrap()` —— 参数验证 → 执行 → 输出截断 |
| 7.2 | `tool` | `src/tool/registry.ts` | 379 | `ToolRegistry` —— 工具注册、发现、过滤、GPT/非 GPT 模型区分 |
| 7.3 | `tool` | `src/tool/read.ts` | 342 | 读取文件/目录/图片/PDF，含 git diff 对比 |
| 7.4 | `tool` | `src/tool/write.ts` | 104 | 写入文件 |
| 7.5 | `tool` | `src/tool/edit.ts` | 711 | diff-based 编辑 `old_string → new_string` |
| 7.6 | `tool` | `src/tool/grep.ts` | ~150 | ripgrep 搜索 |
| 7.7 | `tool` | `src/tool/glob.ts` | ~100 | 文件模式匹配 |
| 7.8 | `tool` | `src/tool/shell.ts` | ~200 | 执行 shell 命令，含权限检测（bash arity） |
| 7.9 | `tool` | `src/tool/task.ts` | 174 | 启动子 agent 会话 |
| 7.10 | `tool` | `src/tool/webfetch.ts` | ~100 | HTTP 请求 |
| 7.11 | `tool` | `src/tool/websearch.ts` | ~100 | 网络搜索 |
| 7.12 | `tool` | `src/tool/lsp.ts` | ~50 | LSP 查询工具 |
| 7.13 | `tool` | `src/tool/skill.ts` | ~30 | 加载技能 |
| 7.14 | `tool` | `src/tool/todo.ts` | ~100 | 任务列表管理 |
| 7.15 | `tool` | `src/tool/question.ts` | ~80 | 向用户提问 |
| 7.16 | `tool` | `src/tool/apply_patch.ts` | ~80 | 应用 patch |
| 7.17 | `tool` | `src/tool/codesearch.ts` | ~50 | 代码搜索 |
| 7.18 | `tool` | `src/tool/plan.ts` | ~50 | plan enter/exit |
| 7.19 | `tool` | `src/tool/repo_clone.ts` | ~50 | 克隆仓库 |
| 7.20 | `tool` | `src/tool/repo_overview.ts` | ~50 | 仓库概览 |

> **检查点**：`Tool.define()` 的三个核心参数是什么？为什么 `wite` 和 `apply_patch` 在 GPT 模型下要被替换为 `edit`？工具输出截断默认限制是多少？

---

## 第八层：模型通道

| 顺序 | 模块 | 文件 | 行数 | 要点 |
|------|------|------|------|------|
| 8.1 | `provider` | `src/provider/models.ts` | 199 | `ModelsDev` —— 从 models.dev 拉取模型目录，5 分钟 TTL 磁盘缓存，文件锁防竞争 |
| 8.2 | `provider` | `src/provider/provider.ts` | **1767** | **核心 Provider Service**。先看 `resolveSDK()`（20+ 内置提供者懒加载）→ `getLanguage()`（模型实例化）→ `custom loaders`（anthropic/openai/xai/copilot 特化） |
| 8.3 | `provider` | `src/provider/auth.ts` | ~200 | API key / OAuth 认证管理 |
| 8.4 | `provider` | `src/provider/error.ts` | ~50 | Provider 错误类型 |

> **检查点**：`BUNDLED_PROVIDERS` 有哪些？`custom loaders` 解决了什么通用 SDK 无法覆盖的问题？

---

## 第九层：安全与基础设施

| 顺序 | 模块 | 文件 | 行数 | 要点 |
|------|------|------|------|------|
| 9.1 | `permission` | `src/permission/index.ts` | ~300 | ask/allow/deny 主流程 —— `evaluate→ask→Deferred→reply(once/always/reject)` |
| 9.2 | `permission` | `src/permission/evaluate.ts` | ~80 | `last-match-wins` glob 匹配 |
| 9.3 | `permission` | `src/permission/arity.ts` | ~100 | 命令前缀 token 计数（用于 shell 权限粒度） |
| 9.4 | `shell` | `src/shell/shell.ts` | ~200 | 跨平台 shell 发现（bash/zsh/fish/ps/cmd），命令模板（login shell 包装），进程树 kill |
| 9.5 | `file` | `src/file/index.ts` | ~200 | File Service —— `read/list/search/scan`，fuzzysort 模糊匹配 |
| 9.6 | `file` | `src/file/watcher.ts` | ~100 | `@parcel/watcher` 原生文件监控 |
| 9.7 | `file` | `src/file/ripgrep.ts` | ~150 | ripgrep 自动下载 + JSON 输出解析 |
| 9.8 | `file` | `src/file/ignore.ts` | ~50 | `.gitignore` 解析 + 内置忽略规则 |
| 9.9 | `file` | `src/file/protected.ts` | ~50 | 受保护目录（TCC 敏感） |
| 9.10 | `git` | `src/git/index.ts` | ~300 | Git 操作 —— `run/show/status/diff/patch/apply`，NUL 分隔防特殊路径 |
| 9.11 | `snapshot` | `src/snapshot/index.ts` | ~250 | Git-backed 文件快照 —— `track()` 保存 tree hash，`revert()` 按文件回退，智能批处理 |

> **检查点**：用户点击 "always allow" 后，权限如何持久化？shell 命令模板里 login shell 包装做了什么？

---

## 第十层：扩展系统

| 顺序 | 模块 | 文件 | 行数 | 要点 |
|------|------|------|------|------|
| 10.1 | `plugin` | `src/plugin/loader.ts` | ~200 | 插件解析管线 `resolve → read package.json → compatibility → dynamic import` |
| 10.2 | `plugin` | `src/plugin/index.ts` | ~200 | `PluginInput` 构造 + `trigger()` 钩子执行 |
| 10.3 | `plugin` | `src/plugin/shared.ts` | ~100 | target 解析 / entrypoint 检测 / package 读取 |
| 10.4 | `plugin` | `src/plugin/meta.ts` | ~80 | 插件元数据追踪（首次加载 / fingerprint / 主题） |
| 10.5 | `plugin` | `src/plugin/install.ts` | ~100 | 插件安装 + config patching（jsonc-parser） |
| 10.6 | `lsp` | `src/lsp/language.ts` | ~100 | 120+ 扩展名 → 语言 ID 映射 |
| 10.7 | `lsp` | `src/lsp/server.ts` | ~600 | 25+ 语言服务器定义（root 检测 + 二进制定位 + 自动下载） |
| 10.8 | `lsp` | `src/lsp/launch.ts` | ~100 | 子进程生成 + stdio 管道 |
| 10.9 | `lsp` | `src/lsp/client.ts` | ~300 | `vscode-jsonrpc` 连接管理，push+pull 双诊断，去重合并 |
| 10.10 | `lsp` | `src/lsp/lsp.ts` | ~400 | LSP 服务编排 `touchFile/diagnostics/hover/definition/references/symbols` |
| 10.11 | `lsp` | `src/lsp/diagnostic.ts` | ~50 | 诊断输出格式化 |
| 10.12 | `mcp` | `src/mcp/index.ts` | 969 | MCP 主服务 —— local/remote 双传输，tool 转换 `convertMcpTool()` |
| 10.13 | `mcp` | `src/mcp/auth.ts` | ~50 | Token 持久化 `mcp-auth.json`，URL-keyed 查找 |
| 10.14 | `mcp` | `src/mcp/oauth-provider.ts` | ~100 | MCP SDK `OAuthClientProvider` 实现，预注册 + 动态客户端 |
| 10.15 | `mcp` | `src/mcp/oauth-callback.ts` | ~100 | 本地 HTTP 服务器 `port 19876`，CSRF 保护（32 字节 random state），浏览器自动打开 |
| 10.16 | `skill` | `src/skill/index.ts` | ~150 | Skill Service —— 发现/加载/过滤/格式化 |
| 10.17 | `skill` | `src/skill/discovery.ts` | ~100 | 远程 skill `index.json` 下载，并发 4 个 skill / 8 个文件 |

> **检查点**：MCP local 和 remote 传输的差异？LSP 的 push 诊断和 pull 诊断如何合并去重？插件的 file 源和 npm 源加载流程有何不同？

---

## 第十一层：拼装现场

| 顺序 | 模块 | 文件 | 行数 | 要点 |
|------|------|------|------|------|
| 11.1 | `server` | `src/server/server.ts` | ~200 | `listen()` —— 构建 layer → 启动 HTTP → mDNS → 返回 `{url, stop}` |
| 11.2 | `server` | `src/server/routes/instance/httpapi/server.ts` | ~300 | 路由组装，所有 handler 接线 |
| 11.3 | `server` | `src/server/routes/instance/httpapi/api.ts` | ~100 | HttpApi 定义（RootHttpApi + InstanceHttpApi） |
| 11.4 | `server` | `src/server/routes/instance/httpapi/event.ts` | ~50 | SSE 事件流 `/event` |
| 11.5 | `server` | `src/server/cors.ts` | ~50 | CORS 配置（localhost/127.0.0.1/tauri:///openode.ai） |
| 11.6 | `server` | `src/server/auth.ts` | ~50 | Basic Auth（`OPENCODE_SERVER_PASSWORD`/`OPENCODE_SERVER_USERNAME`） |
| 11.7 | `cli` | `src/index.ts` | 247 | Yargs CLI 入口——所有命令注册 + 全局中间件 |
| 11.8 | `cli` | `src/cli/cmd/run.ts` | 833 | Run 命令——三种模式（非交互/本地交互/attach） |
| 11.9 | `cli` | `src/cli/cmd/tui/thread.ts` | 261 | TUI 入口——Worker 线程 + RPC |
| 11.10 | `cli` | `src/cli/cmd/tui/worker.ts` | 104 | Worker 线程——服务器 + GlobalBus 转发 |

> **检查点**：10 层中间件的执行顺序？端口 0 的分辨策略是什么？TUI 的主线程和 Worker 线程如何通信？

---

## 学习用时估算

| 层 | 合计行数 | 预估用时 |
|----|----------|----------|
| 第一层（地基） | ~1,000 | 2h |
| 第二层（bus） | ~330 | 0.5h |
| 第三层（配置） | ~1,500 | 3h |
| 第四层（运行时） | ~550 | 1.5h |
| 第五层（agent） | ~450 | 1h |
| 第六层（session 循环） | ~6,800 | 8h |
| 第七层（工具） | ~2,500 | 4h |
| 第八层（provider） | ~2,200 | 3h |
| 第九层（安全与基础设施） | ~1,800 | 3h |
| 第十层（扩展系统） | ~3,400 | 5h |
| 第十一层（拼装） | ~2,400 | 3h |
| **合计** | **~23,000** | **~34h** |

> 建议每天 3-4 小时，一周内完成全部。
