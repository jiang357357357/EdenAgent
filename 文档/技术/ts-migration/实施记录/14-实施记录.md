# 实施记录 14

## B5/B6/B7：历史同步重投页面（已编写，未执行）

- `LegacySyncReplay` 接入已核对原账号的会话/消息/导演历史记录，要求说明及明确勾选后调用既有重投 RPC；人工确认完成/放弃入口继续独立保留。
- 首次发送前将请求编号和说明写入当前浏览器会话存储。连接中断、组件卸载和页面刷新后可继续原请求；存储失败则不发送。已有编号的说明保持不变，结果 unknown 后才提供明确创建新尝试的操作，不自动重投。
- `mon.sync.status` 共享契约和查询增加最近一次重投的编号、状态、说明、错误与时间，页面轮询显示。重投完成刷新时保留组件，避免丢失正在跟踪的请求。主机中的持久账本仍是投递事实来源，不用浏览器状态判定成功。
- 界面明确旧投影会更新原 Core 的会话及相应记录。完整载荷预览、历史通知渠道适配、无原身份的人工核对流程和其余迁移域仍待实现。
- 本批仅阅读和编辑源码、文档；未运行测试、构建、类型检查、生成器、数据库查询/迁移、页面或真实 Core 请求。用户三处前端改动未触碰。

## B3/B6/B7：旧子智能体线程树与邮箱转换（已编写，未执行）

- M67 增加私有上下文与完整邮箱历史表。线程转换先在临时 SQL 表暂存，按父先子后分批装配；缺失父节点、跨根会话父节点、循环和不合法路径使整个域回滚。保留旧路径的自动数字后缀，不把重复任务名误认为损坏。
- 旧线程 ID、根会话、父关系、角色、结果、时间和历史预算/用量进入新线程域；每个线程派生稳定独立子会话 ID，子会话先关闭。旧 queued/running 转 interrupted，不创建新作业。原 prompt/context/config/usage 与协作批次完整保留，标记 context_required，模型、工具策略、历史上下文和预算语义尚未适配前不允许续接。
- 邮箱保存所有原字段与消费时间。普通消息只有目标线程和发送者归属明确时进入新邮箱；发往 root、控制/续接消息及无法映射的消息留在历史域等待审阅，不根据旧 trigger_turn 自动启动任务。
- 转换 runner 在原会话/事件之后转换线程树和邮箱；恢复摘要列出上下文及邮箱待处理数量。agent.read/list 协议增加 recoveryState 并保留协作批次，子任务页面显示恢复限制、关闭续接按钮，repository 同时阻止绕过页面续接。
- 仍需完成旧上下文至 pi 的适配、旧策略/模型继承与显式恢复入口、root 邮箱/控制消息审阅、模型次数缺失的历史预算核对及其他配置域；表转换不代表运行恢复完成。
- 仅使用 cat/grep/find/sed 阅读源码及 Python/文件写入修改源码文档。未运行 SQL、转换器、服务、页面、生成器、构建、类型检查或测试，未读取/修改真实 Data。

## B4/B7：旧应用配置转换（已编写，未执行）

- M68 保存旧 app_config 的原值、时间、对应新设置键、转换状态及后续重设记录。新转换阶段核对 system.runtime_origin 与快照世界一致，不能用配置覆盖目标世界。
- GSV TTS/STT 使用 packages/api 的现行共享 schema 校验后写入 voice_configuration，不能映射的旧配置保留并标 review_required；store 增加已有 workspace API 依赖，清单与锁文件同步编辑，未安装依赖。
- restricted 审批模式及无网络/额外写目录的 sandbox 设置可以转换；旧高权限、本机边界、额外网络/目录访问保留为 confirmation_required，不自动激活。现有权限/命令设置入口成功保存时在同事务标记 reconfigured；语音设置重存同样记录恢复结果。
- self_awake.session 用户索引检查目标会话及元数据中的用户归属后保留为历史，不替换新桥接器每次唤醒独立创建会话的逻辑。未知键完整保留并列入恢复摘要，不静默丢弃。
- 未执行转换、SQL、配置保存、真实服务请求、构建、类型检查或测试；未知配置的通用审阅入口、语音文件/环境配置来源和其他迁移域仍待补齐。

## B7：历史运行统计与导入追溯（已编写，未执行）

- M69 与转换器保留三张旧运行统计表。累计计数采用十进制文本保存，避免超过 JS 安全整数时失真；未结算轮次/工具计时行保留原标识和时间，不生成虚构完成样本、不重启任务，也不从重放事件重复累加旧总数。
- 转换报告增加历史统计摘要，累计值以字符串输出，最多展示 256 种计数并显式标记截断；另列未结算计时行数量。新宿主实时统计与旧累计值的查询/展示仍需继续接线。
- M70 与转换器保存旧导入实体映射、会话来源/用户标识/消息数量以及待重装技能记录。用户标识和消息数保持精确十进制值，会话映射保留外键；原技能源、版本、内容摘要、信任及迁移状态完整保留，不因此安装或启用代码。
- 三个历史导入域逐表事务、哈希扫描完成才提交转换状态，支持已有断点恢复；待重装/不可用技能加入恢复摘要，安装审阅界面与源文件恢复仍待实现。
- 本批只阅读归档 schema/统计写入源码并编辑源码、文档；未运行转换、SQL 查询、真实数据读取、构建、类型检查或测试。

## B2/B7：插件历史与市场撤销转换（已编写，未执行）

- M71 保存旧插件、版本、权限决定、市场来源与撤销记录，原行使用带类型表示完整保留，版本/权限必须找到所属插件，撤销必须找到来源；每域扫描核验后事务提交。
- 插件/版本标 files_required，旧授权标 reapproval_required，不将旧路径或 manifest 当作已验证的新安装制品。市场来源通过现行 schema 后以禁用状态进入来源列表，旧索引/时间/错误/启用状态保留于历史域，签名公钥仍需重新提供；不能映射的来源标 source_review_required。
- 旧撤销进入独立持久索引。市场 release 下载选择、列表撤销显示、包安装和已安装包 verified/激活路径均查询旧撤销，不因重下载或本地导入同版本而丢失历史拒绝。
- 恢复报告增加插件待处理计数。文件恢复、旧格式组件适配、权限审阅与恢复状态收敛仍未完成；五表转换不等于插件可运行，不标全部迁移完成。
- 仅阅读归档 schema 与现行 repository 并编辑源码/文档；未实际恢复插件文件、下载索引/包、安装/激活插件、运行数据库迁移、构建、类型检查或测试。

## B2/B7：旧插件版本文件恢复（已编写，未执行）

- stage/resume 增加可选旧插件 versions 根参数，CLI 支持以 `-` 跳过 Blob 参数；按原插件 ID/版本/revision 目录结构定位，拒绝任意旧 root_path 跳转，不访问未指定的源根。
- copyLegacyPluginTree 按目录/文件职责独立实现，拒绝链接和特殊文件，限制层级/数量/总体积，逐文件复制并记录 SHA-256，检查文件和目录在复制期间的变化，文件与目录落盘后写恢复凭据。
- M72 保存目标相对路径和文件摘要，逐版本完成后提交；失败/中断保留独立 copy-* 副本，不覆盖旧尝试。已提交版本跳过，状态 files_copied_review_required 继续出现在恢复摘要。
- 尚未接通恢复副本至插件审阅/安装 UI，签名及旧 revision 的一致性核对留给下一阶段；复制不等于安装、验签或启用。未读取旧插件实际文件、运行 CLI/复制器/数据库迁移、构建或测试。

## B2/B6/B7：恢复副本审阅与安装接线（已编写，未执行）

- plugin.recovery.list 分页返回历史版本、状态和副本存在标识；plugin.recovery.inspect 仅按私有恢复记录寻找当前世界数据目录下 copy-*/files，不接受客户端任意恢复路径。校验路径无链接、完整文件集合/大小/SHA-256 与 M72 复制凭据一致。
- 复用 MarketService 的预览并发与关闭取消管理，再核验签名、历史 ID/version/revision 及旧撤销；通过后生成现有包预览，不提前写安装/授权状态。普通本地预览也在保存前检查旧撤销。
- 插件页面增加恢复列表与检查按钮，通过现有 PluginPackagePreview 和安装确认操作继续；请求绑定当时世界，失效组件不接收结果。安装事务把对应历史版本标 installed_disabled，旧授权仍待重新确认。
- 旧格式与新 revision 计算/组件 schema 不一致时明确拒绝，不篡改旧签名或伪称恢复成功；格式适配、全部版本/插件状态收敛、旧授权重审仍需后续实现。未运行页面、检查器、安装、迁移、构建或测试。

## B2/B7：原插件完整性格式对齐（已编写，未执行）

- 阅读原 Rust verify_integrity/revision_material/signature 实现后确认：原始 manifest 字节加文件汇总摘要构成 revision，签名覆盖摘要文本；现行公式一致，不需要改写原包或签名。
- 对齐文件名排序为 UTF-8 字节比较，避免 JS UTF-16 默认排序在非 BMP 文件名下生成不同摘要。manifest/checksums/signature 改为严格 UTF-8 解码，非法字节不以替换字符继续解析。
- 原 Rust 支持单字符插件、组件和密钥标识；新包/管理/市场契约此前要求至少两个字符，已允许单字符且保留原有新标识范围。
- 恢复副本检查保留 `.git` 文件，遵循原 Rust 将其纳入文件集合的行为；普通开发目录预览仍按既有约定忽略 `.git`。恢复 receipt 的文件集合检查与旧 revision 核对继续有效。
- 原生 worker、宿主版本约束、权限恢复等仍需继续对齐。只阅读并编辑源码，无实际验签/预览/安装、构建或测试。

## B2/B6/B7：历史插件权限审阅状态（已编写，未执行）

- M73 为插件历史增加 resolution_json/resolved_at，原带类型 row_json 不改写。修正历史转换 INSERT 的显式列名，兼容新增字段。
- 新包权限保存事务中，按 plugin/capability/resource/access 精确定位旧记录，只有 manifest_revision 与当前 revision 一致且用户本次明确提交的决定，才标 permission_reviewed 并记录新决定。不会因安装、未提交的默认项或其他版本授权自动解决旧记录；拒绝仍会停用该版本。
- plugin.recovery.permissions 分页查询旧决定、原 revision、是否属于当前恢复版本及重新确认结果；恢复页面按需展开历史权限并可刷新，继续使用原插件权限区域提交决定。
- 权限来源不匹配、已移除声明的历史处理及插件总体恢复状态仍需继续收敛。未实际调用 RPC/权限保存、运行迁移、页面、构建或测试。

## B2：插件宿主版本约束（已编写，未执行）

- 把包兼容性比较从安装 repository 提取为独立 host-version 模块，支持预发布标识、构建元数据、数值/非数值标识优先级及上下界一致性；核心版本号用 bigint 比较，不截断大整数。
- 握手、健康响应和包兼容判断共用 Server/src/version.ts。当前 2.0.0-dev.0 不再被安装路径误当成 2.0.0 正式版；发布时此元数据应与 Server/package.json 同步。
- 预览保存前即检查宿主约束，安装和运行完整性验证仍再次检查，避免先生成可安装预览后才发现宿主不兼容。无界约束保持允许。
- 本批仅编辑源码/文档，未运行版本比较函数、预览/安装、构建、类型检查或测试；原生组件及其余业务继续推进。

## B5：连接器逐实例故障与撤权终止（已编写，未执行）

- 核对原生组件接线时发现，旧巡检在 try 外读取 grants，单个不可读实例会中止整轮巡检；调整为逐实例处理，权限/制品异常先关闭其现有 worker，其余实例继续。删除配置的 worker 也主动关闭；关闭失败保留进程所有权记录，不释放并发名额后再重复启动。
- ConnectorWorkerRuntime 增加 launcher 提供的当前授权检查。Lichess/OpenTTD 复用各自端点/registry 授权闭包；观察型 worker 检查 generation/制品 revision 和原挂载授权。invoke、通知与健康检查都经过当前授权，失效时关闭通道并触发进程终止，旧事件不继续进入仓库。
- 服务关闭等待所有 worker 的终止结果，保留失败项并聚合报错，避免一个关闭失败就提前结束等待。旧进程退出回调不会再改写替代进程的重试状态。
- 这是原生插件接线前发现的现有生命周期缺口修正，尚未完成插件自带 native_worker 的连接器适配。仅编辑源码，无启动/终止实际进程、网络访问、构建或测试；每事件授权读取成本留待后续验收关注。

## B2/B5：原生组件描述与目录资源（已编写，未执行）

- connectorManifestSchema 提取到共享 API，官方连接器目录与插件原生组件预览使用同一份描述契约。nativeWorkerDescriptor 严格读取 UTF-8 connector.json，将入口限定到组件目录，拒绝逃逸/空字符参数，并核对 worker 声明权限已出现在所属插件声明中。
- 对照官方 HOI4 插件发现 assets/game-mod 是目录，现行 preview 只接受文件会误拒绝。新增 packageAssetFiles 将目录声明展开为包内文件，保持目标子路径，拒绝来源/目标重叠；预览、资源列表、单文件 Blob 导出共用相同展开结果。
- 目录资源只导出声明覆盖的包文件，不读取包外路径；没有自动安装模组到游戏目录。原生组件实际启动与动态目录注册仍未完成，本批不把描述解析记为已运行。
- 未运行插件预览/导出、原生程序、数据库迁移、构建或测试，未读取真实用户 Data。

## B2/B5：版本绑定的原生运行计划（已编写，未执行）

- packageNativePlans 根据当前平台选择包内入口，检查非空/大小上限并复制执行字节，计算制品 SHA-256；计划绑定 plugin ID、revision、component ID、参数和描述，生成独立连接器键，避免与官方内置键冲突。
- InstalledPackageRepository 增加单版本原生计划及启用版本计划汇总，仍先走签名/历史撤销/宿主版本/必需权限校验；计划失败按所属包返回错误，不把一个包的错误传播给全部选择。
- 激活决策先核对原生制品归属，之后现有原生启动限制仍保留。动态连接器目录注册、私有制品准备与通用启动器继续接线；这些计划没有执行代码，也不表示 native_worker 已可用。
- 未运行计划构造、启动器、数据库迁移、构建或测试。

## B2/B5：原生插件目录与文件系统 worker 启动（已编写，未执行）

- 启用原生组件动态进入 ConnectorCatalog，键由插件/组件身份派生，与内置键隔离；目录、设置校验、事件声明、worker revision 均取当前已验证计划。版本切换/停用/撤权后旧描述不可再取得，逐实例生命周期负责终止。
- native 组件从 MCP 描述集合中分离，不再被误当成 HTTP MCP 或因混合组件整体报错；包启用仍先验证当前平台制品与声明权限，连接器实例另行配置并授权具体资源。
- launchNative 将包内执行字节写入私有临时制品，沿已有快照/沙箱进程启动器运行；退出后清理临时制品。worker 协议保留原 connector ID，宿主实例使用派生目录键，两者归属在初始化中明确匹配。
- 通用原生挂载只接受已授权的 settings 路径型文件系统权限，映射为固定 /inputs 或 /outputs 路径；拒绝私有 Data/受保护目录重叠和超过 16 项挂载。启动、事件、调用与健康检查复查包版本、组件和实例授权。
- 当前通用入口覆盖文件系统型 worker，网络、身份凭据和运行依赖文件仍需专用适配；不宣称全部原生类型可用。未实际物化制品、启动 worker、调用网络、运行迁移、构建或测试。

## B2/B5：网络型原生组件与实例凭据（已编写，未执行）

- Lichess/OpenTTD 启动器按已验证 connector manifest 的协议身份选择适配，不依赖实例目录键；插件派生键保持独立实例，worker 协议仍匹配原 lichess/openttd 标识。
- prepareWorker 共用制品准备：原生包取版本绑定字节写私有临时可执行文件，内置连接器继续使用受检平台制品；准备前后检查 revision，实际执行仍经已有 sha256 快照和 OS 沙箱。通用文件系统启动器也复用该实现。
- 网络型组件沿已有 Lichess HTTPS 或 OpenTTD loopback admin 桥运行，未开放一般宿主网络。所有退出/启动失败路径同时清理进程、网络桥和私有制品。
- 原生网络组件可在自己的连接器实例配置私有凭据，要求描述声明 connector.identityCredential；替换/移除凭据旋转 generation 并断开，不继承内置实例授权或凭据。组件停用后已有私有凭据仍可被用户移除。
- 通用网络协议、运行依赖文件及跨平台进程边界仍待补齐。仅编辑源码/文档，未物化程序、启动进程、读取实际凭据、联网、构建或测试。


## B2/B5：原生包只读运行依赖快照（已编写，未执行）

- 原生计划携带独立包字节快照；prepareWorker 不再在宿主数据目录另存单个临时程序，而把当前已验证包和入口交给隔离执行层。
- snapshotWorkerPackage 检查入口 SHA-256、路径/层级/文件数/总大小，复制原包结构到私有临时目录。包文件以只读可执行权限保存，支持包内辅助程序与相邻库/资源，不自动运行辅助程序。
- 沙箱只读挂载 /package，并从原入口路径、以 /package 为工作目录启动；可写实例状态仍是 /data，未继承宿主环境。内置非包 worker 沿原单制品快照和 /data 工作目录，网络桥约束保持。
- 执行层拥有包快照生命周期，启动失败和进程退出统一删除；原生文件系统、Lichess/OpenTTD 启动器都传递相同快照。包路径不指向可变安装目录。
- 一般网络协议、跨平台边界、资源生命周期与整体迁移恢复仍待完成。未实际生成快照、加载依赖、启动进程、构建或测试；包计划重复验签/复制成本仍待验收时评估。

## B5/B6：停用组件的实例收尾操作（已编写，未执行）

- ConnectorRepository 的改名/断开操作不再强制重新解析不可用组件的旧设置，也不被已关闭的绑定会话阻塞；重新连接或提交设置仍检查当前目录与 active 会话归属。
- 权限读取遇到组件停用/缺失时返回明确 unavailable、ready=false，不把旧 grants 当作有效权限。新增 connector.permissions.clear，事务核对 generation 后清除该实例全部历史 grants、轮换 generation 并断开，旧 worker 无法继续使用原身份。
- 权限页面新增清除全部授权并断开按钮，显示描述不可用原因；不再把未知权限误显示为无需权限。凭据组件在插件停用但仍有私有凭据时保留删除入口，禁止在不可用状态新增保存。
- 未执行清权/断开/凭据删除 RPC、启动/停止进程、运行页面、构建或测试；历史配置与未来重新授权关系仍待整体验收。

## B3/B7：旧会话初始 pi 上下文（已编写，未执行）

- 对照原 Rust conversation_entries/build_session_context，读取已完成轮次的 agent.message_end，排除 transient 消息；从最近 context.compacted 的摘要与 firstKeptEntryId 保留点恢复后续记录。缺失压缩锚点明确进入待审阅，不猜测删减历史。
- runtime-pi 新增 legacyContextCheckpoint，生成串联的历史 custom_message 项，保留事件/角色/结果，不生成待执行工具调用，不恢复历史工具授权。pi 结构仅由适配包构造和校验；store 的迁移适配依赖该公开函数，运行时包不反向依赖数据库。
- M74 记录逐会话 prepared/review_required。单角色初始上下文与状态同事务写入，不覆盖已有 checkpoint；断点续跑跳过 prepared，并重新尝试待审阅项。超 512 KiB、多角色待分离、缺失锚点均保持待审阅，原事件不删改。
- 未完成多角色/子智能体历史上下文分配、显式压缩/恢复入口及迁移激活；prepared 仅表示初始上下文已构造，不代表会话可验收。未调用适配器、模型、转换器，未运行构建或测试。

## B3/B7：子会话与多角色历史上下文（已编写，未执行）

- 子智能体从保留的 context.messages 恢复独立子会话；尚未生成 context 时保留 parentHistory 和原任务描述。排除 transient 消息，仅生成历史上下文，不重新排队旧任务。成功后状态为 context_prepared_policy_required，子会话继续关闭，模型/工具策略和邮箱恢复仍待完成。
- 对照旧 runtime/turn/execute.rs，原角色使用 conversation_messages.clone() 的共享历史。转换器为每个当前 assistantId 建立独立 actor_checkpoints，保留说话者和原事件信息，后续角色上下文分别积累；同时保留会话 checkpoint。重复/无效身份进入 review_required，不猜测身份映射。
- 初始会话/角色 checkpoint 和 prepared 状态同事务提交；续跑检查角色 checkpoint 是否齐全，补齐早先单角色转换遗漏的角色记录，并保留已存在的 checkpoint。
- SessionRepository 与 ActorCheckpointRepository 在读取迁移会话上下文前核对 prepared；旧子会话还需策略 ready，防止待恢复记录被当成空上下文开始。普通新数据库先检查 realm_meta 导入标记，不依赖只存在于转换数据库的映射表。
- 仅阅读归档源码、编辑源码/文档及查看 Git 差异，未调用转换器、模型、构建或测试。超限历史的显式压缩、子会话策略恢复、迁移审阅启动/激活及验收仍未完成。

## B3/B7：离线历史审阅与摘要恢复入口（已编写，未执行）

- 新增 review-context/apply-context-summary CLI：复用转换锁，仅接受同世界 incomplete 暂存库，先提供带完成证据的私有 NDJSON，再接收明确确认的摘要 JSON。普通宿主仍拒绝打开暂存库。
- 按原事件顺序计算 source SHA-256，摘要应用时在写事务内重新核对；子会话仅导出任务与消息，不导出完整 config。摘要读取限定字节数，核对文件类型、身份、读取前后元数据，拒绝变化或无效 UTF-8。
- M75 保存源摘要、用户摘要、审阅说明与时间；与会话/角色 checkpoint、prepared 同事务写入，不覆盖既有 checkpoint，不删原文。旧子会话仍等待策略恢复，不重开、不重新排队。
- 写明导出半成品、报告写入失败后的状态查询及退出码语义。仅编辑源码/文档与查看 Git 差异；未执行 CLI、schema、转换器、模型、构建或测试。激活/审阅运行模式、子会话策略恢复及整体验收仍待完成。

## B3/B6：内置子智能体角色与实际工具策略（已编写，未执行）

- 对照旧 eden-agent-multiagent/catalog.rs，补入 general/researcher/explore/file_locator/coder/reviewer，保留 TS worker 名称。新增 agent.roles 与前端角色选择、模式/轮数说明；未知角色在创建子会话前拒绝，不再默默按普通 worker 执行。自定义角色文件目录仍待接入。
- M76 保存新子任务的角色工具策略及指令；创建时与父策略取交集、拒绝项取并集，运行时沿最多四级祖先再收窄。现有非迁移任务按内置角色补算，旧迁移任务仍必须显式完成 policy recovery。
- 工具目录按策略过滤，beforeTool 在写操作记录/扣预算前复查；只读角色仅有明确列出的读取/查询/协作工具，不能调用写文件、终端、插件执行、技能代码或记忆修改。保留旧 root-only 工具排除规则，权限审批继续独立生效。
- coder 或继承 workspace-write 策略的终端要求 sandbox、无网络、无附加可写根；审批后配置变更仍由 CommandService generation 检查拒绝。角色轮数按请求与定义较小值持久化，dispatch 使用持久策略指令。
- 仅阅读原 Rust 策略及当前调用链、编辑源码/前端/文档；未启动子任务、审批、终端、模型或测试。旧模型/工具名映射、token/费用预算、自定义角色目录、恢复操作与迁移激活仍待完成。

## B3/B6：子树 token 与估算费用用量（已编写，未执行）

- M77 增加 token/费用上限、累计用量、未知标记与逐 turn/message 用量收据。assistant message_end 与收据/祖先累计同事务持久化；响应超过上限仍保留实际记录，在下一次模型或工具准入时拒绝，不回滚已发生消耗。
- 新任务默认 token 上限 100 万，费用上限可选；创建幂等摘要包含非默认额度，旧默认调用保持摘要兼容。API 和前端新增额度输入、累计用量及未知提示。最多四级祖先累计并检查各自额度。
- RuntimeModel/configuredModelSchema 接受可选 cost，单位为每百万 token 美元。pi 使用配置单价估算，事件/请求明确携带 costConfigured；未配置单价不把 SDK 默认零成本当免费。尘世可通过 EDEN_AGENT_MODEL_COST JSON 显式配置 input/output/cacheRead/cacheWrite 四种单价，Mon 单价配置入口仍待接入。
- 开启费用上限而缺少单价时，首次模型请求准入即拒绝；缺失/非法 token 或费用响应标为未知，影响对应上限的后续准入。费用向上取整为微美元，属于估算而非供应商账单。当前采用响应后累计，已在途的并行请求可能超过总量，不宣称硬性计费封顶。
- 新导入旧子任务保留旧 token/费用预算及 usage.tokens/costMicrousd，缺失值标未知；较早已转换任务升级后标记待核对，避免将默认零累计当已恢复。
- 仅读源码、编辑源码/前端/文档，未运行模型、迁移、构建或测试。崩溃后的未决请求用量核对、压缩用量回执、Mon 单价配置与旧策略恢复仍需补齐；未据此宣称完整预算能力已验收。

## B3/B6：逐请求用量回执、压缩计量与未知请求审阅（已编写，未执行）

- 根据本地已安装 pi-ai 公共 EventStream 导出，新增响应流适配：每次 provider 调用分配 request ID，请求准入成功才记录终态回执，回执持久化后才把终态交给 harness。该入口覆盖普通回复与压缩/分支摘要调用，不再依赖聊天 message_end 计量，避免漏计辅助请求或重复计数。
- 响应落库失败会设置运行时 fatal、终止请求信号，并返回明确错误终态；未出现终态的流也不会伪造成功回执。文本增量照常转发，实际回执和重建 checkpoint 分别保留自己的持久化语义。
- M78 保存 pending/responded/reviewed 请求及其最多四级预算归属。模型准入、请求 ledger 与 model.request 同事务写入；终态用量收据/子树累计、responded 和 model.response 同事务写入。错误/中断响应中的默认零用量标为未知，不据此认定零消耗。
- 后续准入及任务续接发现原 input 已不运行、但仍无回执的子树请求时拒绝；不会把重启中断当成已结算。已在途的其他请求不因此被倒算成失败。
- 新增 agent.requests.list/review 与子任务面板审阅区。仅原执行结束后可填写实际 token/微美元和证据、明确确认；事务校验请求归属，累计到原子树、标记 reviewed。相同内容可幂等重试，不同内容拒绝；该操作不发起模型请求。
- 此批替代上一节按 message_end 计量的实现。仅查看本地 SDK 源码/声明、编辑源码/前端/文档；未调用模型、迁移、生成器、构建或测试。已返回但缺失用量的回执核对、早期无请求关联的计量补偿、Mon 单价入口与旧子任务恢复仍待完成。

## B3/B6：不完整回执的增量用量核对（已编写，未执行）

- 请求审阅列表同时覆盖 pending 和已响应但 token/费用缺失的回执，协议返回已知金额，前端预填并锁定已知字段。
- 核对时只补 null 字段，对已知字段的更改明确拒绝；按原 request owners 给子任务与祖先追加差额，不重算已累计部分。用量收据、累计、未知状态和审阅说明在同一事务提交；原 model.response 事件保留。
- M79 分离升级前历史未知基线，避免审阅一个新回执时清掉其他历史未知。新回执核对后根据剩余缺失回执重新计算未知标记；导入记录保留自己的未知基线。早期未知基线的整体核对仍待单独恢复入口，不通过某个回执隐式解除。
- 仅阅读现有源码、编辑实现/前端/文档，未执行迁移、RPC、模型或测试。旧子任务完整恢复、Mon 单价配置及迁移激活仍在推进。

## B3/B6：模型身份绑定的单价配置（已编写，未执行）

- 新增逐世界 ModelPricingRepository 与 M80 当前配置/修改历史；身份取 provider、model ID、baseUrl 的 SHA-256，不包含 API key。保存同时校验当前模型身份及 revision，保留说明、前后单价与时间。清除配置明确表示未知，不回落成免费。
- model.pricing.read/set 覆盖主模型、视觉、导演、角色及角色视觉目标。模型解析返回包含单价的独立快照，当前 runtime 已使用的配置不被原地修改；同一世界相同模型端点共用单价，两个世界各自落库。
- Mon 会话面板新增单价读取/编辑/清除入口，四类价格均按每百万 token 美元填写，并说明价格来源、估算与账单区别及共享范围。未绑定目标无法配置；旧请求的原回执计价不重写。
- 只阅读和编辑源码/前端/文档，未查询真实价格、调用外部模型、运行配置 RPC、迁移、构建或测试。旧子任务完整恢复、自定义角色、迁移激活与最终验收仍待完成。

## B3/B6：自定义角色配置与任务策略快照（已编写，未执行）

- M81 新增逐世界角色定义与版本历史；agent.roles 合并内置/用户配置，agent.roles.save 通过预期 revision 避免覆盖并发修改。最多 256 个用户角色，同名可覆盖内置定义。
- 定义支持任务指令、描述、sandboxMode、allowedTools/deniedTools、轮数/模型次数/工具次数/token/费用/时限；全部由 API schema 限定。只读模式仍与明确读取工具集合取交集，root-only 拒绝项不能移除，父策略继续收窄。
- 创建新任务时读取角色定义，固定工具策略/指令及各项较小预算，费用同时尊重请求和角色上限；角色后续修改不改既有任务快照。未知角色在创建子会话前拒绝。
- 子任务面板增加 JSON 配置编辑，支持从现有角色读取、改名创建、同名更新及冲突提示。模型仍继承父绑定；自定义模型/reasoning/技能清单和旧 TOML 目录迁移仍待接入，不静默忽略不支持字段。
- 仅读源码并编辑 API/数据库/服务/前端/文档，未运行 RPC、迁移、创建任务、模型或测试；旧子任务恢复及迁移激活仍待完成。

## B3/B2：角色技能指令快照（已编写，未执行）

- 自定义角色增加 skills，最多 32 个互不重复的已安装技能名。创建前通过当前世界/工作区的 SkillRepository 解析，检查 enabled、modelInvocable、user_chat profile；缺失、停用或超出合计 256 KiB 指令内容时拒绝，尚未创建子会话。
- M82 与新任务/工具策略同事务保存角色定义、技能名称/contentHash/workspaceRoot/正文及时间；创建时再核对角色技能列表，避免选定集合不一致。旧任务没有此表记录时不追溯加载新角色技能。
- 首次任务与续接都引用固定的技能指令正文，附原版本及归属信息；不自动执行技能文件，也不恢复工具授权。后续 read_skill/read_skill_file/run_skill_tool 仍读取当前已安装版本，提示与固定指令核对版本差异，不宣称代码或支持文件也已固定。
- 角色编辑器补充 skills 用法。仅查看源码并编辑实现/前端/文档，未读取实际用户技能、创建子任务、执行代码、迁移或测试。角色模型/reasoning、旧 TOML 迁移、旧任务恢复及激活仍待完成。

## B3：角色模型与推理配置持久化（已编写，未执行）

- 角色定义增加 model/reasoning，支持模型 ID 或同供应商 provider/model，以及 pi 公开推理等级。创建时由 childModel 校验并复制父模型；不同供应商拒绝复用凭据，要求先绑定目标供应商。改变模型 ID 时清除继承单价，由当前世界价格配置重新解析。
- Mon 子任务继续通过原模型绑定仓库存储复制后的配置；M83 保存尘世子模型非凭据配置。尘世重启后从当前环境取得同供应商/同端点凭据，供应商或端点改变则拒绝恢复旧归属。嵌套子任务继承实际父子模型配置。
- RuntimeModel/configuredModelSchema 保存 reasoning，pi 模型描述标记推理能力并将等级传给 AgentHarness，不再固定 off。供应商是否支持模型/等级需后续实际验收，不声明自动识别支持能力；上下文/输出上限继续沿父配置。
- 角色编辑器说明模型覆盖与推理参数。只读旧模型覆盖实现及本地 SDK 声明、编辑源码/前端/文档，未启动模型、读取真实凭据、迁移、构建或测试。跨供应商独立凭据配置、旧 TOML 迁移与旧子任务恢复/激活仍待完成。

## B3/B7：旧 TOML 角色离线候选导出（已编写，未执行）

- 新增 export_legacy_roles.py，使用 Python 3.11 标准 TOML 解析器，不引入宿主 Python 运行依赖。用户明确给定用户/项目角色目录，输出独立私有审阅 JSON，保留原文、scope、来源与 SHA-256。
- 映射原角色指令、skills、model/reasoning、sandbox、预算字段和已有工具别名；未知工具/字段阻止生成可保存候选，不丢弃拒绝项。原只读默认工具名单映射成明确集合，避免新读取工具自动扩权。
- 限制单文件/目录文件数/总字节，校验源文件类型、打开身份及读前后元数据，输出排他新建并 fsync。解析异常保留原文与待审阅说明，候选最终仍由 TS API schema 验证。
- 操作文档明确当前用户候选可经现有角色编辑器审阅保存，项目候选不能自动提升到用户作用域；项目角色优先级与自动导入仍待完成。只编写脚本/文档，未执行脚本、读取真实角色文件、迁移或测试。

## B3/B7：项目角色范围、优先级与覆盖移除（已编写，未执行）

- M84 增加按 workspace_root/name 保存的项目角色，历史记录带工作区。角色列表/新任务解析按当前项目→用户→内置优先级执行；其他项目同名配置不参与当前解析。每个作用域限制 256 个自定义角色。
- 新增 agent.roles.edit，返回目标作用域当前 revision 与工作区，而非误用下层继承定义的 revision；保存和移除同时核对工作区/版本。用户范围编辑不会被项目同名覆盖，项目范围未配置时可从下层定义建立覆盖。
- 前端可选用户/项目范围，显示保存项目；新增移除覆盖入口，删除留历史并恢复下层解析，不删除既有任务策略/模型/技能快照。旧 TOML 操作文档补项目候选的正确保存路径。
- 仅查看和修改源码/前端/文档，未切换真实工作区、保存/删除真实角色、执行迁移或测试。目录批量导入、旧任务恢复、跨供应商配置与迁移激活仍待完成。

## B3/B7：旧角色审阅文件的批量确认导入（已编写，未执行）

- 新增批量预览/应用 RPC，M85 保存私有计划、原文/来源/摘要、预期版本与应用结果。最多 32 条/512 KiB、最多 16 个有效预览，15 分钟有效；不从来源路径自动读取宿主文件。
- 预览检查原文 SHA-256、候选 schema 与同作用域名称去重，返回实际项目根、覆盖状态和全部候选定义。确认后复用 saveInTransaction，整批角色/修改历史/应用回执原子提交；任一冲突整体回滚，成功 preview ID 幂等返回原结果。
- 前端读取离线 JSON 并筛选候选，显示不可用项，按批预览并明确确认。项目候选保留项目范围，只在当前项目根保存；不会自动提升为世界用户配置，不启动任何旧任务。
- 散列不替代映射语义审阅，服务端明确把 definition 当用户确认的配置。仅编辑源码/前端/文档，未读取真实审阅文件、执行预览/导入 RPC、迁移、模型或测试。incomplete 暂存库仍缺激活/审阅运行模式，旧任务整体恢复继续推进。

## B3：排队子任务的工作区归属与缺失恢复（已编写，未执行）

- M86 固定每个新子任务创建时的工作区，策略解析和模型/工具预算准入核对任务及祖先归属；排队后切换项目会拒绝在新根执行，要求回到原工作区。既有任务不根据当前目录猜测历史归属。
- 新增 agent.workspace.restore 与前端核对入口，仅未运行且缺失/空归属的任务可明确绑定当前工作区；校验当前选择、父任务根及已有归属，保存独立恢复记录。已绑定其他项目的任务不能用此入口悄悄迁移根。
- 子任务详情显示固定根/尚待核对状态，续接前也检查归属；恢复根不恢复模型/权限、不打开旧子会话或重排任务。只读 WorkspaceRepository 公共接口用于跨模块核对。
- 仅编辑源码/协议/前端/文档，未切换工作区、恢复实际任务、执行迁移、模型或测试。旧上下文/模型/策略整体恢复及迁移激活仍待完成。
