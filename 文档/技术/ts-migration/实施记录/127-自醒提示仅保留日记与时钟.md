# 127 自醒提示仅保留日记与时钟

2026-09-21，按用户要求进一步简化自醒提示，不继续叠加行动要求。

自醒主提示与 eden-self-awake 技能共用同一短文本：

> 写下日记，正文会自动保存。用 set_self_awake_timer 设置下次醒来的时间。

删除心情/在意的人/小事等写作引导、活动顺序、环境历史查询及联系/计划技能导引。自动保存说明用于解释现有正文落库机制。当前时间、会话及已保存时钟等结构化信息仍按原上下文传入，其他独立技能、权限和工具运行逻辑不在本次修改范围。

更新既有提示测试，移除对旧“暂停则保持暂停”文案的断言，保留结构化状态传递检查；4 项相关测试和 Server 类型检查通过。待空闲后重启开发宿主加载。未新增真实自醒。
