# 79 基础技能真实宿主回归

日期：2026-09-14，用户明确要求开始真实测试。

## 范围与方法

使用独立临时数据库及真实 Node 宿主、HTTP 模型传输、WebSocket RPC、技能子进程和审批持久化。模型端使用本地录制响应，不调用付费供应商，不把确定性链路验证当作真实模型自主选择技能的质量验证。未操作真实用户数据库、未重启当前应用或发布。

运行命令与日志：

- `npm run test:server`：递归服务端及共享包回归，运行时发现 134 个测试文件；日志 `.artifacts/builtin-skills-live/server-tests.log`。
- `node --test Script/Project/server_artifact.test.mjs`：构建后的双世界宿主在临时 cwd 启动，验证四份基础技能分发、目录不带正文、跨世界隔离及进程重启后的持久化；日志 `artifact.log`。
- `node --import tsx --test Server/tests/integration/capabilities/builtin-memory-live.test.ts`：本轮新增的真实宿主记忆链路；日志 `memory-live.log`。此用例在全量测试启动后新增，因此单独运行，未计入当次全量发现结果。
- `npm run typecheck:server`：服务端及测试类型检查；日志 `typecheck.log`。
- `npm run check:architecture`：仍存在原有两项 Mon 复杂度违规，分别为 model-binding.ts:186 和 model-catalog.ts:14；日志 `architecture.log`。

## 新增场景

1. 启动真实宿主，以临时角色进入会话。
2. 读取 eden-memory 的正文，核对读取前后的工具接口集合一致。
3. 调用 remember_memory，经真实权限请求批准后写入该角色的长期记忆。
4. 卸载技能说明，核对工具接口集合仍保持一致。
5. 关闭并重新启动临时宿主，使用同一会话调用 search_memories。
6. 验证返回的内容及 scopeKey，与原角色一致；后续一轮没有重新加载技能。

新增场景通过，审批发生一次，宿主 faultCount 为 0。构建后的双宿主场景也通过。

## 最终结果

服务端及共享包全量 370/370 通过，0 失败、0 跳过，约 251.7 秒；新增记忆链路 1/1、独立制品 1/1 通过，本轮共 372 项。新增测试纳入递归发现，下一次全量将自动包含。最终类型检查通过；主仓库和 Server 的 git diff --check 通过。

本轮没有发现需修改生产实现的运行失败，仅补充回归测试和证据记录。架构检查的两项既有 Mon 违规未消除，不能声称所有工程门禁通过。真实付费模型的自主技能选择、完整 UI 操作和线上部署不在本轮验证范围内。
