# 近期自醒分页显示完整记录

2026-09-11，用户批准修复概览列表每天只显示五条的问题。

- 移除 `SelfAwakePage.tsx` 概览日期组中的 `slice(0, 5)`，展示当前已加载的全部记录，列表继续滚动浏览。
- 核对既有分页：每页 20 条，“查看更多”请求下一页，按运行 ID 去重追加；日期分组在合并后的完整 runs 上计算，同一天跨页记录合并到同一组。无需修改后端分页协议。
- 日记视图原有展开行为保持不变；本次只修复概览记录被隐式截断的问题。
- 未新增单行展示改动的实现镜像测试；执行前端类型检查。未进行浏览器交互验收。
