# 实施记录 07

## B1：活动上下文与后台会话归属（已编写，未测试）

- get_self_awake_context 增加 request、desktop_window、desktop_session、audio_state、recent_events、recent_diaries、recent_contacts 分段读取；复用 Electron 既有活动采集及 Core activity-presence 接口，不新增重复采集器。
- 个人活动仅向已持久化内部触发记录所属的 Mon 后台会话提供，通过服务身份获取令牌；普通会话写入相同 environment 字段不能获得跨会话历史权限，local 不调用 Core 私人活动接口。
- 窗口、系统输入、会话、媒体与语音播放状态按所选段读取；返回采集/接收时间、数据年龄和过期标记。缺失数据代表未知，历史 false 不代表此刻状态，不推断声音内容或意图。
- 日记及桌面联系历史按内部触发归属跨后台会话读取，保留作者和展示/关闭时间；普通会话仅查询自身历史。外部渠道历史仍待 B5，并明确桌面关闭不等于用户回复。
- session.list 默认隐藏内部触发创建的后台会话，可显式 includeBackground 查询；仓库内部默认保留所有会话，避免影响恢复。过滤发生在 SQL 分页前，自唤醒列表与执行详情继续独立访问。
- 内部唤醒预备过程发生模型绑定或持久调度错误时关闭已创建空会话，保留审计记录。崩溃发生在预备流程中间的回收仍待恢复业务补齐。
- 本批仅编写业务代码及记录，未新增测试，未运行测试、构建、类型检查、Electron 或 Core 请求。字段兼容与桌面实机行为均待用户指定验收。
- 后续进入 B2 技能/插件管理，外部联系、Mon 调度回传和完整渠道历史统一依赖 B5 外部业务；B1 尚不宣称全部完成。

## B2：本地指令技能管理（已编写，未测试）

- 新增 skills 契约、快照读取、预览/安装仓库、业务服务、模型工具与八个 RPC；第 26 次迁移保存每世界安装快照及短期预览。
- 本地源保存 SKILL.md 与最多 256 个文件、8 MiB 总量的不可变内容快照；安装使用预览快照，十五分钟过期，更新前比较原内容哈希，防止预览覆盖另一次内容更新。目录层级、单文件与正文大小有界，拒绝链接及特殊文件。
- 技能按需发现/读取，停用或禁止模型调用时模型不可读取正文及附件；附件按已安装精确路径返回，不把路径当作宿主任意文件访问。内容说明不授予执行或外发权限。
- create_skill 经 skill.write 审批生成指令技能；RPC 复用现有前端本地预览/确认安装入口。新增独立 SkillManagement 组件接入启停、确认卸载和正文查看，未继续把管理逻辑堆入大页面。
- 当前仅接入用户范围本地指令技能；Git 获取、绑定项目范围、完整 frontmatter/manifest、内置发现及代码工具执行继续迁移，不能把这一批称作完整技能功能。现有 Git/项目请求明确返回未接入错误。
- 所有新增业务未测试；没有运行构建、类型检查、技能脚本或真实安装操作，也没有新增测试代码。

## B2：Git 技能来源获取（已编写，未测试）

- 新增 execution/git-source 与 skills/git-source：凭据为空的 HTTPS 仓库通过受控 Git 参数在 bubblewrap 中获取，不接受模型命令字符串；不读取用户 Git 配置、不启动凭据提示、不启用钩子、子模块递归或 HTTP 重定向。
- 获取流程仅挂载临时源目录、系统程序与 TLS/DNS 必需资源，并显式共享网络用于 HTTPS 获取；不将会话、工作区或世界数据挂入获取沙箱。该入口是用户确认的来源预览流程，不作为模型通用网络执行工具。
- 分支或标签解析为固定 commit，预览保存该 commit 与读取后的内容快照；安装仍使用预览内容，不再次获取浮动分支。符号链接和包大小继续由快照读取器约束。
- 同时最多两个预览，进程有时间、内存、输出与单文件大小上限；成功或失败均清理临时目录，宿主关闭取消获取并等待收尾。总仓库磁盘配额和非 Linux 获取实现仍需后续补齐。
- 项目范围安装继续待实现，需按绑定工作区区分安装身份与读取可见性，不能仅修改 scope 字段。完整元数据与可执行技能也仍在 B2 待办中。
- 没有执行 Git 获取、安装、测试、构建或检查命令；以上为实现记录，不是运行验证结果。

## B2：项目范围技能（已编写，未测试）

- 第 27 次迁移将技能安装主键改为名称与工作区路径，已有用户技能保留在空工作区键下；预览新增工作区归属。项目技能存入该世界数据库，无需把安装文件写入用户项目。
- 用户范围与多个项目可分别保存同名技能；查询优先当前项目，项目技能停用后不回退同名用户技能，避免隐式启用另一套说明。切换工作区后不会读取其他项目安装记录。
- 预览开始时绑定规范工作区，获取结束和安装提交时重新核对；没有绑定工作区时拒绝项目范围安装。Git 与本地源共用此规则。
- 启停/卸载支持页面提供预期内容哈希及工作区归属，当前页面已接入；不匹配时拒绝修改并要求刷新。技能页订阅工作区变化重新读取，管理组件仍按职责独立。
- 此批未新增或执行测试、构建、数据库迁移或真实安装。完整 manifest、可执行技能、内置技能发现及插件版本管理继续实施；未声称 B2 完成。

## B2：技能元数据与代码执行（已编写，未测试）

- 使用已存在的 yaml 2.9.0 解析 SKILL.md frontmatter，补多行文本、布尔字段及 metadata.edenagent 的展示名、版本、工具、profiles、声明权限和默认提示；限制别名展开并拒绝重复键、未结束头部及格式错误。
- 加载 tools/*.json 的 v1 工具清单，保留命令、测试命令、输入/输出契约、描述及超时；校验包内命令路径和重复名称。当前 JSON Schema 复用插件 SDK 的受限关键字集合，更广旧契约仍需迁移，不默默忽略未知约束。
- run_skill_tool 接入已安装快照，按当前 user_chat/self_awake profile 筛选，执行经过 skill.execute 审批，审批内容包含版本、命令、参数和声明权限，审批后重新核对技能版本及工作区。
- 沙箱只读挂载技能快照，不挂用户工作区或世界数据，不开放网络；输入从 stdin 传 JSON，限制运行时间、内存、文件、输出，退出码和输出契约不合格明确失败。临时文件在收尾清理，宿主关闭会取消在途执行。
- 本轮没有执行技能、testCommand、测试、构建、依赖安装或类型检查。testCommand 仅保存，后续按用户要求集中编写和运行测试时处理；不将声明测试当作已通过。
- 内置技能发现、直接工具名称发布/状态与缺失依赖展示、完整旧 Schema 兼容及插件版本管理仍待完成。代码入口已编写不代表全部技能能力已验收。

## B2：插件开发页面与版本管理（已编写，未测试）

- plugin.list/read 改为向旧管理页提供完整安装信息，版本摘要另由 plugin.version.list 返回；补草稿列表/读取、版本源码与历史测试报告读取、启停、版本切换和卸载入口。
- 新 PluginManagementRepository 负责管理 SQL；卸载在事务内删除激活、授权、版本、报告和草稿，存在执行/构建操作时拒绝卸载，不先删除正在使用的制品。
- PluginDevelopment 独立组件提供清单/源码编辑、保存、验证、声明测试、安装、版本查看及切换按钮。安装继续要求精确 revision 的成功测试记录，页面不自动串行执行验证或测试。
- 工作区授权按插件和 revision 保存；开发面板可显式授权指定只读目录后激活。旧权限编辑入口已适配 workspace.read，拒绝未声明能力和无效范围。
- 插件市场读取失败不再阻止已安装插件与开发面板显示；错误显示在页面，不伪造市场列表已加载成功。签名来源/安装预览、版本差异和运行日志仍继续实现。
- 所有变更仅已编写，没有运行插件、源码验证、测试、构建或 UI。保留旧用户修改，尚不宣称完整插件管理已验收。

## B2：插件版本差异与运行记录（已编写，未测试）

- 第 28 次迁移增加 plugin_operation_log，记录验证、声明测试、安装、激活和调用的开始/结束、精确版本及状态；安装整体进入活动操作跟踪，避免构建结束与安装提交间被卸载穿插。
- 声明测试报告 passed=false 记录失败，取消与异常分别保存；重启遗留 running 标为 interrupted，不依据日志重放未知操作。日志不保存用户参数、插件输出或原始异常内容，仅保留固定错误分类；具体测试报告由版本详情另行读取。
- 日志按单调 seq 分页，不因同毫秒多条记录跳项；插件卸载保留历史操作记录。当前记录覆盖异步执行链路，草稿编辑与同步授权/停用审计仍待补齐。
- plugin.version.diff 对两个已安装版本读取并核验内容，返回清单与源码的替换块；保留公共头尾行，使用线性空间，块内可能包含未变化行，不宣称最小差异。
- 新 PluginHistory 组件提供两个版本选择、清单/源码对比、运行记录刷新和继续加载；独立于草稿编辑，避免继续扩大单文件。
- 本轮没有运行插件、测试、构建、迁移、类型检查或 UI；以上仅代码实现，市场来源与签名、旧插件包安装、技能发现和工具状态继续待完成。

## B2：签名市场来源与索引（已编写，未测试）

- 第 29 次迁移保存每世界 Ed25519 可信公钥与市场来源；新增密钥读取/添加/撤销及来源增删、刷新、发布列表 RPC。密钥 ID 不允许替换为不同公钥，撤销时使相关缓存索引失效。
- 复用旧市场 v1 envelope 的字段及 serde 序列化顺序验签，严格绑定来源 keyId，校验时间窗、摘要格式、重复插件/版本及撤销记录；保存摘要和索引生成时间，拒绝回退和相同生成时间的冲突索引。
- 获取只接受无凭据 HTTPS，拒绝重定向，流式限制 4 MiB，四个并发来源上限，同来源刷新合并，关闭取消在途请求。来源编辑/移除或密钥撤销后，旧请求不能重新写回缓存。
- 旧市场管理页可以调用来源与发布列表接口；密钥管理 UI、摘要固定的包下载、包验签、预览和安装继续待实现。索引验签不代表包验签，当前不能宣称完整市场安装闭环。
- 未请求真实市场、未运行验签样本、测试、迁移或构建；历史 Rust 签名兼容性留待用户指定验收。

## B2：公钥界面与插件包完整性读取（已编写，未测试）

- 新增共享公钥契约和 PluginMarketKeys 独立界面：填写公钥/ID、查看 SHA-256 指纹、显式信任与确认撤销；只处理公钥，不收集私钥。
- 新增有界 ZIP 内存读取器，限制归档 72 MiB、文件数 520、解压总量 64 MiB；拒绝路径穿越、链接、特殊文件、重复路径、加密、多卷及不支持的压缩类型，不将归档路径直接写入磁盘。
- 包完整性校验要求 checksums.json 精确覆盖内容文件，聚合文件名与摘要后校验 Ed25519 签名，按旧逻辑计算包 revision；包签名密钥必须独立处于可信状态，索引可信不能替代包签名。
- 下载实现固定 signed index 中的 SHA-256，核对包 ID/版本/revision，拒绝过期或撤销发布，下载结束重查来源及索引身份。该下载模块尚待接入持久预览与安装接口，不宣称下载按钮已完成。
- 未访问市场、解析实际归档、执行验签测试、迁移、构建或 UI；ZIP/签名与旧制品兼容性等待用户指定验收。持久预览、旧组件执行及包安装继续实施。

## B2：市场持久预览入口（已编写，未测试）

- 第 30 次迁移保存已验证包文件快照、来源、签名密钥、revision 和十五分钟有效期；同时最多保留四个预览，过期记录在新增时清理，提供丢弃接口。
- plugin.market.inspect 接通已有“下载并审查”按钮，最多两个并发包下载；宿主关闭取消并等待下载，下载完成才保存预览。
- 校验旧包组件 manifest 的字段、重复 ID、技能/运行时/UI 文件引用与 hook 目标；读取预览时重验文件与签名，并核对来源未换代、发布未撤销、索引未过期。
- 新 PluginPackagePreview 独立组件显示来源、包摘要、签名状态、有效期、组件和声明权限，避免界面只显示一个名字就要求确认。
- 插件包安装及组件运行尚待接入；持久预览不会伪装成已安装，宿主版本区间及各组件完整约束也待补齐。当前不宣称市场闭环完成。
- 没有访问真实市场、执行下载/验签样本、数据库迁移、测试或构建；所有新增链路只已编写。

## B2：插件包版本保存与安装入口（已编写，未测试）

- 第 31 次迁移保存组件包的不可变内容、manifest、来源、签名密钥、安装时间和当前选择版本；plugin.install_preview 接通市场预览安装，提交前重验预览及来源，成功后消费预览。
- 默认安装并选择版本但保持停用；安装记录与版本选择同事务，列表/read/uninstall 与既有 TS 插件入口统一，提供包版本选择接口。未接入的组件启用明确拒绝，不把安装成功写成执行成功。
- 包与 TS 单工具插件禁止占用相同 ID，双向防止草稿或安装记录产生歧义；包版本选择前重新校验签名、内容和宿主版本区间，支持 major.minor.patch 形式边界。
- 列表保留原始来源与每版本签名状态；密钥撤销后显示阻止状态。后续还需包运行时的持续撤销处理、权限落盘、技能/UI/运行时/hook 启用及旧包完整兼容。
- 本轮没有真实安装、执行迁移、测试、构建或启动组件。市场“保存包”已编写，完整插件能力闭环尚未完成。

## B2：组件包 UI 与技能接线（已编写，未测试）

- 第 32 次迁移保存每版本组件开关；包版本选择后保持停用，启用前校验当前版本签名、组件文件、UI 文档及技能快照，未接入的必需权限/运行时/hook 明确拒绝。
- 声明式 UI 仅接受受限位置、文本与色调，不执行 HTML/脚本，输出复用现有插件详情及设置页卡片。组件开关与版本选择已接独立前端组件。
- 启用包的技能通过动态提供者进入现有 skill.list/read/file 与模型技能执行链路，不复制为可独立残留的用户技能；包停用/换版/卸载后不再提供旧快照，密钥撤销或校验失败时不暴露内容。
- 贡献技能保留插件/版本来源，用户与项目技能仍优先；多个已启用包贡献同名技能时明确报冲突。技能页不能绕过所属插件修改或卸载贡献技能。
- 当前支持声明式 UI 与技能，权限持久化、native/MCP 运行时、hooks、assets 和完整撤销生命周期继续实施，未缩减原业务范围。
- 本轮未执行组件、测试、构建、迁移或实际界面验收，当前变更仅已编写。
