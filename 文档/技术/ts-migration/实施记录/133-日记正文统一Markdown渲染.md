# 133 日记正文统一 Markdown 渲染

2026-09-21，用户指出日记符号没有对应格式。

原概览、日记阅读页与执行记录弹窗均以 p + whitespace-pre-wrap 展示原始正文，Markdown 没有解析。新增 DiaryContent，三个入口统一复用聊天 MarkdownContent 与现有主题 prose 样式，支持标题、加粗、列表、引用、链接、表格和代码块，保留普通段落换行。数据正文不变，没有启用原始 HTML。

验证：前端类型检查与差异检查；未新增自动化或运行视觉测试。本地 Vite 开发服务通过热更新加载。
