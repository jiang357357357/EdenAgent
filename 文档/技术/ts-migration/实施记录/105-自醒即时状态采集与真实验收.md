# 自醒即时状态采集与真实验收

日期：2026-09-18。用户授权修复、重启 Core 并执行真实自醒。

## 实现

- 自醒初始提示只携带触发原因与会话环境，移除重复角色档案、内部作业字段和空历史数组。简化日记提示，允许无新事时简短记录，最终只写日记，不另附状态报告。
- desktop_window / desktop_session / audio_state / recent_events 调用 Core `activity-presence/?fresh=1`，Core 标记本次刷新请求，桌面端每 2 秒检查请求并重新采集、上报。常规上报保留 60 秒间隔。
- Core 以数据库字段 refresh_requested_at 协调，支持跨 worker；最多等待 6 秒，只接受请求之后开始采集且之后收到的结果。超时返回 available=false、fresh=false、payload=null，不回退旧数据。
- 桌面采集开始时记录 captured_at；网络请求 5 秒超时；异步采集期间账号变化时丢弃旧账号结果。退出/注销清理刷新轮询。
- 工具输出明确 fresh、stale、采集时间与未知语义，区分窗口状态与用户是否在场。
- 历史审计记录与 Core 最新上报仍保留，本次没有删除历史数据库记录；实际工具调用结果仍按正常会话事件持久化。旧环境上报不作为当前状态输出。

## 自动验证

- Core activity-presence 5 项测试通过，使用 /tmp 下独立测试数据库，覆盖旧数据超时不回退、本次采集与刷新请求用户隔离。
- 桌面 activity-presence 8 项测试通过，含按需采集与认证上报。
- Server 提示词/提交契约 2 项通过；Server TypeScript 检查通过；三个代码仓库差异空白检查通过。

## 实际运行

已将 Core 四个实现文件同步至实际运行目录 Eden-Linux/LINUX/runtime/core，执行 Users.0006_activity_refresh 迁移并通过 monpm 重启 core。已重启 Agent 开发宿主与 Electron，Core /__ping 和两个 Agent /healthz 正常。

- 首轮 job：cf082a18-3753-4019-938f-65ed7afd11ea；run：8b94df64-bbbe-4e48-be02-8bc62cf6480e。状态读取成功，发现最终文本附带状态报告且将前台窗口解释成老师正在看界面，因此进一步调整提示与工具解释。
- 最终复验 job：d243a917-14f3-4d25-9150-d351140174ea；run：cf2ed6ed-9002-41fa-bc86-76d5716f7fe7；session：af6471fb-ae30-433d-bf22-ade6ec2c088b。
- 调月莉音真实模型运行于北京时间 13:57:06–13:57:19，completed，无 last_error，日记已持久化。
- desktop_window：13:57:10.643 采集，ageMs=33，fresh=true、stale=false；前台 Eden Agent/electron。
- desktop_session：13:57:12.643 采集，ageMs=62，fresh=true、stale=false；未锁定/挂起。
- audio_state：13:57:14.643 采集，ageMs=89，fresh=true、stale=false；系统媒体采集器未接入，media.available=false，音频播放/麦克风/摄像头未知；应用内录音/TTS=false。
- 以上是用户明确要求的真实自醒运行，按正式链路留下日记；自动化测试未使用真实用户数据库作夹具。

## 限制

系统媒体采集器仍未实现。此轮触发原因显式要求状态验证，因此日记仍偏验证叙述，模型仍写了“老师大概就坐在屏幕前，或者刚刚动过键鼠”；提示约束不等于稳定遵守，角色文风与在场推断不能视作完全验收。未删除或改写两次真实运行的原始日记，未宣称所有状态采集功能完成。
