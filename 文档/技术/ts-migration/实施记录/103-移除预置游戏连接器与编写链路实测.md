# 移除预置游戏连接器与编写链路实测

日期：2026-09-18。用户纠正验收目标：不是旧游戏连接器，而是让智能体编写连接器本身；要求彻底删除旧手写连接器。

## 清理范围

- 删除四个官方 TS 游戏连接器、模组/桥资产、专属测试夹具及测试、Linux OpenTTD 启动脚本、Windows HOI4/Victoria 安装脚本与 OpenTTD 启动辅助程序。
- 删除专用 Rust 连接器归档 `Archive/2026-09-10-rust-connectors` 和本地 dist 中四个游戏包；历史技术记录仅作为历史证据保留。
- 删除目录中的官方包扫描/回退逻辑；连接器目录只从已安装并授权的插件组件生成。移除前端按游戏名称分支的配置按钮和提示。
- Server 构建/发行不再自动构建、扫描或打包连接器。保留任意源码构建入口 `npm run build:connector -- --source <目录> <目标>`；CI 改测通用框架和编写链路。
- 删除本机已失效且只指向旧仓库脚本的 `/home/manager/.local/bin/openttd` 包装入口；没有删除游戏本体、存档或操作真实用户数据库中的安装记录。

## 实测与修复

新增 `Server/tests/integration/connectors/authoring.test.ts`。测试在仓库外的临时目录新写本地记事连接器，从开发说明开始，调用构建命令及模型可用的 manage_connector_plugins 工具检查、审批安装、启用；随后启动真实 Node worker，写入/读取中文内容，确认事件持久化，停用后调用被拒绝。

首轮实际失败：仓库外源码无法解析 `@eden/plugin-sdk/connector`。构建器增加自身工作区 node_modules 解析路径，保留源码目录自身依赖优先级并打包 SDK，复验通过。随后修正新测试误用清单 ID 的夹具错误，改为使用发现目录返回的组件 key；不引入按应用名称回退。

## 验证证据

- 通用编写/安装/运行、组件权限与不可变快照、能力输入、SDK 帧与关闭：10 项通过，无跳过。
- 通用制品复制与校验：2 项通过。
- `npm run typecheck:server`、`npm run build:server`、`npm --prefix frontend/web run build` 通过；配色门禁 229 文件通过，仍有大 chunk 提醒。
- 主仓库、Server、frontend 差异空白检查通过；活动源码与脚本中旧游戏标识和自动打包入口扫描无残留。

## 验收边界

这些是新源码、真实工具、真实 worker 与临时数据库的运行证据，**不是线上模型自主编写的证据**。当前隔离测试环境没有 EDEN_AGENT_MODEL 或供应商 API 凭据，没有借用/复制真实账号数据库做夹具。模型自主需求理解、源码生成与自主修错仍待配置测试模型后验收。所有本轮测试进程和临时目录均按夹具清理。

没有重启当前用户宿主或卸载真实账号内的历史安装；源码更新需重启宿主生效，已安装包快照不随源码删除自动删除。
