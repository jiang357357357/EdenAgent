# 记忆大厅完整取景

用户截图显示缩小到 62% 后，记忆大厅仍呈现被裁切的窄矩形。原因是原 Spine 记忆大厅按 cover 和 1.08 overscan 填满竖向面板，外层 CSS 又缩放整个已裁切画布。

调整：可调整角色容器通过 context 向 Spine 传递位置与缩放，保持画布大小固定，在 Spine 场景层应用变换；缩小时可显露完整场景。聊天可调整视图内的记忆大厅使用 contain、取消默认 overscan/y bias，其他使用者保留原取景。变换更新仅调用 refit，不重载资源或重启动画。静态图片仍沿用外层变换。

前端类型检查通过；复用 Spine 布局专项测试。未实机验证具体素材。已有本地 62% 等配置保留，点击重置恢复完整取景基准。
