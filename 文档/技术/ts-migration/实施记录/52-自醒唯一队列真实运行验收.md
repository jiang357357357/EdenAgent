# 自醒唯一队列真实运行验收

2026-09-11，用户明确要求开始真实测试。本轮通过实际模型会话、运行中的 MonOs 和正式服务协议进行验收，未手工插入数据库夹具，未修改智能体提示词或工具描述。

## 实际环境

- Agent：本仓库 `Script/Project/dev.mjs`，伊甸园 40092、尘世 40093，Web 40091 和 Electron。
- MonOs：`/home/manager/work/Eden/LINUX/runtime/os/monos-rust`，Core 来自同一 LINUX 安装，接口 40011。
- 先只读确认没有 queued/running 对话输入，再优雅重启 dev 启用修复。正式迁移将原 5 条自醒待办收敛为 1 条，其他 4 条标记取消，历史保留。
- MonOs 实际接收 Agent 发布的最新计划，`last_timer_tool_source=eden-agent`。身份、令牌通过既有本机配置读取，未写入验收记录。
- 使用正常 RPC 创建验收会话 `3858bb44-e564-4f5e-a800-518a9a5c2421`，由 Core 选择的真实 DeepSeek Flash 模型调用工具，不用模拟模型代替。

## 结果

1. 模型连续实际调用两次定时工具：A 为 1 分钟，B 为 2 分钟。A `cc5991b6-c3ed-40bd-88ee-24bf2c66e75d` 被取消，B `a7a9b0b7-239e-4bf4-b990-c0b3d5841388` 是唯一待办。MonOs 的 `next_wake_at` 更新为 B 的 12:39:10.162（北京时间）。
2. A 未执行；B 由 MonOs 在下一次轮询触发，生成任务 `14b03b89-7361-446d-b9f1-31de5c3d38c4`，attempts=1。A、B 本地计时任务 attempts 均为 0，没有形成第二条本地执行链。
3. 第一轮结束失败：模型返回 `next_wake:null`，被现有对象 schema 拒绝，MonOs 收到 failed。该失败保留记录，没有隐去或计为完整通过。
4. 通过新的验收用户消息明确现有结束协议（并非修改系统提示词）：要求 next_wake 对象并恢复正常巡检间隔。模型设置 C，任务 `64b11f07-48c3-4993-a902-17c66f61c770`，MonOs 接收触发时间 12:41:21.228。
5. C 自然触发任务 `b60e7c14-9737-4ff9-baef-1736b254d739`。运行期间向真实签名 HTTP 接口并发发送两个不同幂等键的重复提交，均返回该任务 ID，保存两条别名，没有额外会话/自醒轮次。
6. C 运行 `298c1099-3075-4b4e-abe6-dcea707faaa3` 最终 completed，日记“验收C：250分钟后再见的安静存档”入库一条；action=write_diary，should_interrupt_user=false，没有调用外部副作用工具。
7. MonOs 确认同一任务 completed，last_error 为空，正常下一次巡检为 16:52:03（250 分钟间隔）。Agent 本地 queued 自醒为 0，后续计划由 MonOs 持有。两个宿主 healthz 均 ok，自醒和任务检查通过。

## 范围与遗留

唯一待办、替换后不触发、单一调度归属、运行期间重复提交合并，以及自然定时到日记入库/MonOs 完成确认的真实链路通过。新宿主保持运行，验收会话和两轮运行记录保留。

首次模型非法结束结构仍是实际观察到的协议可靠性问题，本轮未放宽校验或修改提示词。普通验收会话还观察到一次后台记忆提取 `Model returned non-text content`，不影响定时验收结论，但未在本轮修复。不能据此宣布全部智能体业务或所有模型输出均已通过真实验收。
