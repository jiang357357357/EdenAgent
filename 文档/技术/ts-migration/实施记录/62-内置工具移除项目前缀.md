# 内置工具移除项目前缀

2026-09-11，按用户要求采用小写下划线的功能名称，编写统一规范。

- 七个内置工具改名，提示词引用、前端恢复示例及相关测试同步更新。
- 增加精确旧名映射，仅用于子智能体权限策略兼容；保留拒绝约束及原始审计。历史来源分类兼容新旧模板，角色导出脚本采用新名称。
- 插件 / MCP 来源名称继续保留，既有全局重名校验继续执行。
- 文档：[内置工具命名规范](../../Eden%20Agent%20内置工具命名规范.md)。

验证：tool-names、workspace-tools、host-command-policy、questions、attachment-tool、attachment-rpc、companion-attachments、agent-plugin、director-planning、context-sources 共 25 项通过，无跳过。测试使用隔离临时目录或内存库，包括真实文件与命令执行、录制模型调用链及插件编译调用，不等同于外部真实模型验收。前端类型检查通过，架构 682 文件、0 错误（既有规模警告保留）。

运行验收：确认两个世界无活动输入后重启开发服务，真实 Mon RPC `tool.list` 返回 54 项；七个新名称各出现一次，eden_ 前缀工具为 0。服务端类型检查及三个仓库 diff --check 通过。本轮未额外发送外部模型请求。
