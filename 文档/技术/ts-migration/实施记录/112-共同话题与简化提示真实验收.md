# 共同话题与简化提示真实验收

2026-09-19，用户授权真实测试。提交一次自醒，允许角色选择联系、做事或休息，没有要求必须发消息；最多一次向老师本人外发，不修改系统环境。

## 自动验证

- 新增 integration/self-awake/shared-topics.test.ts 两项：账号/角色限定、后台任务及内部交接排除、14 天时间窗口、五条与 Unicode 截断限制、来源日期、内部 recallQuery 不重复注入系统提示。
- memory-recall.test.ts 三项：更新为实际相关查询才能召回；保留多角色隔离、跨回合刷新与存储故障阻止请求；新增空/无关查询不召回以及不为填满预算混入无关项的断言。旧夹具缺少 Mon 账号归属导致首次两项失败，补齐临时账号上下文后通过，未放宽生产隔离。
- 模型提示词检查一项通过。合计六项通过，Server TypeScript 与差异空白检查通过。
- 测试仅使用临时目录、内存数据库及录制模型，不将真实用户数据库用作自动化夹具。

## 真实运行

job=727a6518-1bb3-46bd-ae5d-b652bc344a99；run=e790335f-1909-4195-bfd8-5ece2533de80；session=1817eb37-d7c1-4a33-a328-7220d4bd7d98。

北京时间 01:23:57–01:26:37，completed，无 last_error，日记落库。

- 初始上下文实际带入同角色此前两条用户消息：“你好”“帮我搜索普雷纳帕斯特”，各带原始时间和来源会话。输入 metadata.recallQuery 仅为这两条文本，未混入操作提示。
- 实际 load_skill 加载到新版 eden-self-awake 内容，确认不存在旧“先查打算推进任务”的指引。
- 角色读取旧日记、联系历史与当前渠道，执行只读系统查询，更新原等待中的打算。一次递归文本搜索超时，运行随后自行继续并完成，没有把超时当作成功。
- 没有调用邮件、QQ 或设备发送工具，mon_contact_deliveries 本轮无记录。角色理由为约半小时前已发过邮件，选择留出间隔。
- 保留当日 08:30 唤醒，MonOs 实际计划一致；未验证到点触发。

## 判断

话题注入、召回查询分离、新技能加载与不重复外发的链路获得实证。此轮允许安静，未外发不是发送能力失败。

行为仍未达标：角色读旧日记后再次围绕播放器/本地桥自启排查，没有接续新带入的搜索话题；日记仍偏过程汇报，仍从窗口/进程推断用户在场和正在测试，也把邮件回复不可读取解释成没有新信号。改短提示并未消除历史叙述的影响。本轮未篡改日记、打算或历史记录，也未连续追加自醒来筛选满意结果。
