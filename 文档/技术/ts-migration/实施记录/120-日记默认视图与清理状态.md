# 120 日记默认视图与清理状态

2026-09-20，针对清空后仍显示满屏历史执行、正文被误标为未写入、旧定时备注占据页面的问题。

## 修改

- 默认进入“日记”，标签顺序为“日记 / 执行记录”；左侧历史列表改名“自醒执行记录”。日记为空时说明新日记的出现位置以及历史执行入口，正文区域显示“等待下一篇日记”。
- 历史定时安排移入执行详情弹窗，明确属于当时记录；顶部仍显示当前生效的调度时间。原始记录没有删除或改写。
- 新增持久化 `diary_cleared` 标记，清空时与正文删除在同一事务内更新，通过 RPC 的 `diaryCleared` 传递，页面及弹窗显示“日记已清除，执行记录仍保留”。未写入日记的失败记录不会被标记。
- 对上次清理前尚无标记的历史记录，迁移限定为已完成、无旧决策、无日记且同回合有 assistant 消息事件的现代记录，补齐标记。完成记录依赖现代 finish 的事务性写入约定；不对所有空记录一概标记。
- 修正旧 waiting 迁移测试依赖“最后一条迁移”的脆弱假设，改为定位其目标迁移。

## 验证

- 7 项内存库测试通过：清理/隔离/后续写入、严格确认协议、已有清理记录升级及迁移幂等、原文保存、两个世界的列表、waiting 迁移。
- RPC 生成、Server 类型检查、Web 类型检查、231 文件主题检查、生产构建与各仓库 diff 空白检查通过。构建仍有既有大 bundle 提示。
- 等待无运行中输入后重启开发宿主（PID 3744686），mon 40092 / local 40093 恢复监听。
- 生产库只读复核：当前账号库及旧 mon 库日记均为 0；最新完成记录的 diary_cleared 均为 1。
- 尝试只读 WebSocket 列表复核，30 秒无响应超时，因此不宣称实际 RPC 往返或可视 UI 点击验收通过；上述运行状态以启动日志及只读数据库核对为证据。本轮未触发真实自醒或外发。
