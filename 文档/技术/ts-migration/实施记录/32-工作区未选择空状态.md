# 工作区未选择空状态

2026-09-10。资源管理器之前并发请求 workspace.info 与 workspace.list，未选择目录时后者报错 Select a workspace first；前端将正常空状态显示为红色故障。

改为先读取工作区信息，有路径才读取目录。未选择时显示中文提示及打开文件夹按钮；桌面调用原生选择器，浏览器可输入服务端绝对路径。选取仍复用现有审计会话和工作区切换流程。真实读取错误保留重试。增加 effect 失效保护，离开文件页后不回填旧请求结果。

前端类型检查与 diff 格式检查通过，未自动操作真实用户目录或进行桌面选择器验收。
