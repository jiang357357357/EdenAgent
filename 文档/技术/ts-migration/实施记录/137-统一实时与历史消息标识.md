# 137 统一实时与历史消息标识

2026-09-21，用户截图显示同轮“你好”重复。

只读核对最新会话 ceec5f27-d049-4081-b567-fe6bc303a820：仅一次输入，运行时 user message_start/message_end 使用相同 payload.messageId，通知自身 event.id 不同。

根因：createSessionEventProjector 在 message_start 强制使用 event.id，历史 apiMessage 则读取持久 messageId，实时与历史加载交错后产生两个消息实体。修复实时投影优先使用 sessionEventMessageID；历史投影同 ID 更新而不是追加，保留不同 ID 的相同文字。

验证：15 项既有消息标识、分页、乐观消息测试通过；新增实时开始→历史加载→实时结束回归通过，并验证同文不同回合仍保留两条。共 16 项不同用例分组通过；前端类型和差异检查通过。新增测试首次运行遇到 Vite 提前关闭，调整顶层加载顺序后重跑通过。没有更改真实消息数据，未额外发送消息。
