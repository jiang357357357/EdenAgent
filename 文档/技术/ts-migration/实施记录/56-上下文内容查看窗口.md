# 上下文内容查看窗口

2026-09-11，用户要求双击上下文用量图标打开内容窗口。

- 图标双击打开原生模态窗口，支持 Enter/空格键打开、Esc 和关闭按钮退出，复用项目 SessionPanelDialog。
- 展示最近一次实际模型请求的消息、系统指令、工具定义与完整请求；文本分块直接显示换行，多角色可切换各角色最近请求。当前未发送文字单独列出。
- 明确标注数据来自最近请求，尚未发送文字不伪装为已发送内容，后续回复和修改在下一次请求中体现。查看和刷新不触发模型调用。
- 新增类型化 `session.context` 只读 RPC，读取当前会话每个角色最新 model.request，复用现有内容寻址快照还原。先校验当前世界会话存在，已删除/不存在会话不能读取。不扫描或传输整段流式事件历史。
- 更新协议并执行 RPC 客户端生成。Server 和 Web 类型检查通过；架构 676 源文件、0 错误。专项验证角色最新快照、跨会话隔离、完整内容还原、空会话与删除/不存在会话拒绝；修正异步异常测试写法后通过。
- 已重启无活动输入的开发宿主。真实 WebSocket 调用验收会话返回 1 个最新角色请求、18 条消息、54 个工具定义，内容去重引用已还原。未进行浏览器鼠标交互自动化验收。
