# 子任务图标与弹窗

2026-09-10。按用户对顶部入口的统一要求，将子任务文字按钮改为图标，任务数量非零时显示角标；悬停和无障碍名称保留数量说明。

原绝对定位下拉面板改为原生 dialog 模态弹窗，内容独立滚动，支持 Escape、关闭按钮、焦点约束和恢复。现有创建、查看、续接、中断、预算及恢复操作保留；关闭窗口不取消后台任务，不清空当前组件的表单状态。

前端类型检查及 diff 格式检查通过，未执行桌面实机交互验收。
