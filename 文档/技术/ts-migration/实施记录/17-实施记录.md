# 实施记录 17：协议与产品整合

## 浏览器 RPC 生命周期（已编写，未运行）

- 实际阅读生成脚本、模板、当前 rpc-transport/rpc-contracts 以及发行脚本，发现模板原实现缺连接关闭拒绝、请求超时与旧 socket 隔离。
- 新 rpc-client.ts 持有连接、请求和通知状态。当前前端直接引用，模板重导出；生成脚本摘要纳入客户端文件。未手改生成文件或执行生成器。
- 连接建立 15 秒超时，请求默认 120 秒超时；关闭清理全部计时器，每连接只发布一次关闭。旧 socket 事件不能污染新连接，序列化/发送异常清理请求。超时/断线只结束本地等待，远端执行结果仍需通过业务记录核对。
- 响应对象/协议版本与 result/error 互斥校验、未知响应忽略、通知回调异常隔离、安全整数转换和 256 待处理请求上限。
- 操作仅 cat/grep/ls 读取源码，shell 写入新源码与 Python 文本编辑；未运行任何业务、测试、类型检查、构建或生成器。
- 待集中测试覆盖连接初始关闭、初始化失败、关闭后重连的旧事件、发送失败、迟到响应、并发请求、通知异常和超时后业务状态核对。此阶段不编写或执行这些测试。

## 剩余工作

完整旧业务工具/协议事件覆盖与平台发行仍待补齐；既有业务实现未运行验收。P0–P5 目标保持进行中。

## TS 桌面发行链路（已编写，未运行）

- 现有 Windows desktop workflow 仍构建旧 eden-agent-server.exe，并检查旧 skills/manifests 路径。替换为手动触发的 Linux/Windows x64 matrix：固定 Node、协议生成、TS 宿主与迁移工具、独立官方 workers、连接器资源、Node 发行目录、Web、Windows pointer observer、Electron 组装。
- 发行流程本身不运行测试；原测试 CI 保留独立。Windows Rust workflow 只构建独立 native connectors，移除旧宿主二进制项，已有原生测试步骤保留。未触发任何 workflow 或上传。
- desktop_manifest.mjs 遍历组装后的制品，记录各文件大小、模式和 SHA256，记录范围内相对符号链接；拒绝逃逸链接与特殊文件。不启动任何制品来生成清单。
- archive_desktop.mjs 将桌面目录封装 tar.gz，保留 Linux 执行位和符号链接，避免 CI 原始目录下载丢失。输出使用版本/平台/架构命名、拒绝覆盖，另写 archive 字节数与摘要，并明确 unsigned，不冒充签名或验收证据。
- Workflow 上传归档与摘要，替代直接上传目录。新增 npm --prefix frontend run archive:desktop -- <platform> <arch> 入口。
- 实际仅 cat/grep 阅读源码和 shell/Python 文本编辑。未下载、构建、打包、归档、类型检查、测试或触发 CI。
- 签名、系统安装器、更新/回退发行体验以及运行验收仍待完成；这些脚本写好不等于制品已经生成或可用。

## 发行签名与离线校验（已编写，未运行）

- 现有源码未发现自动更新器。新增 release_signature、sign_release、verify_release，Ed25519 签名绑定描述文件原始字节，并核对归档 SHA256/大小/平台版本命名。
- 校验要求外部指定可信公钥，签名 envelope 内无自授信公钥。私钥仅由显式签名 CLI 读取，不生成或复制密钥；签名输出拒绝覆盖。校验不会解压、启动或替换版本。
- 归档目录附离线校验源码，重复复制仅接受相同内容；npm 入口与桌面发行操作文档同步。CI 未配置签名密钥，不能声称已产出签名制品。
- 实际仅阅读源码和写入文件，未读取真实密钥、签名、校验制品、下载、构建或测试。
- 安装、自动更新与数据回退演练仍待补齐；文档区分应用版本回退与数据库 schema 回退。

## 独立版本安装（已编写，未运行）

- 发行描述新增 desktop-manifest SHA256 并由签名覆盖；校验返回该摘要及原描述摘要，安装不信任异步重读产生的不同描述。
- installed_files 核对文件路径唯一/相对、安全符号链接、完整文件集合、大小、摘要与 POSIX 权限。安装输入为已解压目录，核对后复制，再核对暂存副本。
- install_release 使用安装锁与独立 versions 目录，已有版本拒绝覆盖，成功原子重命名本次暂存目录并保留描述、签名和安装记录；失败清理暂存，不改变当前启动入口或 Data。
- 归档附安装工具与依赖源码，npm 入口及文档同步。自动解压与版本切换仍待实现。
- 本轮仅阅读源码和写入实现/文档；未解压、校验真实制品、安装、启动、构建或测试。

## 受管版本选择与启动（已编写，未运行）

- 签名校验拆出 verifyReleaseDescriptor，安装后的描述和独立签名可配合文件清单核对，不依赖原归档仍在磁盘。
- installed_version 提供共享安装锁、受限版本名、签名/平台/清单复核、修订比较及原子选择文件。previous 保留上一版本，回退要求与记录匹配。
- select_version 提供 read/select/rollback；launch_version 根据当前选择校验并启动 Electron，持锁直到进程退出。全部命令仅写源码，没有启动应用。
- 首次接管及直接双击/外管实例不受该锁约束，文档明确先退出；异常终止不自动删除残留锁。系统快捷方式与图形入口仍待接入。
- 发行归档附启动/选择工具，npm 命令与操作文档同步。应用版本选择不等于数据库回退，真实双世界升级/回退演练未进行。

## 受管安装直接启动与重启（已编写，未运行）

- 桌面主入口在宿主创建前识别受管安装目录，核对安装记录、所选版本、启动修订、父进程 PID、锁令牌与 IPC。直接双击受管安装不再绕过启动器锁；未安装便携版和开发环境仍保持独立入口。
- 启动器在已持有的锁中写本次拥有者，传递启动修订与令牌，并通过 IPC 接收明确重启请求。只有请求与正常退出同时成立才重启，重启前再次校验安装文件和签名。
- runtime-migration 的重启改为受管启动器协调；沿用主进程 before-quit 等待两个宿主退出，不通过强制退出跳过收尾。
- 未提供对同一 OS 账户恶意改文件的安全保证；残留锁回收、外管宿主与旧便携版接管仍需独立核对，系统快捷方式尚待编写。
- 实际只 grep/cat 阅读现有退出代码和写入源码/文档，未启动 Electron、宿主、IPC、安装器或任何测试。

## 固定 Node 用户启动入口（已编写，未运行）

- 发行工具清单集中为 release_tool_files，packager 将受管安装/选择/启动代码纳入 app 资源，后续 desktop manifest 和发行签名覆盖其内容。
- setup_launcher 在安装锁内核对选定版本，从签名覆盖资源复制 Node 和启动依赖，再逐文件核对复制摘要；固定外部已可信的发布者公钥，拒绝覆盖已有 launcher。
- 生成 POSIX sh / Windows ps1 启动入口，分别做 shell/PowerShell 字面量引用。此后启动不依赖 PATH 中 Node，仍进入受管签名校验与实例锁。
- 系统桌面/开始菜单入口、启动器自身升级与密钥轮换仍待接入；不称为已完成系统安装器。
- 本轮仅阅读并编辑源码/文档，未安装启动器、复制真实发行资源、启动应用或运行测试。
