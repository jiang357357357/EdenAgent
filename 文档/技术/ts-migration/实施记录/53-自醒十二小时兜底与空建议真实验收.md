# 自醒十二小时兜底与空建议真实验收

2026-09-11，用户要求修复后进行真实测试，并明确：12 小时是自醒保底，不限制普通任务、会议或备忘提醒的时间。

## 实现

- `selfAwakeDecisionSchema` 允许 `next_wake:null`，其他必填字段与非法对象校验保留。Agent 原样保存 null，正常结束自醒并保存日记。
- Agent 自醒工具把实际 dueAt 限制在持久化自醒基准时间之后 12 小时内。只改 `set_self_awake_timer`，没有修改通用任务 dueAt、会议或备忘提醒规则。工具审批和唯一待办规则保留。
- MonOs 新增 `self_awake_deadline`，持久化 `wake_anchor_at`、`forced_wake_deadline_at`，工具计划、结果建议、缺失计划都由现有单一调度状态统一执行截止约束，不额外建立强制队列。
- 初次升级优先从已记录的自醒事件时间恢复基准；后续由 Agent 签名状态接口返回真实 `started_at`，只有比当前基准更新的实际自醒开始时间才能推进截止。重复提交、结果回收、设置工具和单纯重启均不推进。
- MonOs 将 null 建议归一化为默认建议，再由实际开始时间的 12 小时截止约束。已有本轮显式工具计划优先于结果建议，但同样受截止限制。正在运行时仍保持单执行名额。
- 项目提示词、角色提示词、工具描述均未修改。普通会议可以排在三天后，自醒兜底不会提前触发它。

## 自动化与构建

- MonOs `cargo test --lib`：14 项通过，2 项需本地模型资源的既有用例忽略。新增覆盖 null 归一化、超时截止、重复延期、序列化重启恢复、实际激活推进及运行中不重复调度。
- Agent 自醒专项共 17 项：首次会议用例误用不支持的 kind 查询参数，修正夹具后该文件 3 项全部通过，其余 14 项已通过。
- 三天后会议用例经过自醒替换、任务恢复，仍在原定时刻才能领取；并验证原会议任务没有被取消或改期。
- Server 类型检查通过；架构检查 675 个源文件、0 错误，保留既有规模提醒。MonOs release 构建成功。
- 时间边界采用可控时间验证，未等待现实 12 小时，也未更改机器时钟或生产状态时间戳。

## 真实验收

通过正常 RPC、真实 Core 模型和自然 MonOs 定时执行，不向真实库插入夹具。

1. 重启 Agent；将本次编译的 MonOs 更新到 `/home/manager/work/Eden/LINUX/runtime/os/monos-rust`，由 LINUX MonPM 托管，旧二进制备份于 `/tmp/monos-before-self-awake-deadline`。没有发布或上传制品。
2. 真实模型连续请求 4320、5760 分钟后的自醒，实际任务 dueAt 均为 `1789144907095`，即原自醒基准之后 12 小时。第一次取消，第二次保留，延长请求没有移动截止。
3. 普通接口创建三天后会议，memo ID 2，提醒时间 `2026-09-14T05:13:19.704Z`。自醒工具调用及 MonOs 更新后，会议及其任务时间仍完全相同，状态 active/queued。核验后通过 RPC 归档该验收专用会议，避免未来弹出测试提醒。
4. 模型设置一分钟后自醒，触发真实任务 `60ef5765-c038-4a20-9888-710ab860d493`。运行 `8a5eb61c-7949-4551-87cd-c13940f9fef1` 的真实结果包含 `next_wake:null`，状态 completed，日记入库一条，未调用外部副作用工具。
5. 实际开始时间为北京时间 13:15:44.725；MonOs 确认 completed、last_error 为空，截止和下一次计划均为次日 01:15:44.725，精确相差 12 小时。
6. 再次真实重启 MonOs，基准、截止、下一次计划和最后任务 ID 均保持不变。两次测试重启仅临时抑制启动巡检，以单独核验重启不会延长截止；每次进程启动后恢复磁盘原有 STARTUP_WAKE_ENABLED=true 配置，未来正常启动行为保留。

自醒兜底由运行中的 MonOs 实施，受服务在线和正常轮询周期约束，不代表停机或机器休眠时仍能执行。真实分钟级链路和重启验证已完成；12 小时边界由时间测试覆盖。本轮仅访问本机服务，未使用代理，未提交或推送。
