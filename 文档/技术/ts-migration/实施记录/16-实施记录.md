# 实施记录 16：子任务恢复业务补全

状态：源码与文档已编写，未运行验证。承接 15 的 M104 续接实现。

## 历史续接放弃

- 输入为 sessionId、旧消息 id、当前 fingerprint、说明和独立 confirmAbandon。仅 followup_prepared 可归档，已提交作业必须走任务停止入口。
- 新 M105 subagent_mailbox_abandonments 保存原始行和操作依据；保留 M92 准备记录，不覆盖 consumed_at。相同请求重试返回原结果，不同依据拒绝。
- 宿主事务核对归属、指纹、状态、提交记录后写审计和归档状态。与执行的事务互斥，先成功的一方决定状态。
- 前端增加独立说明与确认，不复用执行勾选。迁移审阅允许放弃，不允许执行。

## 迁移前预算与新账单并存

- 新 baseline-ledger 负责读取模型请求、预算归属及回执，要求金额齐全且请求已结算。无归属回执、缺失回执、未决请求阻止确认。
- 历史下限 = 当前累计值 - 当前任务及后代归属的新回执累计。输入仍是迁移前总量，已知历史字段保持不变；保存为历史确认值 + 新回执值。
- 指纹绑定任务、后代、原始迁移信息及新账单，写事务重新核对；M89 审计附账单快照。预览增加单列新账单金额，前端明确禁止重复计入。
- 保留从后代到祖先的核对顺序。后代历史值增加时，祖先历史累计需重新确认，不能直接累加导致重复。祖先正在执行或排队输入时拒绝变更。
- 不修改回执，不把缺失费用当零；安全整数范围之外拒绝保存。

## 操作与待验收

- 实际仅使用 cat、grep、find、ls、git status 阅读源码/状态，以及 Python 文本编辑；未执行测试、构建、类型检查、迁移、数据库查询或模型请求。
- 待用户要求测试后覆盖：执行与放弃竞争、重复确认、跨会话拒绝、混合账单去重、回执缺失、历史祖先累计顺序、指纹失效及金额溢出。
- 全部业务迁移仍未完成，本记录不构成最终验收证据。

## 多角色父会话创建子任务（已编写，未运行）

- 移除多参与者会话的一概拒绝。agent.spawn 可提交 actorId，宿主核对其唯一匹配当前参与者，子会话仅保留该参与者。多角色未选或重复角色拒绝创建。
- runtime 工具工厂已有 actorId，现传入子任务工具。模型可填写的 spawn schema 不含 actorId；宿主固定当前发言角色并将其纳入审批内容，模型不能指定其他角色的凭据来源。
- ModelService 从选定角色 main/vision 绑定生成独立单角色子任务快照，再应用固定任务角色的模型名与推理设置；角色模型仍不能跨供应商复用凭据。
- M106 parent_actor_id 持久保存模型来源，创建幂等摘要纳入该 ID，数字/字符串同一 ID 归一化；未提供 ID 的旧请求摘要保持原格式。恢复模型候选使用已保存来源，不能改用其他角色默认绑定。
- 子任务界面加入父会话角色选择及已创建任务来源显示，选择变化更换创建幂等键。用户的三处自唤醒文件未改动。
- 仍待实现：没有父角色来源的旧任务显式来源核对；跨供应商独立凭据配置。此轮不宣称这两项已完成。
- 实际仅阅读和编辑源码/文档，未执行生成器、测试、类型检查、构建或模型调用。

## 旧任务模型来源确认（已编写，未运行）

- 增加 agent.recovery.model.sources 返回父会话 ID 与保存的角色 ID，不返回模型凭据。模型预览和确认允许明确 actorId，界面读取当前父参与者并显示候选来源。
- 候选核对父参与者唯一性、子会话已经恢复的单角色上下文和当前模型绑定；选择不会自行修改旧角色上下文。缺少上下文依据时先完成原恢复流程。
- 指纹纳入 actorId，M107 subagent_model_source_reviews 保存来源和确认依据；同事务写模型快照、来源 ID 和待重开状态。重开时重新核对，绑定变化须重新确认。
- 同一快照再次确认可重试；更换快照时原模型确认进入历史表，来源审计保留首次记录。已在旧版本预览的指纹会失效，重新预览即可，不迁移或猜测旧确认。
- 审阅模式可读取来源及确认配置，不启动模型。前端修改选择会清空原预览与勾选。
- 实际仅 cat 阅读源码和 Python 文本编辑，未运行测试、SQL、构建、类型检查、生成器或模型调用。
- 下一批仍需独立跨供应商配置，以及业务清单中其余协议/前端/桌面发行覆盖；P0–P5 保持进行中。

## 尘世独立供应商模型目录（已编写，未运行）

- M108 local_model_profiles 保存完整 provider/model 配置和独立凭据；local_child_models 增加 independent 标志。所有入口核对 local 世界，不接收或读取伊甸园凭据。
- 列表剔除 apiKey，只给出配置元数据、修订号和是否存在凭据。保存/删除使用 expectedRevision 防止覆盖并发修改，边界为 configuredModelSchema 的地址、容量及配置大小约束。
- 子任务固定角色指定完整 provider/model 时优先匹配明确配置，可跨供应商；找不到配置仍走原同供应商继承，不复用其他供应商凭据。已创建独立子任务及其后代保留独立快照，不依赖全局默认供应商。
- 模型恢复预览同样可使用独立配置；模型凭据仅进入私有快照，不进入公共预览/事件。此处遵循当前宿主私有数据库存储方式，不声称操作系统钥匙串或静态加密已实现。
- 子任务面板增加新增、编辑、删除界面和独立确认；凭据不回显，保存需重新输入，留空明确移除该配置凭据。删除目录不撤销已经持有独立快照的任务，界面已说明。
- 伊甸园仍经 Mon Core 管理模型归属，未将尘世独立配置入口开放到 Mon；Mon 子任务独立实体选择仍待补齐。
- 本轮只阅读源码并编辑，未新增或运行测试、构建、类型检查、迁移、模型调用。上述实现未验收。

## 伊甸园子任务独立模型实体（已编写，未运行）

- 读取 Mon 原模型选择流程确认其 PATCH 会修改角色或默认模型。因此新增子任务专用目录与绑定入口，只 GET 当前已验证 Core 连接有权读取的实体详情，不修改远端默认模型。
- M109 mon_child_models 按父会话和完整 provider/model 保存实体 ID、私有绑定、Core 连接指纹及修订号。保存比较连接指纹与 expectedRevision，避免异步读取期间跨连接串用；列表只给出模型键、实体 ID、修订及连接是否仍匹配。
- ModelService 要求父模型仍有已确认绑定，并按当前父角色确定继承上下文；任务角色指定完整模型键时使用父会话已登记的独立实体，允许不同供应商且使用其自己的凭据。视觉继续来自对应父角色。
- 目录项作用域仅当前父会话。子任务拿到独立 main 快照，后续同供应商继承沿用此快照；不会把父会话整个模型目录或 Core 凭据自动授予后代。
- 前端子任务面板增加目录读取、实体选择、明确绑定、比较修订移除。相同 provider/model 的不同实体占同一槽位，界面同时显示实体 ID，替换需当前修订。
- 凭据不进入公共 API 或事件，目录修改不改写已创建任务。迁移审阅未开放网络目录和绑定操作。
- 本轮只 ls/grep/cat 阅读源码及 Python 文本编辑，未调用 Core、模型或数据库，未测试、构建、类型检查、生成客户端。
- 仍需完整业务基线覆盖、跨平台发行、协议生成和最终集中测试代码；本项不是最终验收。
