# 自醒模型请求分组

2026-09-10 用户指出无法看到单次 API 请求与工具调用的关系。此前概览平铺工具，执行记录为缩小体积省略了 model.request。

后端保留 model.request 的 requestId/model/provider 摘要，SQL 层不读取完整提示词和工具定义。前端按有序请求、响应、工具事件分组，显示请求总数、各请求工具数及没有工具的响应；缺少响应关联的调用单独显示，不假造分组。新组件与解析模块独立文件。

真实只读查询验证本轮为 5 次请求、工具数 [2,3,2,1,0]；响应仍仅约 169 KB。专项测试 3 项通过，前后端类型检查通过。需要重启 Agent Server 加载查询，未做 Electron 视觉验收。

用户后续截图显示 0 次请求：实际运行接口返回请求 0、响应 5、工具 8，确认仍是省略请求的旧查询。前端缺失请求时改为“模型请求记录缺失”，不把缺少证据显示成零调用，并提示重启 Agent 服务后刷新。
