# 上下文用量实时更新

用户反馈右下角上下文不更新。前端只处理 context.usage_updated，TS 实际发出 model.response；圆圈数字又显示输入框 token，发送后必然回到 0。

新增共享 modelContextUsage 解析 pi input/output/cacheRead/cacheWrite，实时 model.response 投影为 session.context_usage；SessionRepository 从持久化最新响应恢复相同字段。显示最近请求输入/输出及缓存数据，当前上下文为最近输入加输出的估算（不是本会话累计 token）。圆圈显示占用百分比。分类数据未提供时不填零，保留真实文本估算作为无用量时的回退。

2 项回归测试通过，覆盖缓存计数、后续请求替换、仓库重新实例化恢复及缺失值处理；前后端类型检查通过。只读实际响应解析出非零输入与缓存数据，未发起额外模型请求。刷新前端即可接收后续响应，已有会话恢复需要重启 Agent Server 加载新增查询。未做 Electron 视觉验收。
