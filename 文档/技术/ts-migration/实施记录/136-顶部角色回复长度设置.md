# 136 顶部角色回复长度设置

2026-09-21，按用户批准，在聊天顶部外观按钮旁增加回复长度图标与浮层。

- 少/中/多三档，默认中，多角色会话可切换目标角色；支持点击外部及 Escape 关闭。读取或保存失败显示错误，保存成功后更新选中项。
- 新增 ui.reply_length.get/update 合约，复用 runtime_settings 保存，键按账号与角色区分；两个世界仍各自使用独立数据库。重新生成 RPC 客户端摘要。
- 接收会话输入时从服务端读取角色偏好并保存到回合元数据，单角色及多角色实际提示注入简短长度偏好。明确本轮用户长度要求优先，保持答案完整；不修改 max token，不截断输出。
- 自醒及其他 job 回合不应用聊天长度要求；正文中的偏好只指导聊天回复，不更改思考或工具结果。

验证：前后端类型检查、差异检查通过。空闲后重启开发宿主 PID 1237635，双世界恢复监听。依照实现阶段约束未新增或运行测试，未额外调用模型做少/中/多效果对照；视觉与真实模型对照待用户验收。
