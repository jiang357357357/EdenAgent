# TS 服务端独立仓库关联

日期：2026-09-10

用户授权将当前 Server 创建为独立仓库并重新关联。

- 当前 `Server/` 注册为 `server-ts` 子模块，远程 `https://github.com/jiang357357357/EdenAgentServer.git`，分支 main。
- 在原 Rust main 提交 e6c1946 上继续提交 TS 实现，保留历史，未强推。
- 旧 `Archive/2026-09-09-rust-runtime/Server` 继续固定 e6c1946，不修改归档内容。避免对归档执行 `git submodule update --remote`。
- 新子模块包含 TS 服务端、测试、四个官方连接器及许可证。排除 node_modules 等本地制品。
- 构建继续使用主项目 npm workspace、packages、锁文件与 Script；独立 Git 仓库不表示独立构建工程。
- 拆分前后核对 458 个既有文件 SHA-256，内容一致。此次仅调整 Git 边界与说明，没有重新运行业务测试；上轮 700 项测试证据见记录 23。
- 主项目提交包含此前连接器统一所需的共享 SDK、脚本和归档变更，保证子模块版本配套。前端原有三项未提交修改保持原样。

拉取：`git submodule update --init --recursive`。修改服务端后先提交并推送 Server，再提交主项目的 Server 指针及配套改动。
