# 上下文图标放大并居中

2026-09-11，按用户要求调整输入框旁的上下文用量图标。

- 圆形图标从 4.7vh 放大为 6vh，百分比文字调整为 1.8vh，取消截断以完整显示百分比。
- 图标独立定位到输入框右侧垂直中央，随输入框高度变化保持居中；发送按钮继续位于输入框右下方。
- 仅调整布局与样式，未新增测试或进行实际界面验收。
- 验证：前端 TypeScript 检查与 diff --check 通过。

## 用户纠正

用户说明居中指水平方向，图标仍应位于原来的底部。已移除 top-1/2 和垂直平移，恢复 bottom: 1.75vh；保留放大尺寸、水平位置和圆内百分比居中。此次仅调整定位样式，diff --check 通过。
