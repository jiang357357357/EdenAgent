# TS 迁移实施记录 01

### 2026-09-09：启动

- 目录、文件规模、复杂度和依赖规则已写入工程约束文档。
- 已启动 P0，后续记录实际验证结果。

### 2026-09-09：基础宿主与插件版本链路

- npm 固定 pi 0.82.0，运行时使用公共 `pi-agent-core.AgentHarness` 和公共 Session/存储接口。
  原方案偏好 coding-agent SDK；实测需要宿主持久化屏障，因此选择公开的下层 harness，
  不访问 pi 私有成员，不加载隐式扩展或环境凭据。Eden 保留会话和副作用的事实来源。
- 18 项 TS 测试通过：真实本地 HTTP 录制模型、checkpoint 恢复、取消、follow-up、持久化故障阻断、
  SQLite 事务/世界绑定、WebSocket 鉴权/幂等及原前端生成客户端兼容。
- 3 项工程门禁测试通过；TS 类型检查及依赖/规模检查通过。构建后的两个独立 Node 进程
  已验证独立端口、健康检查与 IPC 有序退出。
- 新增 5 项 Linux 实际隔离测试通过：宿主私有文件不可见、工作区只读、环境密钥不继承、
  超时终止；插件草稿→测试→安装→激活→调用→重启→回滚；变更草稿/失败测试拒绝安装；版本独立授权。
- 插件当前是单入口 TS 工具，隔离运行而非任意宿主内 pi 扩展。激活前必须实际探测 OS 沙箱，
  没有可用后端的平台拒绝执行。当前已实现墙钟和输出限制、Node 堆限制；总内存/进程数强限制尚未完成。
- 尚未完成：全部旧业务 RPC、完整 UI、Mon 模型绑定、迁移工具、跨平台发行、真实供应商验收。
  当前测试使用临时目录和录制模型，未读取或迁移旧生产 Data，也未声称 P0–P5 全部验收。

### 2026-09-09：模型自编写插件与压缩屏障

- `eden_plugin` 提供 describe/draft/validate/test/install/activate/disable/list/invoke；
  已用录制模型实际生成源码、经过用户审批、执行测试、安装并调用返回 42。
  每次工具请求记录 revision；模型没有审批或工作区授权入口。
- TypeScript 验证运行于限时子进程，检查严格类型、默认函数入口和依赖；
  禁止私有路径依赖、引用指令和计算 import。类型通过不等于测试通过，安装必须匹配测试的源码/制品 hash。
- 插件执行叠加 bubblewrap、Node 权限限制和 prlimit：网络隔离、只读授权目录、禁止子进程，
  5 秒墙钟/CPU、1 MiB 输出、64 MiB JS 堆、1 GiB 地址空间和文件大小限制。实际越界分配和子进程测试通过。
  Windows/macOS 沙箱后端仍未实现，执行明确拒绝。Node 权限限制是额外约束，OS 隔离仍是必需边界。
- 运行中禁用/退出会取消插件调用、类型检查和测试；被撤销的工作区权限不能继续开启新调用。
  授权目录不得与运行数据重叠，调用时重新核对规范路径及版本授权。
- 进程所有权改为独立 SQLite 文件上的内核锁，强制杀死宿主后无需手动删除锁；已通过双拥有者拒绝及 SIGKILL 恢复测试。
- 对话新增历史分页、改名、参与者资料、关闭、逻辑删除、手动压缩。每个排队输入持久化角色/环境快照。
  崩溃时已运行输入标记 interrupted，可能完成的副作用标记 unknown，同会话后续队列 held，不自动重放。
- 实测发现 `before_provider_payload` harness 钩子不覆盖手动压缩；改为公开 provider API 包装的 onPayload 屏障，
  同时统一取消信号、禁用隐式重试和请求预算。真实录制 HTTP 压缩、写入失败不联网、取消保留原 checkpoint 均通过。
- 验证：`npm run test:server` **33/33**，`npm run typecheck:server` 通过，
  `npm run check:architecture` 检查 **71** 个源文件，0 错误。测试全过程不依赖真实 API key。
- 待补：长期 permission always、完整协议生成、输入信号恢复、工作区、Blob、原插件市场契约、Mon/角色等完整业务与 UI。
  会话删除目前保留持久审计及恢复数据，发行前需补保留周期与物理清理策略。

### 2026-09-09：输入队列、长期授权与工作区

- steer/follow-up 在注入前记录 accepted，跟踪 injected/consumed；退出或重启将未消费信号标记 interrupted。
  已验证两类输入的真实 pi 消费，以及安全排队输入从持久快照恢复；不确定回合后的 held 队列跨重复重启保持暂停。
- permission always 以会话、能力、资源和规范化参数摘要绑定，服务重建后有效，不能跨版本/参数/会话沿用。
  新增用户 RPC 撤销入口。PermissionService 只协调等待者，SQL 已拆到 PermissionRepository。
- 工作区必须显式选择并持久化；当前要求项目目录与私有运行数据分离。
  浏览/读取限制路径和链接，文本最大 1 MiB，二进制不返回文本，完整小文件返回 SHA256。
- 新增 eden_read_file、eden_write_file、eden_exec。写入/命令先审批，之后在 bubblewrap 工作区内运行。
  写入支持仅创建、旧内容摘要校验和临时文件替换；命令无网络、30 秒、输出限制，不隐式重试。
  本机命令模式与 Windows/macOS 执行后端尚未实现，不退回无隔离执行。
- `npm test` 现在运行工程门禁和宿主测试；最近整套结果为 **3 项门禁 + 40 项宿主测试通过**。
  前端 `npm --prefix frontend/web run typecheck` 已通过；这不等于真实 UI 功能验收。
- 检查发现仍有并发文件写协调、真实目录变更竞态和 shell 进程总资源限制需要继续收敛，尚未宣称执行边界全量验收。
- 公共 AgentHarness/provider 方案已实测，移除未使用的 pi-coding-agent 依赖，避免把未采用的 SDK/扩展加载器留在生产依赖中。
- 补充验证：工作区修改队列在取消等待项时仍保持串行，配套测试与实际写入/命令测试均通过。
  同一宿主的修改不会互相抢跑；外部编辑器不参加该队列，内容摘要检查不宣称为 OS 级原子比较交换。
- 最新 TS 类型检查通过；工程门禁检查 **94** 个源文件、0 错误。根 `AGENTS.md` 已同步实际结构，
  原有前端三处用户修改保持原样。当前后续验收重点仍是完整 UI 与旧业务协议，不把独立工具测试代替产品验收。

### 2026-09-09：模型状态与 Mon 传输基础

- 上一目标轮属于实质进展：完成文档、核心实现和可重复验证；本轮继续推进原 P0–P5 范围，没有缩小验收目标。
- 新增 `model.read`，匹配旧客户端的模型状态结构；验证会话归属，配置为空时明确 unavailable，绝不返回 apiKey。
- 模型配置在 API 边界和运行时入口共同验证。非 OpenAI 供应商须显式设置 `EDEN_AGENT_BASE_URL`，
  不借用 OpenAI 密钥；拒绝含用户名/密码/query/fragment 的模型地址，以及非 loopback 明文 HTTP。
- 新增 `packages/integrations` 的 Mon HTTP 客户端，遵循旧实现 Token 认证和 base path；禁止跟随重定向，
  限时 30 秒、响应最多 8 MiB，错误不回显上游敏感正文。PATCH 仅供已获授权并已记录意图的业务调用者使用。
- 验证：模型状态/配置测试 2 项、相关运行时回归共 9 项通过；Mon 传输真实本地 HTTP 测试 1 项通过。
  TypeScript 类型检查和工程门禁通过。未调用真实 Mon，也未标记模型目录/选择/角色绑定完成。
- 下一批：Mon 模型解析、目录与会话绑定、动态模型选择；随后补 Blob、问答、UI 及完整旧业务契约。

### 2026-09-09：Mon 目录与会话模型绑定

- 上一轮已有模型接口和传输实现，属于实质进展；本轮继续接入真实协议行为。
- `model.catalog` 按旧 Mon API 获取 assistant、character、settings、vendors、entities 和模型详情，
  选择角色绑定/默认的活动模型。供应商和模型对外字段显式投影，不返回详情中的 API key。
- Mon 集合支持分页，限制 100 页/10000 条，检测循环并拒绝跨来源或越过 base path 的 next 地址。
- ModelService 的 Mon 主模型和视觉模型按 session 分开保存；无 session 的默认目录不向其他会话泄漏绑定。
  凭据只在内存，重启后需重新绑定。完整凭据持久策略与 Mon 重连仍待后续验收。
- 目录绑定先持久化不含密钥的 model.bound 事件，再更新内存；磁盘失败不生效。
  会话配置期间禁止启动回合；关闭宿主取消目录请求并排空绑定任务。
- 输入快照新增模型描述符（不含密钥），恢复时比较供应商、模型、端点和预算，配置改变则中断并要求核对重提。
- 验证：两会话分别调用两个真实本地 HTTP 录制模型；无默认凭据继承；目录/事件无密钥；绑定写入失败；
  关闭时取消等待请求；分页与地址边界。相关 10 项回归通过，新增模型配置变更拒绝联网测试通过。
  TS 类型检查与 109 个源文件工程门禁通过。
- 尚未完成：model.select 的外部写入与恢复、多角色各自绑定、视觉工具接入、完整 Mon 同步。
  当前多参与者目录请求明确拒绝，不能按单角色替代后标记完整业务已完成。

### 2026-09-09：Mon 模型选择与操作查询

- `model.select` 先验证目标模型与角色，再记录 `mon_operations` 意图，最后执行一次 Mon PATCH。
  角色已有显式绑定时修改角色模型，否则更新用户默认模型；跨会话的选择操作串行写入并刷新，各会话保留自己的绑定。
- 操作记录由 `MonOperationRepository` 拥有。意图写入失败不发送 PATCH；明确拒绝保留原模型并记录 failed；
  断连等无法确认结果时记录 unknown 并清除可能过期的模型绑定，禁止隐式重试。
  PATCH 成功则记录 applied；后续目录刷新失败仍为 applied，模型保持未绑定，避免误报远端未写入。
- 宿主重启将残留 running 操作标记 unknown，不重放外部写入；记录、事件均不保存 Core token 或模型 API key。
- 新增 `mon.operation.list`，仅伊甸园提供，支持可选 sessionId、state 和 limit（默认 50，上限 100），
  无需 Core 凭据即可查询已持久化结果。无 session 的操作保留空 sessionId；不伪装为旧版工具 `operation.list` 数据。
  客户端仍需接入查询展示；unknown 的远端核对与人工处置流程尚待实现。
- 验证：本批查询接口加入前 `npm test` 为 **3 项门禁 + 54 项宿主测试通过，无跳过**；
  加入查询接口后，`npx tsx --test Server/tests/mon-operations.test.ts Server/tests/mon-selection.test.ts` **9/9 通过**。
  覆盖持久化屏障、默认/角色选择、并发、拒绝、响应丢失、刷新失败、重启恢复、查询过滤及世界边界。
- `npm run typecheck:server`、`npm run check:architecture`（**114 个源文件，0 错误**）、
  `npm run build:server` 与 `node Script/Project/smoke_server_artifact.mjs` 均通过；构建制品实际启动两个隔离 Node 进程。
  所有 Mon 写入测试仅访问本地测试 HTTP 服务，未调用真实 Mon，未读取或修改旧生产 Data。
- 后续顺序：多角色各自模型绑定与角色执行语义 → Blob/问答与协议生成 → 完整 UI → 其余旧业务与 P5 迁移发行。
  本批有可运行实现及验证证据，长期目标继续推进；尚未达到 P0–P5 全量验收标准。

### 2026-09-09：参与者配置互斥与多角色模型集合

- 上一轮已完成外部操作记录和查询，属于实质进展。本轮对照旧角色配置实现继续迁移，没有缩小 P0–P5 目标。
- 修复 `session.set_participants` 绕过配置锁的问题：现在通过 SessionService 的会话互斥修改。
  目录绑定、结束会话或回合执行期间拒绝改参与者；元数据持久化成功后撤销旧主模型、视觉模型和角色集合，等待重新绑定。
  元数据写入失败则保留原参与者与模型。生产容器已连接失效通知，验证覆盖实际容器组装路径。
- `model.catalog` 对多参与者逐一解析主模型和视觉模型，按 sessionId/assistantId 保存，最多 32 个角色。
  拒绝重复 assistantId（数字和等值字符串视为同一 ID）及上游返回身份不符。
  所有角色解析完成后先持久化 `session.actor_models.bound`，再替换内存集合；任一解析或事件写入失败不留下部分新绑定。
- 对外 actors 仅包含身份及公开模型选项，不包含凭据；目录返回空 current，避免把首个角色模型表示为全会话共用模型。
  多角色选择仍需明确目标角色协议，实际回合执行/导演调度未迁移，目前不允许退回共享单模型执行。
- 上游 assistant 身份校验提取到 `model-schema.ts`，目录与选择共用；错误详情不会触发另一个角色的 PATCH。
  工程门禁曾检出目录函数复杂度 16，按职责拆分后通过，没有增加豁免或提高阈值。
- 验证：`npx tsx --test Server/tests/mon-binding.test.ts Server/tests/mon-selection.test.ts Server/tests/participant-binding.test.ts Server/tests/session-history.test.ts`
  **13/13 通过，无跳过**；`npm run typecheck:server` 与工程门禁（**117 个源文件，0 错误**）通过。
  覆盖两个角色不同模型、会话隔离、部分解析失败、事件失败、配置锁、绑定撤销、身份不符禁止外部写入及既有对话/压缩回归。
- 下一步：迁移导演选角/角色发言、各角色运行快照与审计，再接入客户端。完整 UI、其余业务、跨平台沙箱和 P5 仍待完成。
  用户已有前端三处修改和旧 Server 子模块保持原样，未访问生产 Data 或真实 Mon。

### 2026-09-09：导演计划解析与选角约束

- 对照归档 `eden-agent-app/src/director.rs` 迁移导演计划规范化，协议 schema 位于 `packages/api/src/director.ts`，
  服务端按 roster、normalize-beats、plan 拆分，公开入口仅导出。上一轮已有模型集合及测试，本轮继续完成实际解析逻辑。
- 保留 1–5 个节拍、角色最多出现两次、禁止连续重复、场景/执行策略归一、提及角色优先的诊断回退。
  接受旧输出字段别名；仅允许名册角色，名字有歧义时不按名字选角，未知回应对象改为有效前序角色或用户。
- 回复引用按原始节拍索引映射到保留节拍；被过滤、未来或无效引用被移除，避免过滤后指向错误发言。
  角色资料与模型输出只投影协议字段；限制输出大小和 Unicode 文本长度，原始凭据字段不进入计划。
- 类型检查、工程门禁（**123 个源文件，0 错误**）及 `npx tsx --test Server/tests/director-plan.test.ts` **4/4 通过**。
  规范化函数曾触发复杂度门禁，已按身份解析、回应对象和节拍内容拆分，无新增豁免。
- 本批仅完成导演输出解析与约束，尚未接入模型请求、导演运行持久化、`director.list` 或角色执行；
  不将这些单元测试解释为多角色端到端验收。下一批接入持久化导演运行及真实录制模型请求。

### 2026-09-09：导演运行持久化与查询

- 上一轮已迁移计划解析并通过测试，属于实质进展。本轮新增 `director_runs` 数据迁移、运行协议 schema 和业务 repository。
  导演计划及节拍状态由 DirectorRunRepository 持有；传输层提供旧参数结构的 `director.list`，生产容器已注册。
- 计划创建、节拍开始/完成、失败均与对应会话事件同事务提交，提交后才广播。
  不允许跳过节拍、同时开启多个节拍、重复完成或重开终态；完成最后节拍后运行进入 completed。
- 宿主组装时恢复未结束计划：planned/running 转 failed，保留已完成节拍并注明重启中断，不自动重放。
  重复恢复不会重复事件。查询验证会话存在与世界归属，不混入其他会话记录。
- 验证：类型检查及工程门禁（**126 个源文件，0 错误**）通过；
  `npx tsx --test Server/tests/director-runs.test.ts Server/tests/director-plan.test.ts Server/tests/server-runtime.test.ts`
  **9/9 通过，无跳过**。包含事务故障注入、真正关闭/重开临时数据库、重复恢复、查询隔离及鉴权宿主回归。
- 尚未接入导演模型请求和角色执行，因此正常对话尚不会自动产生导演运行；接口空列表只表示尚无持久化记录，
  不能视为多角色执行完成。后续继续连接录制模型、执行协调与客户端事件适配。

### 2026-09-09：导演模型请求与持久化屏障

- 上一轮完成导演运行 repository 与查询，属于实质进展。本轮新增公开 pi `Models.completeSimple` 的无工具文本请求适配，
  只在 `packages/runtime-pi` 引用 pi，复用显式供应商配置和实际请求载荷记录屏障，不引入默认凭据或隐式重试。
- DirectorPlanningService 根据名册、近期公开对话、用户消息和附件摘要发起隐藏导演请求；
  先记录 `director.model.request`，解析输出后记录结果摘要并创建持久化 planned 运行。
  导演原始输出不进入公开 assistant 历史，单角色不请求导演模型。请求最长 60 秒并接收上层取消信号。
- 持久化错误独立传播，不被供应商的错误消息转换吞掉；取消、非文本/工具响应不会生成可执行计划。
  文本 JSON 格式错误仍使用上一批的带诊断回退。旧实现对普通供应商故障的回退策略仍需在执行协调接入时补齐，
  不允许把取消或数据库故障当作普通回退后继续副作用。
- 验证：`npx tsx --test Server/tests/director-planning.test.ts` **4/4 通过，无跳过**，实际访问本地录制 HTTP 模型。
  请求记录与真实载荷一致；请求记录写入失败时 HTTP 次数为 0；等待响应中取消不重试；模型工具调用无法执行或生成计划。
  类型检查与工程门禁（**129 个源文件，0 错误**）通过。
- 当前规划服务已可独立运行并产出持久化计划，但尚未接入 SessionService 多角色回合；
  后续必须连接角色发言、每角色上下文/模型、取消排空、执行权限归属及 UI，才能进行完整端到端验收。

