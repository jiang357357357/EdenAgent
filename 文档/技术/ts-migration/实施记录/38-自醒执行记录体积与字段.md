# 自醒执行记录体积与字段

2026-09-10，用户截图显示执行记录请求 RPC connection closed，顶部同步显示时间读取失败。

## 调查与修复

- 当前服务 readyz 正常，自醒记录已完成。单独执行真实只读 RPC 可返回约 6 MB；同一连接上并发读取时，该体积会触及 4 MiB 发送积压保护。未重启或修改真实数据库进行调查。
- 记录含 1600 条 agent.message_update，累计大量重复文本快照；还包含模型请求与重复消息容器。执行记录查询省略这些事件，保留最终消息、工具、权限、操作和自醒结果；原始数据库不删除。
- 真实记录修复后响应 173290 字节，89 个事件，其中工具开始 8 次。下载内容是精简执行记录，页面明确说明省略项，不再称记录标识为已保存文件。
- 弹窗改为读取 record.run 中的状态/决策/日记，工具计数与事件标题读取 kind；概览工具解析支持当前 kind 字段，修复有调用却显示无工具的错误。

## 验证

- 后端大体积快照回归 1 项通过，确认保留工具与最终消息且不删原始快照。
- 前端事件解析 3 项通过，测试包含当前 kind 字段。
- 前后端类型检查通过，diff whitespace 检查通过。
- 当前运行服务需要重启加载后端查询修复；未进行 Electron 弹窗视觉验收。

本次不将精简记录定义为全部原始事件导出；极大量非流式工具结果的分页/下载仍需独立处理。
