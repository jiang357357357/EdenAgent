# TS 迁移实施记录 03

### 2026-09-09：审批提交竞态与前端问答回放

- 修复 PermissionRepository.finish 在提交后立即广播、等待者尚未结算的竞态。
  现在仓库返回已提交事件，服务先移除等待者并结算，再广播；广播触发取消不会把已提交的 allowed 改成失败。
  更新必须命中一条 pending 请求，重复结算不再插入额外 resolved 事件或授权。
- 回归用 SQLite 触发器阻断 permission.resolved 写入：请求保持 pending，持久授权表仍为空。
  移除故障后提交 always 授权，事件订阅者同步取消，等待方仍获得已提交审批结果，只有一条 resolved 事件。
  这不取消工具执行阶段对 AbortSignal 的检查，也不允许审批授权替代终端沙箱边界。
- 通过独立兼容进程调用现有前端 projectSessionEvent 与 runtimeReducer，验证真实持久化问题事件能恢复待答问题，
  answered 和 rejected 回放后均移除问题。未修改前端源码；该检查不等同浏览器显示和点击交互验收。
- 问答/审批专项 **6/6 通过**，类型检查及工程门禁（**159 个源文件，0 错误**）通过。
  本轮没有重跑全量测试；最近全量结果仍为上一批的 3 项工程门禁和 94 项宿主测试通过。
- 完整迁移仍在进行：全量协议生成、Blob、导演策略、其余旧业务、浏览器/Electron 验收及 P5 尚未完成。


### 2026-09-09：导演请求失败降级

- 对照归档 Rust director.rs 的 director_request_failed 行为，补齐独立导演请求失败时的确定性降级。
  选择用户点名角色，否则选名册首位，生成单角色回应节拍；模型意外返回工具调用时也只使用宿主降级计划，不执行该调用。
- runtime-pi 将模型传输/非文本完成失败归为 TextCompletionError；请求记录失败仍是独立持久化错误。
  导演只捕获可降级模型错误及自身超时，用户取消、配置错误和持久化错误继续向外报告，不能偷偷继续角色执行。
- 本地 HTTP 503 实测只发送一次请求，降级选择被点名角色，公共事件不带供应商错误原文。
  请求记录失败仍阻止 HTTP；降级结果事件写入失败仍阻止计划创建；用户取消仍不创建计划。
- 导演专项 **6/6 通过**，类型检查及工程门禁（**159 个源文件，0 错误**）、构建、双进程制品健康检查通过。
  超时分支已实现但本批未做 60 秒计时验收。导演模型选择/重规划策略及其余 P4/P5 继续推进。
- 本批最终 `npm test`：**3 项工程门禁 + 98 项宿主测试通过，零失败、零跳过**。未调用真实模型/Mon，未读写生产 Data。


### 2026-09-09：独立导演模型绑定与排队快照

- 归档 Rust 的导演读取会话模型，与角色模型绑定分离。新实现移除固定取首位角色模型的逻辑，
  ModelService 单独保存导演绑定；Mon 多角色目录刷新同时读取当前助手目录作为会话导演配置来源，角色仍分别按 ID 加载。
  所有目录读取和绑定事件提交成功后才替换内存绑定，公开目录和事件仅包含模型描述，不保存密钥。
- bindActors 不再隐式借用首位角色模型；缺少导演时 model.read.available=false，排队前也拒绝启动。
  尘世导演与角色使用本世界显式配置。切换参与者/清空绑定同时清理导演。
- companion 输入快照改为 actors + director 对象，执行前分别检查描述；只更换导演也会中止旧排队输入且不发送请求。
  先前开发版数组形态输入不会被静默补上新的导演模型，恢复时校验拒绝，需复核后重新提交；未操作生产 Data。
- 生产会话实测独立导演只收到规划请求，首位角色模型无请求，计划指定的第二角色收到回复请求。
  Mon 名册倒序时导演仍绑定当前助手模型，证明不由名册排序决定。导演/角色/压缩/信号/目录相关 **18/18 通过**，
  类型检查和工程门禁（**159 个源文件，0 错误**）通过。
- 当前实现显式刷新时绑定导演；重新连接自动恢复、用户指定导演/多角色模型选择 UI、重规划和剩余 P4/P5 仍未完成。


### 2026-09-09：显式角色/导演模型切换

- model.select 新增可选 target：{kind: actor, assistantId} 或 {kind: director}。
  未指定 target 的单角色调用保持旧行为；多角色调用必须指定目标。角色必须属于指定会话，导演目标要求多角色会话。
- 新 selection-target.ts 负责目标解析，避免在 MonBindingService 叠加分支。
  显式角色选择始终 PATCH 对应 character.ai_talk_entity_id，即使原先为空，也不改动全局 default_model。
  导演选择使用当前助手配置来源，沿用既有角色绑定/用户默认选择规则；若它与某参与者指向同一 Core 角色，共享配置变更会同时影响两者。
- 目标信息随 endpoint/body 写入操作意图，提交成功后才 PATCH；成功后刷新全部角色及导演绑定。
  延续原有串行选择、配置锁、失败/未知/已应用状态，以及远端结果未知时清理绑定并禁止自动重试的处理。
- 本地模拟 Core 验证角色 2 原绑定为空时只修改 /api/characters/2/；导演单独修改当前角色 3；角色 1 保持原模型。
  无目标、非参与者目标以及意图写入失败均未发送 PATCH。模型选择相关 **9/9 通过**。
- 类型检查与工程门禁（**161 个源文件，0 错误**）通过。首轮 schema 编辑误加自引用、随后模拟路由模型 ID 解析错误，均已修正并重跑。
  前端目标选择控件、生成客户端字段和浏览器交互尚待接入；其余 P4/P5 范围保持不变，未调用真实 Mon 或操作生产 Data。


### 2026-09-09：前端角色/导演模型选择菜单

- 前端新增 ModelSelectionMenu，显式选择导演或具体角色，再选择模型；当前标记按目标计算，提交期间锁定选择。
  ChatInput → 设置 hook → updateRuntimeModel 传递 target，使用 @eden/api 的 ModelSelectionTarget 类型。
- 将模型类型及目录映射从既有大文件 agent-client.ts 提取到 runtime-models.ts（136 行），菜单组件 37 行。
  修复多角色目录 assistant/character=null 导致旧映射解引用失败；保存导演及各角色当前模型，服务目录新增助手姓名供选择器展示。
  单角色调用保持无 target，角色模型共享同一实体时仍按实际绑定显示。
- 前端专项 **2/2 通过**，覆盖空身份、多目标当前模型标记、不修改原目录、失效目标和无效角色输入。
  前后端类型检查与工程门禁（161 个源文件，0 错误）通过。用户原有 self-awake 三处改动未修改。
- 本批没有手改旧生成客户端；新增请求目标通过结构化参数传递，目标类型由 TS API 导出。
  全量协议生成迁移仍未完成，既有生成目录类型尚未反映完整新目录；浏览器实际点击、Electron、多角色断线刷新仍待验收。
- 最终 Web 生产构建通过；存在 Vite 产物块大于 500 kB 的提示，未调整阈值掩盖。Mon 目录与目标切换回归 **3/3 通过**。


### 2026-09-09：模型菜单异步状态隔离

- 新增 73 行 ModelMenuController，将模型读取/保存状态从设置 hook 拆出，hook 缩至 122 行。
  控制器固定所属 sessionId，以请求序号拒绝旧读取覆盖新读取/已保存结果，保存期间同步拒绝重复提交与目录刷新。
- 会话切换的 layout effect 清理立即停用旧控制器；已发出的远端保存仍完成，但不更新新会话状态或关闭新菜单。
  同控制器重新激活时保留在途保存互斥，等远端结束后重新读取确认状态，适配挂载/隐藏变化。
- 延迟 Promise 实测覆盖读取逆序、保存后旧读取失败、重复提交、跨会话保存完成、重新激活等待与刷新。
  菜单控制器/目录专项 **5/5 通过**。初次测试发现 Node strip-only 不支持参数属性，改为显式字段后通过。
- 前端全量本轮 **219 通过、2 失败（共 221 项）**：assistant-handoff 和 session-environment 测试仍读取旧 Server Rust 源路径，报 ENOENT。
  未将路径改到 Archive 或跳过测试来制造通过；助手交接尚未迁移，环境测试需要转为 TS 实际行为证据，列入下一阶段待办。
  当前模型菜单专项通过不代表前端全量或完整业务已通过。浏览器点击、完整协议生成与 P4/P5 范围继续保留。
- 最终前端类型检查通过。全量两项失败保留原始证据，未宣称迁移完成。


### 2026-09-09：会话环境持久化与模型坐标过滤

- 替换读取已归档 Rust 源文件的 session-environment 测试；前端环境快照构造提取为独立函数，测试用户设置/浏览器回退与字段边界。
  宿主补充实际 RPC → 排队 → SQLite 重开 → 录制模型的行为测试，不以源码关键词证明迁移成功。
- 修复两个实际缺口：原提示词直接序列化环境导致经纬度进入模型；新 turn 环境只保存到输入、未更新会话默认值。
  新 sessionEnvironmentSchema 校验环境，经纬度仅保留在宿主存储；单角色与角色提示词共用 modelEnvironment，仅输出时区、语言和命名地点。
- 环境更新的 metadata/兼容事件与 input.queued 同事务提交，失败全部回滚；幂等重复提交不重复产生环境事件。
  未显式传环境的下一轮继承最新会话环境，显式 null 可以清除环境；已排队输入仍使用自己的快照。
- 宿主初轮相关 **5/5 通过**，实测关闭重开临时 SQLite 后坐标仍保留在会话记录，模型请求有城市/时区而无坐标。
  前端环境专项 **2/2 通过**，工程门禁（**163 个源文件，0 错误**）及宿主类型检查通过。
- 助手交接的旧路径测试仍待迁移；本批不把其改指 Archive、不跳过或降级为恒通过。完整 UI、协议生成及剩余 P4/P5 持续推进。
- 最终 `npm test`：**3 项工程测试 + 102 项宿主测试通过，零失败、零跳过**；前后端类型检查通过。全量包含角色实际请求坐标过滤回归。

### 2026-09-09：助手交接账本与实施记录分卷

- 新增 assistant_handoffs 表及 HandoffRepository，按 session/sourceTurn 唯一记录目标，保持 requested/failed 前端事件字段。
  调度必须来自本会话运行中的回合，同回合重复同目标幂等，不同目标拒绝，目标资料大小限制为 64 KiB。
- 当前回合仍运行时不可领取；只领取来源回合已完成的交接。取消/中断来源回合不触发交接，原参与者保持不变。
  领取、失败、恢复均先持久提交再广播；恢复仅将尚未提交切换的 claimed 退回 scheduled，不恢复终态，不重复产生恢复事件。
- 交接账本/数据库专项 **6/6 通过**，包含事件写入失败回滚、幂等调度、重复领取、取消来源与恢复。
  本批仓库尚未接入生产调度，未宣称助手交接可用了；后续需 Mon 目标解析与授权、模型准备、参与者/输入原子切换及生产回合触发。
- 主跟踪文档原已达 481 行，现将完整历史批次分卷，主文档保留阶段状态与索引；已有证据和未完成项全部保留。
- 最终宿主类型检查与工程门禁通过：166 个源文件，0 错误。主跟踪文档缩至 44 行，三卷历史记录均不足 200 行。


### 2026-09-09：助手交接原子提交仓库

- 新增 HandoffCommitRepository，claimed 交接仅能在来源回合 completed、会话 active、无运行输入的边界提交。
  事务同时更新参与者快照、公开兼容事件、无密钥模型描述、后续输入和交接 completed 状态，保留原始公开历史。
- 有排队输入时保留文本、环境和输入 ID，替换角色/模型快照并移除旧 companion 快照；没有排队输入时创建内部交接输入。
  交接改变输入执行身份，旧身份的重试参数不会被默默当作新身份执行；完整交接恢复入口仍需接入调度。
- commit 返回已提交事件但不自行广播，预留宿主先安装准备好的内存模型绑定、再广播并继续下一回合的顺序。
  当前仓库未接入生产服务；内部输入隐藏/临时上下文处理、Mon 目标准备与回合触发仍未完成。
- 交接专项 **6/6 通过**，覆盖排队输入迁移、无排队输入创建、完成事件失败全回滚、运行输入竞态及终态不恢复。
  另补排队元数据更新事件失败的回滚断言，确保既有输入快照和参与者均不变化。
- 补充回滚断言后提交专项 **3/3 通过**；宿主类型检查及工程门禁通过（168 个源文件，0 错误），提交仓库 54 行。


### 2026-09-09：内部交接输入的隐藏与临时上下文

- runtime-pi 新增 transientInput，仅将当前运行首条用户输入持久化为公共 SessionTreeEntry 的 custom 审计记录，保留 ID/parent 链。
  后续真正用户输入仍是普通消息；重新创建运行时读取检查点时不把内部指令放入对话历史，审计内容仍保留。
- 首轮工具循环测试发现 pi 在工具完成后重建会话上下文，指令会过早消失。改用公共 context 钩子按原位置补回当前运行的临时消息，
  运行结束即清除内存补入状态；不导入 pi 私有模块、不删除审计事件。
- SessionService 识别持久输入 internalHandoff 标记，公开消息投影隐藏内部用户输入，仍正常显示助手回复和后续实际用户消息。
  回合内请求记录与检查点仍须先持久提交；临时审计写入失败时无模型请求。
- 初轮故障修正后专项 **3/3 通过**：工具循环两次实际请求均有指令，恢复后的请求不含指令，生产 SessionService 执行交接输入仅产生助手公开消息。
  进一步补充同一运行时下一次 prompt 不含旧指令断言。类型检查与工程门禁通过（170 个源文件，0 错误）。
- 生产交接调度及 Mon 准备/授权尚未接入；此批完成内部输入执行机制，不宣称完整助手切换已经可用。
- 最终 `npm test`：3 项工程测试与 **111 项宿主测试通过，零失败、零跳过**；构建及双 Node 制品健康检查通过。


### 2026-09-09：交接调度接入生产会话边界

- 新 SessionBoundary 接口由会话队列在每次领取输入前调用；HandoffDispatcher 已注册生产容器。
  来源回合完成后准备目标模型，原子提交交接，安装主/视觉绑定，再广播完成事件并领取下一输入。
- 缺少会话 Mon 凭据时释放领取状态并暂停队列，避免忙循环、借用默认凭据或继续用旧身份执行排队输入。
  显式目录刷新会恢复等待的交接，包括没有用户排队输入的交接。取消准备则释放任务且不切换角色。
- MonBindingService 仅在本会话目录/选择成功后缓存连接参数，进程关闭清除；prepareHandoff 只读取目标模型，不提前更改绑定。
  缺少绑定返回等待，目标准备失败标记交接失败；数据库提交失败继续报告宿主故障，不伪造完成。主/视觉配置提交前均校验。
- 会话执行专项验证旧助手完整工具循环结束后，新助手处理已排队用户输入或内部接话；完成事件订阅者已看到新模型绑定。
  无凭据时准备仅尝试一次，刷新后恢复。首轮调度/账本/Mon **8/8 通过**，补充 Mon 准备断言后相关 **5/5 通过**。
- 类型检查与工程门禁通过（172 个源文件，0 错误）。调度测试用测试工具创建持久交接，正式 switch_assistant 工具、目标资料解析与审批入口仍待迁移。
  此批完成已入账交接的生产执行路径，不把测试工具当成最终用户入口。
- 补充验收范围：当前调度测试覆盖单角色间交接；多人会话转为单角色时，需将公开对话接入新运行时，不能仅凭数据库历史未删除宣称模型上下文连续。该项继续待办。
- 最终全量：3 项工程测试与 **114 项宿主测试通过，零失败、零跳过**；构建及双 Node 制品健康检查通过。


### 2026-09-09：多人交接到单角色的公开历史分支

- 将 publicMessage/conversationWindow 移至会话 history 目录，导演与交接共用同一公开文本过滤及 12000 字符窗口，不复制解析逻辑。
- 多人交接提交时，从最近 100 条公开 message_end 中提取用户/助手文本与 assistantID，作为 pi 公共 custom_message 建立新的根分支。
  旧单角色检查点条目与角色私有检查点仍保留审计，但新的模型上下文不跟随陈旧分支，也不带工具参数、结果或思考块。
- 公开历史检查点更新与参与者/输入/交接完成同事务，完成事件写入失败时原检查点和参与者一起恢复。
  单角色之间的交接保持原检查点路径。窗口截断仅限制模型上下文，不删除原始事件。
- 真实录制模型依次运行两位角色、交接新角色、重建 SessionService 后继续对话：新角色及恢复后的请求均包含双方公开回复，
  不包含私有工具参数/结果、陈旧单角色上下文或上一轮内部交接指令。相关专项 **8/8 通过**。
- 初轮测试初始化误用非 UUID 回合 ID，抛错后模拟服务器泄漏使子进程未退出；确认具体子进程后终止，改用 UUID，补清理钩子/超时后重跑通过。
  此问题在测试夹具，未改弱生产 UUID 校验。正式切换工具、审批入口、浏览器验收及其余 P4/P5 继续待办。
- 最终宿主类型检查与工程门禁通过（174 个源文件，0 错误）。本轮未重跑全量测试，未以专项结果代替完整迁移验收。


### 2026-09-09：Mon 助手目录与交接目标解析

- 新 assistantTargetSchema 定义 assistantId/assistantName 输入，沿用显式 ID 优先规则；无有效目标拒绝。
  assistant-catalog 独立处理分页目录、去空白/忽略大小写的助手名与角色名精确匹配、重名拒绝和详情身份核验。
- 目录遇到重复 ID 拒绝，名称唯一命中后仍单独读取详情；指定 ID 不扫描目录，不从名称模糊推断身份。
  assistantSummary 仅返回限长名称、ID、默认标记与角色身份，不包含提示词、资产或凭据。完整详情仅作为宿主内部结果，待正式切换入口构建角色资料。
- MonBindingService 通过本会话缓存凭据提供 listAssistants/resolveAssistant；未绑定会话拒绝，不借用其他会话或默认凭据。
  解析只读目标，不提前更改当前模型绑定；预先取消时不发送请求。
- 本地 HTTP 目录/Mon 回归 **3/3 通过**，覆盖跨页命中、重名、ID 优先、错配详情、缺失名字、重复 ID、摘要边界及取消。
  正式工具、资料投影和审批仍待接入；未调用真实 Mon、未操作生产 Data。
- 补充会话凭据隔离与解析不改变绑定断言后，Mon 回归 **2/2 通过**；宿主类型检查及工程门禁通过（177 个源文件，0 错误）。
