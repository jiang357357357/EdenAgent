# 自醒真实验收与 JSON 包装兼容

2026-09-14，用户明确要求真实测试。

## 实际执行

使用当前部署服务身份签名调用真实 `/internal/self-awake/run` 与 `/internal/self-awake/status`，走实际 Core 当前助手、模型绑定、宿主调度、模型推理与日记持久化链路。验收请求要求只观察写日记，不执行命令、外发、拨号或修改定时。

第一轮 job `2d054e73-0c6f-49fd-8d70-db19efa2c44d` 失败：模型返回了完整 JSON，但带角色开场白及 Markdown 代码围栏，整段 JSON.parse 报错。补充解析器兼容唯一 json/无语言代码围栏，仍严格校验结构，多代码块或非法决策不采纳；提示明确要求最终只输出 JSON。不新增模型修复调用，不改写首次失败记录。首次真实原文用新解析器只读复验通过。

第二轮 job `eb93ff97-d367-463d-9a36-654ae8a65bc9` 完成，run `56999016-b0ea-41b3-a912-42444e629d59`。真实状态 API 返回 completed，前端使用的 self_awake.list WebSocket RPC 返回 completed、1 篇日记、lastError=null。2 次模型请求，2 次 get_self_awake_context 均成功。无拨号、消息外发、命令和定时工具调用。

MonOs next_wake_at 保持 2026-09-14T21:56:40.013+08:00。调度状态依旧 retrying，历史失败计数 2889 未清零。这次通过服务入口手动提交，没有由 MonOs 到点触发，也没有验证它后续消费结果与重设计划的自然周期。

## 检查与证据

- 解析回归测试通过，覆盖纯 JSON、角色文字加代码围栏、非法对象和多个代码块拒绝。
- 服务端类型检查与构建通过，已重启生效。
- 私有证据位于 `.artifacts/self-awake-live/`，包含首轮与第二轮提交/结果、列表 RPC 结果及复验脚本；不包含认证密钥。
- 历史失败保留；本轮真实验收经正式入口写入的记录作为真实执行证据保留，不将真实用户数据库作为自动化测试夹具。
