# 聊天标签栏管理面板溢出

2026-09-10。标签栏为 h-10 横向 flex，但直接挂载 MonSyncPanel、OperationPanel 和 McpPanel 完整 section，导致面板高度溢出、标签被挤出、管理内容覆盖聊天消息。

新增独立 SessionManagementMenu，将三个面板移入按需打开的浮层；标签栏仅保留触发按钮。浮层宽度受聊天主区域容器约束，内部限高滚动；支持外部点击、Escape 和关闭按钮，切换会话时重置。记忆候选和子任务原有按钮入口保留。

前端类型检查及 diff 格式检查通过。未进行桌面截图或实机交互验收。
