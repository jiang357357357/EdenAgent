# 所有会话只读入口

日期：2026-09-25。范围：Web 主导航与会话浏览。

左侧活动栏在“会话”后新增“所有会话”。新页面请求当前世界、当前账号的会话列表，包含应用聊天、QQ 私聊、后台自醒、子智能体和已关闭会话；删除状态由后端既有规则排除。列表可按分类或标题、ID 筛选，选中后分页读取消息。后台会话在此页只读，不接入聊天输入与普通会话缓存。

现有“会话”入口仍只展示应用聊天。单次列表受当前 RPC 的 1000 条上限约束，达到上限时页面显示提示；完整服务端分页尚未实现。

按当前业务优先要求，本轮未运行测试、类型检查、构建或真实界面验收。
