# 实施记录 15

## B2/B6/B7：插件钩子作业恢复（已编写，未执行）

- `PluginHookRepository` 在派发和重提前核对持久事件 ID、会话、类型、时间及作业因果来源；`PluginHookService` 同时核对当前启用且获授权的原版本钩子、事件声明和固定技能。原事件已缺失时拒绝重提，不凭作业 payload 重建事件。
- `plugin.hook.resubmit` 要求原作业 ID、预期更新时间、必填说明和明确确认。只接收 failed/cancelled 原作业；关联输入须先取消，相关 running/unknown 工具操作须先核对。重提前要求会话活动且历史上下文已恢复。
- M99 保存原作业、新作业、预期版本、说明及确认时间；同一来源和相同确认重复提交返回既有新作业，不再入队。不同确认拒绝。原记录不改，新作业保留原 payload、因果 ID 和深度，派发时再次检查插件与事件；后续自主触发仍受深度限制。
- 显式重提旧事件不倒退自动事件游标，也不改变钩子的自动订阅起点。不会自动选择其他插件版本或同名技能。原版已卸载或撤权时必须先通过现有安装/授权业务处理。
- 插件页接入按类型分页的作业恢复面板，聊天作业面板也可重提钩子；支持先打开关联输入的处置入口。插件页按世界重建，避免保留上个世界的恢复表单。迁移审阅 RPC 允许清单不开放执行型重提。
- 本批仅阅读和编辑源码、文档；未运行测试、类型检查、构建、生成器、迁移、服务或真实插件。用户三处自唤醒前端文件未修改。
- 自主唤醒作业的决策失败与动作失败必须分别恢复：动作已有 `self_awake.action.resume`；决策重新运行还需保留原日记、动作结果、身份和外部提交归属。下一批继续实现，不能把插件入口视为全部作业恢复完成。整体切换、回退与发行工作仍未完成。

## B1/B6/B7：自主唤醒决策失败后的恢复（已编写，未执行）

- 新增 `SelfAwakeJobRecovery`，`self_awake.job.preview/resubmit` 以 SHA-256 绑定原作业、运行记录、当前角色/环境及外部提交归属。M100 事务保存来源、新作业、确认依据和身份/环境快照；同一来源相同确认只创建一次新作业。
- 区分作业投递完成与决策有效。failed/cancelled 作业可以核对重提；completed 作业只有对应 run 已 failed 时才允许重提。已有 decision/action_result 或 awaiting_action/action_running/action_failed 等状态不得走此入口，继续原动作恢复，不通过重新决策绕开动作记录。
- 关联输入须 completed/failed/cancelled；held/interrupted 先人工处理。相关 running/unknown 工具操作阻止重提；原日记、回合、工具与通知不清除。新请求提示此前副作用仍然存在，不能因为再次决策就假设从未执行。
- 当前会话必须活动且历史上下文已恢复，单角色且与原 run 作者一致；原 Mon 外部提交的用户归属需与 payload/环境一致。新外部归属记录沿用原用户，使用新的内部恢复键；原 HTTP 幂等键和原作业查询结果不改写。
- 新作业保留原触发 payload 和因果深度，不自动恢复其他定时器。派发再次核对确认时的作者和环境。当前严格支持新 TS 定时器与 Mon 提交格式；其他旧 payload 拒绝并保留，旧格式转换仍需业务适配，不能宣称全部历史自醒已恢复。
- 前端通用作业面板接入重提预览/确认；独立 `SelfAwakeRecoveryPanel` 挂载于 App，提供所有自醒记录分页，使“作业 completed、决策 failed”也可找到。世界切换重建表单；用户三处自唤醒文件未修改。迁移审阅仅开放记录读取和预览，不开放重新决策。
- 仅阅读/编辑源码与文档、查看 Git 差异摘要；未运行测试、构建、类型检查、生成器、SQL、真实迁移或服务。界面布局与重提行为均待用户授权后验证。下一批优先推进整体迁移切换/回退；自醒最终 run_safe_check/sync_context 动作适配及旧格式兼容仍在业务缺口清单。

## B7：离线激活预检与切换证据（已编写，未执行）

- `plan-activation` 入口已编写，输入原快照、暂存目录和世界。先取得与宿主相同的 SQLite 进程排他锁，再取得转换锁；运行中宿主/审阅器/导入器阻止预检，崩溃转换锁不自动删除。
- 读取真实暂存库而非报告 JSON，逐表重新扫描原快照并校验已有转换摘要/行数/状态，读取各恢复域、工作区确认、未结输入/作业及 SQLite 完整性。已有 `eden-agent.db` 阻止新激活，不能覆盖运行库。
- 计划绑定世界、目录、schema、转换表、恢复摘要及数据库/WAL SHA-256。输出恒为 review_required，退出码 3；零阻断项也不表示兼容性验收完成。该功能不写入完整迁移标记、不启动宿主、不改配置。
- 切换采用保留原暂存根目录与资源的方向，避免插件绝对路径因目录搬迁失效；后续在同目录生成独立运行库并保留原暂存库。原子发布、确认凭据、回退后运行禁止与启动入口衔接仍待实现，不能将预检等同于切换完成。
- 本批只编辑源码与文档；上述 SQL/摘要扫描是待运行实现，未执行预检、迁移或任何测试，未访问真实数据。

## B7：运行库原子发布与回退停用（已编写，未执行）

- 新增 `activate_legacy.mjs activate`：要求绑定计划 fingerprint、说明、兼容性验收依据和明确 confirmActivation 的 JSON。取得双锁后重建计划；schema 必须与当前宿主一致，原快照逐表完整核验，各恢复阻断项须清除，Blob 文件逐个核对目录、大小和 SHA-256。
- 使用 SQLite `VACUUM INTO` 生成同目录独立副本，副本内事务写入 complete 与激活回执，关闭并持久化后通过不覆盖目标的硬链接发布为 `eden-agent.db`。原 `agent.sqlite` 不改；同目录发布保留已有资源路径。文件系统不支持硬链接时失败，不降级为覆盖式复制。
- 崩溃在发布前仅遗留随机命名候选库，不被宿主读取；发布后回执已在运行库内，重复相同确认返回原 activationId，不重建数据库。目录同步失败也不会删除已发布数据库，可凭同一确认重新确认发布结果。手动删除或修改候选库前仍需确认离线进程已停止。
- 新增 `rollback`：必须指定世界、activationId、说明和 `--confirm-rollback`，双锁防止运行中回退；事务保存回退记录并将运行库标记 rolled_back。普通宿主沿用 import state 检查拒绝启动；重复相同说明幂等，不恢复旧模型调用或自动重发任务。
- 启动保护拒绝在未激活的暂存目录旁创建空 `eden-agent.db`。运行库一旦发布，原暂存副本冻结，审阅宿主、离线恢复和 resume 均拒绝修改，即便已经回退也不修改这份切换前副本；共享资源可能已有新版本写入，不能把旧数据库当作全目录时光回退。
- 回退当前完成的是新运行库停用和切换后数据保全；不会撤销外部副作用、倒退共享文件或把新数据合并回 Rust。自动恢复启动配置、双世界联合切换/回退与桌面选择入口仍待接线，不将单世界数据库发布等同于整个发行流程完成。
- 激活确认中的兼容性依据是操作者提供的验收引用，不能替代实际验收。当前没有执行激活、回退、预检、SQLite 语句、Blob 扫描、构建或测试；命令与行为均为待验证源码。

## B6/B7：启动入口选择已激活的双世界目录（已编写，未执行）

- 开发入口和桌面监管器共用 `realm-data-roots.cjs`，支持 `EDEN_AGENT_MON_DATA_ROOT` 与 `EDEN_AGENT_LOCAL_DATA_ROOT`。配置必须为绝对目录，启动任何子进程前一并核对两个根目录，拒绝相同/嵌套路径；真实文件系统下解析现存祖先的符号链接，防止不同字符串实际指向同一目录。
- 开发 Server 分别取得各自 `EDEN_AGENT_V2_DATA_ROOT`；传给桌面的双世界根目录与 token 文件路径同步更新。桌面无论监管还是外部模式，能力令牌路径和宿主数据根均取同一个选择；不再固定到默认 Data 或 userData。
- 单宿主 `start_server.mjs` 在未指定单进程 V2_DATA_ROOT 时按当前世界选取成对目录，保留显式离线审阅目录覆盖。双世界入口不继承单宿主 V2_DATA_ROOT 作为两个世界的共同根，避免错误共享。
- 配置读取后固定于本次监管器生命周期，切换须停止进程后重启。双世界联合发布/回退事务、持久配置版本切换及桌面用户选择界面仍待完成；此批是实际启动链路接线，不是这些能力的替代。
- 仅阅读与修改源码/文档，未运行开发启动、桌面、测试、构建、迁移或真实数据读取。

## B6/B7：持久双世界选择与配置回退（已编写，未执行）

- 新增 `eden.runtime-selection.v1` 配置，单次原子替换同时选择 mon/local 根目录；开发入口、单宿主启动脚本和桌面监管器均支持 `EDEN_AGENT_RUNTIME_SELECTION`。显式世界目录若与配置冲突则拒绝，不静默覆盖其中一边。
- `select_runtime.mjs` 提供 read/select/restore。select 绑定预期 revision（首次为 none）、说明和 `--confirm-switch`；配置文件锁加上原/新世界的进程锁及转换锁，在固定顺序下核对各目标现有数据库的世界和迁移状态。未激活、已回退、缺失或符号链接数据库拒绝选择。
- 发布前持久保存上一版配置，历史以 UUID 命名且不覆盖，临时完整文件硬链接发布，避免崩溃留下半份正式历史；当前配置通过同目录 rename 替换后同步目录。restore 读取上一版目标，再经过同样的离线锁、当前 revision 和数据库准入流程，生成新 revision；不能恢复到已经停用的运行库。
- 宿主配置接收选择文件与 revision，取得自身进程锁后再次核对 revision 和真实数据路径。监管器读取配置到子进程取得锁之间如发生切换，旧选择启动失败并要求重新启动监管器。持续运行的宿主持有锁，阻止离线配置变更。
- 当前配置只包含目录、版本、时间与说明，无能力令牌或模型凭据。不启动程序、不更改系统环境；首次切换前需先把原目录作为基线版本保存，才能通过 restore 返回。
- 双世界目录选择已成对提交，但两个数据库的激活本身仍各自独立；跨两库的准备/发布编排和桌面用户操作界面仍待完成。选择回退不替代对新库的停用/副作用对账。
- 本批只编辑源码和文档，未执行配置读取/切换命令、服务启动、构建、类型检查、数据库查询或测试。

## B7：双世界共同就绪与联合回退（已编写，未执行）

- `activate_pair.mjs` 接受两个世界各自的快照、暂存根与确认文件。组记录放在两根之外，绑定规范化请求摘要和 UUID，首次持久化 preparing 后分别执行原有幂等单库激活；每库回执固定组文件路径/ID，不能当作普通单库重复接管。
- 两库发布完成后同时取得两边离线锁，重新核对激活 ID、complete 状态和组归属，再原子发布共同 ready。不是声称跨目录文件系统事务：中断允许存在单边文件，但 preparing 使其不可运行，重复相同请求继续原批次。
- `EdenDatabase` 在迁移 schema 和恢复作业之前核对共同 ready、激活 ID 和真实世界根；启动选择同样检查此门禁，手工指定其中一库也无法绕过共同就绪。组记录缺失/被移动时拒绝启动，部署必须保留其绝对路径。
- `rollback_pair.mjs` 要求组 ID、说明和明确参数。锁定两边核对成员后先持久化共同 blocked/回退说明，再分别写单库回退；崩溃即使只有一边写完，另一边也不能启动。未完成激活的组允许缺少尚未发布的成员；相同说明可继续，中途失败保留所有已发布数据。
- 单库回退对组成员要求组已 blocked，不能直接拆掉一个 ready 组。共同就绪与持久启动选择仍是两个显式步骤：先发布两库，再 select 成对目录；回退先 block 两库，再 restore 原配置。没有自动启动或自动外发。
- 本批仅源码/文档编辑，未运行联合激活、回退、启动配置操作、SQL、构建、测试或服务。桌面操作界面与发布制品内迁移工具组装仍待完成；全业务验收仍未完成。

## B7：独立迁移命令的发行组装（已编写，未构建）

- `build_server.mjs` 接入六个迁移入口的独立 esbuild ESM bundle，保留 source map、Node 22 目标和 zod 外部依赖；CommonJS 共用目录选择组件通过 bundle 和 createRequire 兼容，不需要运行时引用 frontend 或 Script 源码目录。
- `package_server.mjs` 必须复制完整迁移 bundle，缺失任一文件即中止制品组装，不静默发布缺少恢复工具的包。工具随主宿主共用 Node 与已声明 zod 运行依赖，无需在安装环境 npm install 或引入 tsx。
- 发行包 `migration/` 附独立命令索引 README、当前操作文档及可选旧角色导出 Python 源码；manifest 增加每份工具、source map、说明和辅助脚本的字节数/SHA-256。Python 3.11+ 是可选角色导出依赖，不自动执行。
- 修正文档中 Server 仍用 Cargo、复用其他进程和自动复制旧数据的过时说法。源码命令 usage 暂仍可能显示开发入口路径，包内 README 给出对应 bundle 文件名和相同参数，后续 UI 封装不应照搬源码路径。
- 只修改构建/打包脚本与文档；未实际调用 esbuild、打包器、迁移命令或测试。没有生成可验收制品，桌面交互和安装器资源纳入仍待完成。

## B6/B7：桌面分发资源改接 TS 制品（已编写，未打包）

- `frontend` 的旧打包命令仍引用 target/release Rust 宿主、已归档 Server/skills 与 connectors/manifests；改为 `package_desktop.mjs`，从对应平台/架构的 runtime 分发目录取资源，不再引用旧宿主。
- 打包器先核对 Node 版本、平台/架构、宿主摘要和迁移工具逐文件摘要，再将 Node/server/node_modules/connectors/migration 等运行包资源作为 Electron resources 的直接子项，匹配桌面监管器的现有资源路径。
- 应用临时目录只组装 desktop/src、assets、web/dist 和必要 package metadata，不复制用户数据、源码工作区或开发依赖。目标已存在时拒绝覆盖，Windows 仍要求预先构建 pointer observer；macOS 暂未提供专用图标制品，不错误传递 PNG 作为 icns。
- Linux/Windows 命令已替换，新增 macOS arm64 入口；支持显式平台和 x64/arm64 参数。脚本不会隐式构建宿主、迁移工具或 Web，需要先准备匹配的完整 runtime 和 Web 制品。
- 当前完成的是 Electron 应用目录组装规则，签名、公证和系统安装器格式仍未实现；桌面迁移用户交互仍待接线。仅阅读本地 packager 实现/类型并编辑脚本/文档，未调用 packager、构建、测试或真实迁移。

## B6/B7：桌面启动目录切换交互（已编写，未运行）

- 配置页新增独立 `DesktopRuntimeMigration`，显示当前监管目录、已保存的下次启动目录与配置版本，提供选择两目录、恢复上一版及重启桌面。主进程原生目录对话框与明确停止确认拥有最终选择，renderer 不传递任意执行命令或目录参数。
- 专用主进程处理器仅允许主窗口主 frame 调用。当前桌面须拥有两个宿主，存在外部监管或显式目录/token 路径覆盖时拒绝改配，转离线流程；不会杀掉外部服务或静默清除用户启动参数。
- 确认后等待监管器停止两个宿主，再通过固定 Node/固定 select-runtime 入口操作，首次先保存原根目录作为基线。子进程环境仅含启动必要 OS 变量，输出大小和时间受限，不继承模型凭据。开发模式使用源码入口与 tsx，安装包使用已组装的独立迁移 bundle。
- 配置写入后本次监管器保持停止，不尝试用已固定旧根目录重新启动；页面提示显式重启桌面，失败同样提供重启入口。下次主进程自动读取 userData/runtime-selection.json；显式指定其他选择文件时沿用该文件。
- IPC 保留忙碌互斥并拒绝操作中重启，失败保留离线命令产生的原始状态与历史，不推断是否未提交。界面分别展示当前与下次根目录，避免保存成功却仍显示旧目录造成误解。
- 当前 UI 覆盖已激活目录选择/配置恢复，不代替快照导入、共同激活和副作用对账；这些仍走现有明确离线入口。仅编辑源码/文档，未启动 Electron、调用 IPC、停止真实服务、切换目录或运行测试。

## B1/B6：自主唤醒检查/上下文决策标记兼容（已编写，未运行）

- 阅读归档 `eden-agent-app/src/self_awake.rs` 的实际行为：run_safe_check 仅保存 requested 的 local_runtime 标记，sync_context 仅保存上下文决策事件；不自动执行检查或同步。因此移除 TS 对两动作的“未接入”异常，按原业务恢复持久事件，不扩大成未经请求的新工具执行通道。
- 两动作记录前核对活动会话、单角色和原作者；完成事务同时保存 run 结果、旧事件名与 action_applied，再广播。事务失败不会只留下完成状态，动作已完成后不能通过普通 resume 重复插入标记。
- 自醒提示明确标记不代表实际完成，工具检查/同步仍须在模型轮次中通过现有审批执行；结果 status=requested，不能宣称检查通过。前端执行记录沿用旧事件名，修改 action_applied 的泛化标签，避免将所有动作误标为“保存任务或提醒”。
- 仅阅读归档/当前源码并编辑实现和文档；未运行模型、动作、SQL、测试或构建。此前记录的这两个最终标记缺口已由本批源码覆盖，其他历史自醒通知/格式恢复仍未完成。

## B1/B6/B7：历史自醒通知未知结果核对（已编写，未运行）

- 新增通知记录预览与明确处置 RPC，绑定原行 SHA-256，只接受 unknown → delivered/suppressed；M101 保存原始行、核对依据、人工决定与时间，和状态修改同事务。重复同一确认幂等，不同依据拒绝覆盖。
- 历史结果读取区分原运输回执与人工核对；保留原 payload、result、错误和尝试次数，不把人工确认伪装成新渠道回执。delivered 不表示用户回复，suppressed 不表示之前没有副作用。
- 自醒恢复面板的各运行记录接入独立通知核对组件。迁移审阅模式可使用，操作不调用渠道、不创建作业、不重开会话，也不清除其他未知工具结果；恢复摘要据实际通知状态退出 unknown 项。
- 仅阅读/编辑源码文档，未运行 RPC、SQL、通知发送或测试。明确再次联系用户仍使用现有需授权的新联系流程；历史通知自动重投未加入，其他旧格式/运行恢复继续推进。

## B1/B6/B7：旧自醒载荷与中断运行处置（已编写，未运行）

- 旧 jobs 转换对所有 self_awake 状态适配受限触发载荷，而非只处理 scheduled。非 scheduled 保留原状态、审阅错误和原稳定作业键，转换不把未知/失败作业改成 queued；原完整载荷仍在导入追溯记录中。
- M102 保存历史 interrupted run 的人工处置、原行及依据，要求原作业/关联输入已明确终止；确认 completed 前不能存在未知/运行中工具操作。处置只更新运行状态和审阅时间，不重演原决策、通知或定时器，不清除原错误/日记。
- 协议与运行记录增加 outcomeReview，明确区分人工确认和模型实际完成；自醒恢复面板接入处置表单，先处理原输入与作业，再处理 run。迁移审阅模式开放读取和处置，失败运行后重新决策仍是独立确认流程。
- 修改后的转换器用于尚未转换的域；既有已完成 jobs 域不会被 resume 自动重写。旧阶段已生成的不兼容副本应另建暂存目录重新转换，后续如需原地修复须单独实现绑定源证据的适配，不能覆盖已恢复的业务记录。
- 只编辑源码/文档，未执行转换、RPC、SQL、模型或测试。Mon 外部归属恢复与部分历史决策适配仍待完成，不能将此批视为全部迁移完成。

## B1/B7：旧 Mon 自醒提交归属与幂等请求恢复（已编写，未运行）

- 阅读旧 `rust/self_awake_bridge.rs` 的 enqueue/read_status：原宿主将用户、事件、完整 context 和 self-awake:user:key 保存在 jobs，并通过后台会话 environment 约束用户；没有新宿主式独立提交表。
- jobs 转换同事务恢复符合旧 bridge 契约的 self_awake_submissions，要求世界为 mon、后台会话用途、会话 userId、载荷 userId、schema、事件和幂等键前缀全部一致。不从单个路由字段推断归属，不复制服务凭据。
- 使用原完整 trigger 重建当前 bridge 解析后的请求对象及 SHA-256，而非运行提示的截断 allowlist。相同请求重试可返回原作业；改变 context/事件等内容则通过现有 hash 比较拒绝，同一键不会创建第二次自醒。
- 恢复关联使现有拥有者状态查询、自醒最近日记与后续明确重提能够使用原归属。实际访问 Core 仍要求当前 Mon 身份与原用户一致，未导入任何访问令牌。
- 缺少/冲突证据或旧请求超出当前契约时拒绝该 jobs 域事务，源快照不改；已转换域不自动重写，需新暂存转换或后续单独证据绑定修复。仅编辑源码文档，未运行转换、网络请求、服务或测试。

## B1：自醒最近联系证据涵盖外部渠道（已编写，未运行）

- 最近联系上下文原本只有桌面提醒，尽管 QQ/邮件已有本地运输账本。新增独立 recent-contacts 查询，统一读取桌面窗口状态、外部联系回执及历史自醒通知，按时间和来源 ID 排序后共同限量，避免每类分别限量造成顺序混乱。
- 保留 accepted/delivered/displayed/closed/unknown 等原状态及渠道，历史人工决定单独标记；展示内容仅取自醒决策或桌面消息的受限标题/正文，不读取外发 payload、收件地址或凭据。缺少自醒决策关联的普通外部联系仅提供回执身份与状态，不猜测正文。
- 保留作者归属；当前会话可读取自身证据，跨背景会话聚合仍必须通过当前 Mon 用户与持久提交归属校验。本批同时补环境 selfAwakeUserId 与当前身份的一致性检查，防止会话环境变化后继续借用旧提交关联获取用户上下文。
- get_self_awake_context 的 limit 现在作用于最近联系结果，不再固定返回十项。提示明确这是本地回执摘要、不是远端收件箱，没有状态可单独证明用户已读或回复，未知结果先核对。
- 仅阅读与编辑源码/文档，未执行查询、网络请求、模型、服务或测试。

## B3/B7：根会话收件箱与向父级通信（已编写，未运行）

- M103 新增根会话收件箱，保留发送者会话、内容、幂等键和读取时间。根智能体可以通过现有 read_agent_messages 读取最多十条，和子任务读取一样确认已消费；读取前核对历史上下文可续接。
- 新增 send_parent_message 工具，经 agent.manage 审批后发送给直接父级；父级为子任务时进入其现有信箱，顶层任务发送进入根收件箱。核对任务树和父级活动状态，消息内容与幂等键绑定、未读容量 128，不自动启动父级回合或授予工具权限。
- 历史 `/root` 普通消息在发送者任务树与原文明确时，可通过原恢复表单明确释放到根收件箱；同事务保存审阅记录和状态，不重演 trigger_turn。根输入运行或排队时拒绝释放，避免并发读取与审阅交错。控制消息的完整续接语义仍待实现。
- 角色只读策略按已有智能体通信规则允许新通信工具，但具体角色/父级白名单仍收窄；每次发送仍需审批，既有已固定角色快照不自动扩大。
- 仅阅读和编辑源码/文档，未执行通信、数据库语句、模型或测试。

## B3/B7：历史完成通知恢复（已编写，未运行）

- 对照旧 multiagent 的 enqueue_agent_message 调用确认：completion 用于完成/失败/中断后的父级通知，followup 才请求后续运行。将 completion 纳入明确释放的可读消息，不再一概只能归档。
- 新导入可将归属明确的 completion 映射进子任务信箱；根通知沿用 M103 收件箱。释放时进一步核对目标为发送者直接父级，根目标只接受顶层任务完成通知，不能用路径字符串制造错误父子归属。
- 收件结果新增 kind、historical 与受限 details，区分历史完成事实与新普通消息；正文/原详情仍保留，详情超过 16000 字符返回截断预览与说明。读取不会修改当前任务状态、预算或触发回合。
- 恢复表单说明和选项同步更新，旧 trigger_turn 继续不自动执行。既有已转换域不原地重写，followup 的显式续接仍待接入；不能把 completion 恢复视为所有控制语义完成。
- 仅阅读旧/新源码并编辑文档，未执行导入、消息读取、模型或测试。

## B3/B7：历史 followup 明确续接（已编写，未运行）

- 原信箱处置增加 prepare_followup：核对 root 发送者、未消费原记录、目标任务和 64000 字符上限后，仅保留为 followup_prepared，不入队。这解除“旧信箱待处理阻止任务重开、但任务重开前又无法续接”的依赖循环。
- 新预览返回完整原文与目标，普通模式的 apply 要求 fingerprint、说明和明确执行确认。复用 repository.followup 的历史恢复状态、工作区、未决请求、期限、轮次及容量限制；M104 来源关联与新作业同事务保存，重复相同确认返回原任务，不再次消耗一轮。
- 提交后标记 followup_submitted，保留旧 consumed_at、内容及准备/执行两次确认记录；不通过读取普通信箱意外消费续接，也不根据旧 trigger_turn 自动执行。
- 前端准备后刷新列表可查看完整内容并明确执行。迁移审阅只允许准备/预览，apply 未开放。已准备续接的再次放弃入口仍需补充；用户仍可不执行，准备状态本身没有外部效果。
- 仅阅读/编辑源码与文档，未运行任务、SQL、迁移或测试。
