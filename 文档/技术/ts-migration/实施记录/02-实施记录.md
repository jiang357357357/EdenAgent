# TS 迁移实施记录 02

### 2026-09-09：角色检查点与工具调用归属

- 上一轮完成真实导演模型请求及持久化屏障，属于实质进展。本轮新增 `actor_checkpoints`，
  ActorCheckpointRepository 按 sessionId/assistantId 保存检查点，与单角色会话检查点分开。
  检查点和 actor.checkpoint 事件同事务写入；拒绝跨会话快照，事件失败则回滚快照更新。
- 运行时新增宿主指定的 toolCallPrefix；beforeTool、工具执行上下文、afterTool 使用同一带前缀调用编号，
  pi 模型协议仍保留供应商原始 callId。这样同一回合不同角色返回相同编号时不会覆盖操作或权限请求。
- runtimeCallbacks 支持指定检查点存储与 actor（assistantID/planID/beatIndex）归属；
  模型请求、工具意图/结果和运行事件带角色信息，未指定时保持既有单角色行为。
- 验证：两个独立录制模型实际执行工具并回答，均产生 call-1，但数据库中是两条独立 completed 操作，
  工具执行收到相同的规范化编号；两份检查点包含各自内容，不互相混入，也不写入单角色会话检查点。
  另外覆盖检查点事务故障、会话隔离及错配快照拒绝。
- `npm run typecheck:server`、工程门禁（**132 个源文件，0 错误**）及
  `npx tsx --test Server/tests/actor-checkpoints.test.ts Server/tests/director-planning.test.ts Server/tests/server-runtime.test.ts`
  **8/8 通过，无跳过**。测试只使用临时/内存数据库和本地录制模型。
- 尚未连接导演节拍执行协调和 SessionService 多角色入口；公开角色消息格式、共享对话观察、每角色恢复、
  取消/信号归属及工具负责人规则仍须继续实现和验证。本批不宣称完整多角色对话已经可用。

### 2026-09-09：角色发言执行器

- 上一轮完成独立检查点与调用编号，本轮新增 ActorExecutionService，按导演节拍核对 assistantId，
  创建真实 pi runtime，读取/保存该角色检查点并使用 planID/beatIndex 隔离工具调用编号。
- actorPrompt 显式提供原始用户消息、导演场景/节拍及前序公开回复；角色资料作为上下文，不授予权限。
  内部 user/tool 消息归入 actor.runtime 审计事件，不写入公开 agent.message_end 历史；角色 assistant 消息保留角色归属。
- 同一执行器拒绝同会话并发角色；外部取消及执行器 close 取消当前 runtime 并等待其排空，关闭后拒绝新执行。
  错配角色在模型请求前拒绝。协调层尚未连接，模型解析与工具负责人过滤仍由后续调度集成负责。
- 验证：重建角色执行器后，第二次真实模型请求带有该角色上次回复和另一角色公开回复；
  公开历史仅含两条 assistant 消息，没有重复内部任务消息。关闭实际等待中的录制 HTTP 请求不产生额外请求。
- `npx tsx --test Server/tests/actor-execution.test.ts Server/tests/actor-checkpoints.test.ts` **3/3 通过**；
  类型检查和工程门禁（**135 个源文件，0 错误**）通过。
- 下一批连接规划器、执行器、导演状态与 SessionService 多角色入口，并补工具负责人、共享观察与输入信号规则。
  本批仍不是完整 UI 或 P0–P5 验收；生产数据未读取或迁移。

### 2026-09-09：导演与角色执行协调

- 上一轮完成角色执行器和恢复测试，属于实质进展。本轮新增 CompanionTurnCoordinator，将导演模型请求、
  持久计划、逐节拍角色执行及进度提交连接起来。开始前确认名册角色均有模型，并复制本回合模型配置。
- 用户消息只写入一次公开历史；每节拍先持久化开始状态，角色完成后持久化完成状态，之后才进入下一节拍。
  执行或状态写入失败则记录失败并停止；取消当前角色后不启动下一角色，关闭协调器会取消并等待现有任务。
- 共享观察只投影公开文本、消息角色与 assistantID，过滤 thinking、toolCall、toolResult 和无关身份字段。
  下一角色实际请求包含前一角色公开回复，内部导演输出和工具参数不通过共享观察传递。
- 验证：`npx tsx --test Server/tests/companion-turn.test.ts` **4/4 通过**，
  使用 1 个导演 + 2 个角色的独立本地录制 HTTP 模型；核对两节拍完成、公开消息顺序、共享回复、
  取消后第二角色请求数为 0、节拍提交失败后第二角色请求数为 0，以及共享观察内容过滤。
  类型检查和工程门禁（**138 个源文件，0 错误**）通过。
- 协调器现已能独立执行完整两角色回合，但尚未接入生产 SessionService 队列与多角色 RPC 入口。
  后续接入时须保留输入幂等、模型快照比对、重启 held 队列、取消/steer/follow-up，并补齐工具负责人策略与 UI 事件契约。
  当前不将独立协调器测试当作产品端到端验收，P0–P5 长期目标保持不变。

### 2026-09-09：多角色接入生产会话队列

- 上一轮完成独立协调链路，本轮通过 SessionTurnExtension 接入 SessionService，生产容器实际注册 CompanionSessionExtension。
  会话模块仅依赖扩展契约，不反向依赖导演/角色模块，避免循环依赖。
- 多角色 turn.start 先记录参与者、环境和各角色不含凭据的模型描述，再走原输入幂等/claim/finish 队列。
  执行前再次比对所有模型描述，变更则中断且不请求导演或角色。Mon 必须逐角色绑定，不借用单角色模型；local 使用本地配置。
- 取消和宿主退出将信号传给协调器并等待会话排空；普通取消不登记为存储故障，取消同时发生的其他异常仍继续传播。
  生产关闭流程同时排空插件、会话、Mon 请求和协调器。
- 使用实际 createServices 与 sessionRoutes 验证：幂等输入只有一条记录且 inputId/turnId 不变，
  1 个导演请求与两角色回复完成后输入 completed；角色等待中取消则 input interrupted、导演 failed、后一角色请求数为 0；
  排队模型变更时全部请求数为 0。`Server/tests/companion-session.test.ts` **3/3 通过**。
- 幂等测试最初错误地要求两次提交状态相同；实际原输入已从 queued 进入 running。已改为验证稳定身份及唯一记录，保留真实进度返回。
- 类型检查、工程门禁（**140 个源文件，0 错误**）、构建和两个隔离 Node 制品健康检查通过。
- 当前导演暂使用名册首个角色模型，独立导演模型选择策略还需与旧行为对齐。
  多角色 compact 明确未实现；steer/follow-up 当前会进入后续输入队列，尚未实现活动角色即时注入及节拍间信号规则。
  工具负责人策略、完整 UI/模型状态展示、权限及其余旧业务仍需继续验收，不能把本批队列测试视为全量完成。
- 本批最终整套回归：`npm test` **3 项工程门禁 + 78 项宿主测试通过，零失败、零跳过**。
  修正前已启动的那次整套结果为 77/78（唯一失败是上述幂等断言），已在当前工作树完整重跑通过。
- 补充旧行为核对：归档 `runtime/turn/prepare.rs` 使用会话 active_model_spec 请求导演，
  发布 `companion.director.started` / `companion.plan` 等事件；当前首角色导演模型及 director.* 事件需在后续兼容工作中对齐，
  不能仅凭 `director.list` 结构相近就宣称旧前端已完全兼容。

### 2026-09-09：导演事件前端兼容与原子提交

- 上一轮已接通生产多角色队列，本轮对照旧前端 agent-client/rpc-transport/session-reducer 和 Rust 发言事件完成导演兼容映射。
  保留内部 director.* 审计事件，同时在同一事务中产生 companion.director.started、companion.plan、
  companion.speaker.started/finished、companion.director.completed/failed，提交后才广播。
- director_runs 新增公开参与者快照，重启后仍可重建发言者字段；只存允许的标量字段，不复制 profile、任意嵌套对象或凭据字段。
  计划 userMessageID 与队列 input.id 对齐，公开用户消息也使用该 ID，避免计划引用另一个随机消息编号。
- 使用现有前端 projectSessionEvent 和 runtimeReducer 实际回放持久化事件，最终识别 completed、正确 userMessageID 和 [0,1] 完成节拍。
  兼容事件写入故障时，内部节拍状态和全部相关事件一起回滚，不出现只更新一边的进度。
- 首轮相关回归 **7/7 通过**（导演前端、运行持久化、生产会话）。门禁发现测试使用计算路径 import，
  已改为固定入口 `Script/Project/verify_director_frontend.mjs` 独立进程，保留真实前端代码验证，不添加架构例外。
  修改后前端兼容专项 **2/2 通过**，服务端/前端类型检查及工程门禁（**142 个源文件，0 错误**）通过。
- 用户已有前端修改未触碰。本批验证导演状态投影，不等于浏览器或 Electron 完整 UI 验收；
  角色消息头像归属、完整页面加载依赖、模型状态、即时信号、压缩和其他业务仍待继续迁移。

### 2026-09-09：角色消息姓名、头像与编排归属

- 上一轮完成导演状态事件兼容，本轮核对前端 apiMessage：其读取 message.speaker / message.orchestration，
  仅有外层 actor 审计字段不会显示角色姓名和头像。现已在角色 assistant 事件投影时补齐这两个字段。
- speaker 只包含公开身份字段；orchestration 包含 planID、导演来源、场景、执行策略、节拍、回应对象及意图。
  原始运行时消息/checkpoint 不因 UI 元数据发生重写；内部用户/工具消息仍留在 actor.runtime 审计事件。
- 将公开参与者投影移到 actors/public-speaker.ts，导演与角色消息共用，避免 actor 反向依赖 director 形成环。
  所有入口都过滤未识别字段与嵌套对象，角色 profile 和凭据字段不会复制到公开 speaker。
- 验证真实录制模型的两条角色回复经过现有前端 projectSessionEvent 和 runtimeReducer 后，
  姓名、assistantID、头像 URL、planID 和 beatIndex 保持正确，公开消息不含测试 profile 中的 apiKey。
- `npx tsx --test Server/tests/actor-execution.test.ts Server/tests/director-frontend.test.ts Server/tests/companion-session.test.ts`
  **7/7 通过**；类型检查和工程门禁（**144 个源文件，0 错误**）通过。前端源文件没有修改。
- 尚未完成：多角色模型可用状态与页面加载依赖、即时信号、压缩、导演模型策略和完整浏览器/Electron 验收。
  本批证明消息投影与状态归属正确，不把 reducer 测试当作实际页面渲染验收。

### 2026-09-09：多角色模型可用状态

- 上一轮完成消息归属，本轮修复 model.read 仅查看单角色绑定、导致已绑定多角色仍显示 unavailable 的问题。
  RPC 使用会话实际名册检查，新增 mode=multi_actor 和各 actor 的公开状态；全部角色配置有效才 available=true。
- 缺少任一绑定、无效或重复 assistantId、超过 32 名角色时返回不可用；Mon 的旧单角色模型不能掩盖角色绑定缺失。
  local 使用本地模型配置。撤销绑定后状态立即变为不可用；状态读取不会创建可执行的共享模型绑定。
- 抽取 model-status.ts 负责公开字段投影，单角色响应结构保持原契约，所有状态均不返回 apiKey。
  多角色顶层不伪装成某一个共享模型；每个模型的 id/provider/预算位于对应 actors 项中。
- `npx tsx --test Server/tests/model-status.test.ts Server/tests/companion-session.test.ts Server/tests/mon-binding.test.ts`
  **10/10 通过**；类型检查与工程门禁（**145 个源文件，0 错误**）通过。
- 新增响应字段仍需纳入完整 TS 客户端生成及页面展示验收；本批验证的是状态服务与 RPC 行为，
  不代表模型选择界面、其他页面加载依赖或完整 UI 已完成。即时信号、压缩、其余业务与 P5 继续推进。

### 2026-09-09：活动角色即时输入

- 上一轮完成多角色模型状态，本轮将 SessionService 的 steer/follow-up 经扩展、协调器传到当前 ActorExecutionService 的 pi runtime。
  仍先保存 accepted 信号，再注入并记录 injected；实际 user 消息消费时记录 consumed，不新建输入回合。
- 统一角色消息回调中的信号消费逻辑：内部任务 user 消息继续隐藏，真正消费的用户信号进入公开历史。
  节拍完成后从持久化公开消息刷新共享上下文，后续角色能看到用户新指令及前序公开回复，不共享思考/工具原始内容。
- 会话取消排空期间拒绝新注入，排空后允许重新发起后续输入。未处于活动角色运行阶段（例如导演规划期间）时，
  注入尝试标为 rejected，并将文本作为后续持久输入排队；节拍间重规划和更细的信号目标归属仍待后续处理。
- 验证：两种信号均在第一个角色等待工具时注入，同一 turnId、inputs 仅一条，信号最后 consumed，
  当前模型及下一角色模型请求均包含新指令，公开历史恰含原始与注入两条用户消息。
  注入前数据库故障时信号记录为 0，模型请求中不存在被拒绝文字。
- 初轮角色/协调/信号回归 **12/12 通过**；补充故障注入后信号回归 **6/6 通过**；
  类型检查和工程门禁（**146 个源文件，0 错误**）通过。
- 尚未完成：多角色压缩、导演重规划策略、全量协议生成、完整 UI 与其他 P4/P5 业务。当前结果不改变长期验收范围。

### 2026-09-09：多角色排队压缩

- 上一轮完成活动角色即时信号，本轮新增 ActorCompactionService，并接入原 session.compact → 输入队列 → 模型快照比对 → 协调器路径。
  压缩与发言共用同会话互斥和取消边界，逐角色读取/更新自己的检查点，不产生额外导演计划。
- 无历史的角色记录 skipped，不发起模型请求；有历史则记录 started，调用真实 pi compact，并记录 completed。
  请求、事件和检查点仍走持久化屏障，不向压缩运行时提供工具。
- 采用逐角色持久提交：第二个角色取消时，第一个角色已完成的摘要保留，第二个原始检查点保留，整个输入标为 interrupted，
  对应记录分别为 completed/cancelled；不宣称全部成功，不自动重放。数据库错误不会被普通取消逻辑吞掉。
- 初轮压缩/历史回归 **7/7 通过**；补充请求写入故障后多角色压缩专项 **4/4 通过**，
  证明记录失败时两个摘要请求均未发送、两个原检查点均保留。全部使用本地录制模型及内存数据库。
  类型检查和工程门禁（**148 个源文件，0 错误**）通过。
- 当前完成手动多角色压缩执行路径；仍需验证后续恢复与前端压缩进度展示，以及自动压缩的业务策略。
  导演模型/重规划、全量协议生成、完整 UI、其余旧业务和 P5 尚未完成，长期目标继续推进。

### 2026-09-09：压缩恢复、共享历史边界与环境快照

- 上一轮完成多角色手动压缩，本轮实际关闭并重开临时 SQLite 文件，重新创建角色执行器后发起两个模型请求。
  两个请求分别包含各自持久化摘要，不包含另一角色的摘要；这补足了只看数据库中有摘要的间接证据。
- 对照旧 director_conversation_context 的 12000 字符边界，新 conversation-window.ts 对序列化共享上下文限长，
  优先保留最近消息和最近文本，保留说话者身份，截断项带 truncated 标记，按 Unicode 字符切割且计入 JSON 转义开销。
  仅截断模型侧观察窗口，不删除持久化原文。此为共享窗口界限，不宣称已经替代完整 token 预算或自动压缩策略。
- 修复角色执行提示遗漏 input.metadata.environment：现在使用排队时保存的环境，避免会话环境后来改变而影响已排队请求。
  实测保存 UTC 后将会话环境改为 Asia/Shanghai，角色恢复请求仍包含 UTC，不包含新的时区。
- 恢复/窗口/即时信号相关 **5/5 通过**，角色执行及环境快照回归 **2/2 通过**；
  类型检查和工程门禁（**151 个源文件，0 错误**）通过。
- 尚未完成全量协议生成、浏览器/Electron 渲染验收、自动压缩策略、导演配置对齐及其余 P4/P5 业务。
  本批增加真实恢复证据并修复上下文传递，长期目标保持完整范围继续推进。

### 2026-09-09：持久化用户问答

- 上一轮完成压缩恢复与上下文边界，本轮迁移 question.list/resolve/reject 和模型工具 eden_question。
  API schema、question_requests 表、QuestionRepository、QuestionService、工具及 RPC 按职责拆分，生产容器已接入。
- 问题先提交并登记等待者再广播 question.requested；答案校验并与 question.resolved 事件同事务提交后才唤醒模型。
  保持旧请求/结果字段，支持最多 3 个问题、选项、多选及自定义答案；拒绝无效选项、重复答案和不完整回答。
- 拒绝、取消和重启恢复分别持久记录 rejected/cancelled/interrupted；list 只返回 pending。
  关闭时终结等待者，持久化失败继续向宿主关闭流程报告。提交答案先于其 resolved 广播触发的取消时，以已提交答案为准。
- 实际录制模型验证提问后停在工具等待，用户答案写入成功后才产生第二次请求；答案写入故障时保持 pending 且不继续请求。
  问答/生产会话/鉴权宿主相关 **9/9 通过**；类型检查、工程门禁（**158 个源文件，0 错误**）、构建和双 Node 制品检查通过。
- 初次测试发现 z.toJSONSchema 默认输出形态将有默认值的 multiple 标为必填，模型工具调用提前被拒绝。
  已改为 io=input；确认旧测试进程仍在等待后停止该测试，再修复并重跑通过。测试等待新增 5 秒超时，防止类似问题无限等待。
- 仍需实际前端问答展示/回答交互及完整页面验收；全量协议生成、Blob、导演策略和其他旧业务/P5 继续推进，未宣称完整迁移完成。
- 本批最终 `npm test`：**3 项工程门禁 + 94 项宿主测试通过，零失败、零跳过**。
  用户原有前端三处修改保持原样，未调用真实模型或 Mon，未读取/迁移生产 Data。


