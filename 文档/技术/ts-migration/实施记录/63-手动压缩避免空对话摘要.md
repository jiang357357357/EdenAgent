# 手动压缩避免空对话摘要

2026-09-11，用户反馈对话压缩失败。

## 原因与修复

只读核对真实失败表现：压缩输入 03b67138-1e1e-42f5-ae24-0dcd5e23d7c9 标记 completed，但摘要声称没有提供对话；retainedTail 仍保留原始消息。SDK 默认 keepRecentTokens=20000，当前对话全部落在保留区，待总结消息为空；请求用量包含系统和工具，不能据此认定对话正文超过保留阈值。

runtime-pi 通过公开 session_before_compact 钩子和 prepareCompaction / compact API 处理手动压缩：先保留约 4096 token，若无待总结正文，再收紧保留预算为 0（SDK 仍按有效切分点保留尾部）。没有实际可总结消息时明确失败，不请求空对话摘要；模型返回空摘要时不提交压缩记录。保持取消信号、请求先持久化、压缩结果检查点与事件流程。普通与多人角色共享该运行时边界。没有修改 node_modules 或调用 SDK 私有导入。

历史摘要不覆盖，原始对话和审计仍保留。本次不声称每次压缩都能减少总字符：非常短的对话可能比结构化摘要更短；系统提示与工具定义也不属于对话压缩对象。已压缩且没有新增消息时，SDK 仍会拒绝重复压缩。

## 验证

- compaction-boundaries / persistence-boundaries 共 9 项通过，覆盖低于默认保留阈值的真实正文入参、摘要恢复续聊、空会话、取消以及持久化失败。
- 服务端 TypeScript 通过，架构 683 文件、0 错误，保留既有规模警告。
- 无活动输入时重启开发服务。
- 独立验收会话 5eebd477-4e58-466a-a2cb-e1b84b1fe9eb 经真实 RPC 执行 session.compact，turn dd75d430-c600-47ce-9fd9-ea9b733bb040 completed。真实模型摘要准确包含上下文传输验收、历史会话恢复验收及“只回复通过，不调用工具”的要求，保留尾部 1 条消息，没有生成空对话占位摘要。
- 自动测试只用内存/临时存储；用户数据库仅只读诊断，验收会话通过正常 RPC 操作。
- 真实续聊 turn 345dd323-ef65-444b-a055-10d162db470b completed，最新实际请求确认包含压缩摘要中的两项验收上下文，恢复链路通过。
