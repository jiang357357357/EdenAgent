# 官方连接器目录归并

按用户批准，将根目录 `Connectors/official` 移至 `Server/connectors/official`，保留独立 worker 构建边界。

## 变更

- 原目录 36 个文件全部移动；逐文件 SHA-256 对比确认只有四个 worker 的 Cargo.toml 内容变化，用于修正 Native 依赖的相对路径。
- 更新根 Cargo workspace、连接器打包脚本、TS 源码模式资源定位、Linux OpenTTD 桥接路径、Windows HOI4 安装入口及当前结构文档。
- 根 `target/`、`dist/connectors/` 和打包宿主的运行时资源布局保持原位。
- 新增 `Server/connectors/README.md`，说明目录职责及根目录构建命令。
- 首次 Cargo 锁定检查发现锁文件仍包含已归档宿主依赖。离线刷新后由 390 个包收敛至 170 个包，补入当前本地 `eden-agent-openttd` helper；没有升级外部依赖版本。

## 实际验证

- `cargo metadata --offline --locked --no-deps --format-version 1`：通过。
- `cargo check --offline --locked --workspace --bins`：通过；Lichess 仍有一条既有未使用导入警告。
- 四类连接器分别执行 `package_connector.mjs <id> --profile debug`：通过，使用当前 target/debug 中已有 worker 制品验证新源码目录打包。
- 临时目录中验证无 dist 制品时的源码资源定位：四个官方清单均可读取，未知 key 被拒绝。
- `node --test Script/Project/openttd_launcher.test.mjs`：9/9 通过。
- `npm run test:server-artifact`：宿主及迁移工具构建通过，双宿主真实进程测试 1/1 通过。
- `npm run check:architecture`：613 个 TS 文件，0 错误；`git diff --check` 通过。

本轮为目录归并的相关验证，没有重新运行全部 682 项测试。Windows 安装入口仅核对路径，未在 Windows 或真实游戏中运行。不修改用户数据和 frontend 既有改动，未自动提交 Git。之前实施记录中的旧路径是历史事实，当前路径以本记录及目录说明为准。
