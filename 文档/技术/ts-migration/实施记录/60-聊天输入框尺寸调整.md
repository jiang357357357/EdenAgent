# 聊天输入框尺寸调整

2026-09-11，按用户要求缩窄、加高普通聊天输入框。

- ChatInput 普通布局宽度改为原可用宽度的 90%，居中。
- 最小总高度从 16vh 调整为 20vh；文字区域从 9.3vh 调整为 13.3vh，保留底部操作区空间及长文本滚动。
- 本次为 CSS 尺寸调整，未新增自动测试；前端类型检查结果见下。
- 验证：npm --prefix frontend/web run typecheck 通过，frontend diff --check 通过。未进行实际界面验收。
