# 请求分类估算与前缀记录

2026-09-11。补齐上下文浮层中缺失的分类估算，保留供应商实际输入、输出及缓存用量作为独立数据。

- runtime-pi 在实际发送的请求快照中标记工具提示词来源。会话模块按实际系统消息拆出参与者配置与技能目录，统计其余系统文本、工具定义及请求消息；不重复计数。
- 分类采用文本/JSON 启发式估算，并在界面明确标注。请求消息包含当前输入、历史回复和工具结果；通过工具读取的技能正文在这里统计。图片数据不按 Base64 文本估算，供应商消息封装与图片计费仍以实际返回总量为准。分类和不要求等于实际输入。
- 系统消息、工具定义、供应商、模型和请求中的推理级别生成 SHA-256 本地前缀指纹。按会话和执行角色从持久化请求读取上一代，普通消息增长不改变代数。该状态不代表远端缓存失效或完整缓存前缀命中。
- requestId 关联响应与估算，随 model.response 持久化并广播；现有实时事件和会话恢复共享解析逻辑。旧响应没有分类数据时继续显示未提供，不伪造历史数值。新请求返回后更新。
- 命中率保留两位小数，92,032 / 92,463 显示 99.53%。

验证：服务端统计与恢复专项 7 项、前端实际组件及事件映射 3 项通过；前后端类型检查通过。补充 runtime-pi 回归与拆分后的检查结果见下方。测试使用内存数据库，未调用真实模型、未重启用户服务。

最终检查：`node --import tsx --test Server/tests/request-context.test.ts packages/runtime-pi/tests/*.test.ts` 23 项通过；拆分后服务端类型检查再次通过；`npm run check:architecture` 659 个源文件、0 错误（既有规模警告仍保留）；三个仓库的 `git diff --check` 通过。尚未进行真实 Electron 页面或供应商调用验收。运行中的旧服务须重新加载新代码，新请求完成后才能生成新统计。
