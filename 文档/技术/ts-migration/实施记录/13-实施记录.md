# 实施记录 13

## B6：TypeScript RPC 生成入口（已编写，未执行）

- 将旧生成文件的兼容声明迁入 packages/api/src/legacy-wire.ts，包提供显式子路径导出；浏览器上传/连接实现单独保留为模板。
- 新增 Node 生成器，读取 API 契约与模板，附带来源摘要，临时文件写入后替换输出；根 generate:rpc 已移除旧 Rust Cargo 命令。
- 目前前端旧生成文件没有被重写，生成器未运行。legacy-wire 是待逐域替换的兼容基线，不是已完成 schema 驱动的最终协议事实来源。旧 bigint、默认值及临时 RPC 类型扩展均继续列入 B6。
- 具体来源、生成命令和剩余工作见 ../协议生成迁移.md。当前只迁移声明和编写脚本，没有读取 Data、运行生成器、构建、类型检查或测试。

## B6：共享 RPC 方法表首批接入（已编写，未测试）

- API 新增 rpcMethods/SchemaRpcMethodMap，MCP 状态、操作历史、结果读取/导出四个方法关联现有参数和结果 schema。
- 服务端新增 contractHandler 并接入四个 MCP 方法，统一解析参数、等待业务结果、校验输出；不把业务 SQL 移入传输层。
- 前端移除 MCP 面板与结果组件的重复 RPC 接口声明，集中通过 rpc-contracts 类型桥继承 API 方法表，状态/历史也直接使用 API 推导类型。
- 浏览器生成模板按共享方法表覆盖兼容映射；生成摘要改为递归包含整个 API TS 源码树。其他业务的零散声明尚未迁完，不以四个方法代替 B6 全量目标。
- 本轮只编写源码及文档，未执行生成器、类型检查、构建、测试或真实 RPC。

## B6：连接器权限、凭据与历史协议（已编写，未测试）

- 新增 connectorRpcMethods，八个管理方法（凭据状态/写入/删除、权限读取/设置、事件列表/详情、调用历史）复用共享输入输出 schema，并并入 API 方法表。
- 输出明确区分事件、操作、权限快照与凭据状态；凭据响应仅含 configured/supported/generation 等元数据，不包含密钥。
- 连接器路由使用 contractHandler 校验结果，前端权限与凭据页面移除本地类型和 RPC 声明；历史面板从 API 推导事件/操作展示字段，不再声明独立方法契约。
- 共享表当前共十二个方法。连接器目录/身份配置、原有 83 项兼容 RPC 和其他新业务接口仍待逐域迁移；没有执行生成器或据此声称全协议完成。
- 本轮只编写源码和文档，未运行构建、类型检查、测试或连接真实服务。

## B6：连接器目录、身份与前端协议覆盖（已编写，未测试）

- 连接器目录/列表/创建/更新四个方法加入共享 schema，覆盖 generation、状态、数字时间戳、能力描述和可空 invocation；连接器全部十二个当前管理接口已接入共享表。
- 前端 agent-client 的连接器类型改用 API 推导类型，修正创建身份默认传空 displayName 与服务端 minLength 冲突，默认采用 identityKey；更新 patch 保留明确类型。
- rpc-contracts 改为以共享方法覆盖旧映射的组合类型，避免旧显式 bigint 字段与接口扩展冲突；rpc-transport 通过受控 JSON 传输适配，校验已迁移请求及响应，其余接口仍保留兼容映射。
- 本次补丁首次应用因同文件重复操作被工具拒绝，随后以文件编辑完成；未运行生成器或测试来替代用户要求的业务优先顺序。当前共享方法共十六个，原有全部协议目标仍待完成。

## B6：备忘、通知与作业方法表（已编写，未测试）

- memoRpcMethods 接入备忘列表/创建/更新/完成/归档、通知列表/确认及作业列表/读取/取消，共十个方法；服务端路由统一使用参数/结果 schema。
- 备忘前端类型改用 API，移除通知 RPC 重复声明；创建、更新、完成与延后不再构造旧 bigint，改传数字 ID/毫秒时间戳，创建和 patch 使用共享 schema 校验。
- 备忘日期展示转换改为数字类型；空时间保留 null，非法日期在 schema 边界拒绝。任务取消仍遵守业务仓库对已开始派发作业的限制，不因协议统一放宽行为。
- 当前共享方法表二十六项，仍非完整协议迁移。未执行生成、构建、类型检查、测试或真实业务请求。

## B6：媒体与桌面提醒契约（已编写，未测试）

- 新增媒体请求输出 schema，明确 request/session/turn ID、screen/camera、pending/resolved/rejected/cancelled/expired 状态及数字时间戳。
- 媒体列表/回复、桌面提醒列表/已显示/关闭共五个方法接入共享表和服务端 contractHandler；移除前端桌面提醒重复 RPC 声明。
- 前端屏幕和相机回复在上传前拒绝同时有结果与错误或两者皆空，提交前通过 mediaResolveSchema 校验；不改变捕获审批及宿主 Blob 签名检查边界。
- 当前共享表三十一项，其余业务继续迁移。本轮没有捕获屏幕/相机、上传媒体、弹出提醒、执行生成、构建或测试。

## B6：语音配置、合成与分段契约（已编写，未测试）

- voiceRpcMethods 接入配置读取、TTS/STT 更新、GSV 目录/试听、STT 检测入口、聊天合成和分段列表八个方法，服务端使用统一参数/输出校验。
- 新增发现选项、试听、检测、合成及持久分段输出 schema，明确 Blob ID、缓存、分组序号与数值时间；duration 允许有限非负数，避免无依据强制音频时长为整数。
- 前端合成 configId 改传数字，PersistedSpeechSegment 直接引用 API 类型，移除旧 bigint 分段类型来源。既有音频 Blob 播放流程保留。
- 当前方法表三十九项；模型、会话、插件、自唤醒等其他域仍待迁移，不能按数量宣称协议已完成。本轮未调用 GSV/Mon、试听或检测连接，也未执行生成、构建、类型检查或测试。

## B6：模型与 Mon 状态契约（已编写，未测试）

- 模型读取、目录、选择、Mon 同步状态和操作历史五项加入方法表，服务端统一参数及返回值校验；保留单角色与多角色/director 的完整公共目录字段，不包含模型 API 密钥。
- 模型状态明确 actors/director，目录明确供应商、模型选项、选择来源及角色绑定。同步状态页面移除独立 RPC 声明和手写返回类型，直接使用 API 推导类型。
- 当前共享表四十四项；会话、插件、自唤醒等范围仍在推进。本轮未刷新真实模型目录、切换模型、访问 Mon 或运行生成/构建/测试。

## B6：会话、回合与事件分页契约（已编写，未测试）

- 会话创建/列表/读取/重命名/参与者更新、关闭/删除/压缩、回合启动/取消/追加、ping 以及事件/消息分页共十五个方法接入共享契约，当前方法表五十九项。共享 SessionSummary 使用实际持久化返回结构，数字时间戳和 JSON 参与者；事件序号使用十进制字符串。
- 会话路由统一包装已注册方法的参数及返回值校验；工具目录占位会话不再读取不存在的真实会话元数据。tool.list 尚待共享契约迁移。
- 前端参与者位置及语音配置 ID 改用数字，缺失 profile 使用 null；分页游标保持字符串。事件展示通过独立兼容类型暂时兼容旧生成通知，旧生成客户端本轮未重生成。会话映射移除服务端当前并未返回的 contextTokens/tokenBreakdown 读取；上下文用量恢复仍需后续业务补齐，不能当作完整兼容已完成。
- 未运行生成、构建、类型检查、测试或真实会话请求。新契约仍需 T 阶段测试代码及用户授权后的 V 验证；权限、插件、自唤醒等其他方法继续迁移。

## B6：审批、问答与工作区交互契约（已编写，未测试）

- 新增 interaction-rpc 共享 schema，覆盖审批列表/决定/撤销授权、问答列表/回答/拒绝、工作区信息/切换/目录/文件十项接口，实际路由统一使用参数及返回值校验。当前共享方法表六十九项，不代表旧业务全覆盖。
- 前端审批列表仅显示 pending，避免已允许、拒绝或中断的历史请求再次进入待办。目录映射不再读取当前服务并不返回的 entry.size；文件读取仍保留实际大小、截断、二进制及摘要字段。
- 发现未完成业务：permission.mode.get/set 仍有前端调用但 TS 路由缺失；终端目前只有沙箱执行路径。需要继续实现真实审批策略和独立执行边界，不能以固定成功响应代替。工作区切换已有会话检查但缺少前端注释宣称的持久事件审计，也列入后续实现。
- 本轮只读取和编辑源码/文档，未运行生成器、构建、类型检查、测试或命令执行服务。

## B6：权限模式实际策略与工作区审计（已编写，未测试）

- 对照归档 ApprovalService 的 Restricted/FullAccess/Takeover 语义，补入 permission.mode.get/set 两个共享契约与真实路由。restricted 使用审批/持久授权，full_access 自动允许非终端操作，takeover 自动允许全部经审批服务请求的操作；command.execute 与旧 shell.* 均保留为终端分类。
- 权限模式读取当前世界 runtime_settings，默认 restricted；M51 runtime_setting_changes 与配置更新在同一事务内记录前后值。新请求按当前模式决定，自动允许仍插入 permission_requests 并发布持久事件。已有 pending 请求不会因切换模式被追溯允许；已经批准运行的操作不被回滚。
- 模式只影响审批服务，不修改沙箱、插件版本授权或连接器身份权限；模型无切换入口。独立 command.execution.get/set 和本机执行路径仍待实现，不能把 takeover 视作已支持本机执行。
- 工作区切换的配置写入与 workspace.changed 事件改为同一事务，提交后才广播，事件包含会话、世界和前后路径。仍禁止活动回合期间切换。
- 本轮未打开业务数据库、切换实际权限/工作区，也未运行迁移、生成、构建或测试。

## B6：独立终端执行边界与设置入口（已编写，未测试）

- 新增 commands/CommandService，command.execution.get/set 接入共享契约与认证 RPC；默认 sandbox、无网络、无额外可写根，配置与审计记录位于当前世界数据库。切换 host 必须 confirmHostExecution，host 明确返回网络/文件系统不受沙箱限制，权限为当前 OS 账户。
- eden_exec 审批详情绑定执行配置和 generation，审批等待期间配置改变后拒绝旧命令；执行计数覆盖能力探测和进程等待，存在活动命令时拒绝切换。审批模式与本机边界独立，插件/技能/MCP 不使用本机路径。
- 沙箱执行先用现有 probeSandbox 检查能力，失败关闭；支持显式网络共享、只读 DNS/证书配置及额外可写目录挂载，目录禁止覆盖私有 Data 且执行时再次规范化。命令保留 30 秒和 1 MiB 输出限制。本机调用现有进程执行器并设置 cwd，不继承宿主凭据环境。
- 前端恢复 CommandExecutionSettings，使用共享类型；读写失败显示实际错误。没有在本轮打开页面或触发探测。
- 平台边界：当前本机实现限 POSIX /bin/sh，Windows 拒绝开启；macOS/Windows 沙箱尚未实现。进程树/额外挂载布局、跨平台终止与全部运行行为仍待后续实现和授权验收。未运行任何执行探测、构建、生成、迁移或测试。

## B6：插件自开发闭环共享契约（已编写，未测试）

- pluginDevelopmentRpcMethods 接入开发指南、草稿保存/列表/读取、验证/测试入口、安装/激活/停用、版本列表/读取/差异、操作历史、版本授权十五项方法。既有插件与组件包的统一列表/读取/启停/权限仍需后续迁移。
- 插件测试报告 schema 从宿主内部定义移至 API 包，宿主原导出名兼容保留；草稿、版本摘要和激活返回结构依据实际实现声明。安装仍要求该 revision 的通过报告，不改变版本授权及隔离执行条件。
- 插件开发前端移除重复的 RPC module augmentation 和手写返回接口，直接引用共享类型及统一传输校验。未修改用户保留的自唤醒文件。
- 本轮只编写契约及连接代码，未调用插件测试入口、编译插件、安装/激活插件、探测沙箱、运行生成、构建或测试。

## B6：统一插件管理与组件包契约（已编写，未测试）

- ManagedPluginInfo 统一生成插件及组件包的组件、UI 卡片、权限/授权、版本来源、信任状态、manifest 与数字时间戳；列表/读取/启停/卸载/权限，以及组件开关/包版本选择/预览安装八项接入共享方法表和服务端校验。
- 修正统一管理接口错误沿用生成插件 ID 规则的问题：组件包合法点号/下划线 ID 可读取、启停和卸载；同时保留单字符生成插件 ID 的权限设置支持。草稿与生成插件版本入口继续使用原限制。
- 前端 Plugin 类型和组件页引用共享结构；权限提交通过共享 schema，组件页移除重复声明。安装预览仍遵守现有停用安装和签名要求，未放宽激活条件。
- 本轮未安装、启停、卸载或授权任何实际插件，未运行生成、构建、迁移或测试。市场来源、资产和自唤醒等契约继续推进。

## B6：市场来源、密钥、预览与资产契约（已编写，未测试）

- 市场来源列表/写入/删除/刷新、密钥列表/添加/撤销、发布列表、本地/市场预览/丢弃、资产列表/导出共十三项接入共享方法表。来源 HTTPS/无内嵌凭据校验迁到 API 并供宿主原导出复用，输出明确数字时间戳和可空刷新状态。
- 预览结构复用统一插件组件及权限字段，前端市场类型、预览页和资产/密钥页移除旧声明依赖；已有签名、撤销、过期、固定版本检查及 Blob 导出范围保持。
- 本轮没有联网刷新来源、下载插件、增删密钥、创建预览或导出资产，未运行生成、构建或测试。

## B6：技能管理共享契约与读取版本检查（已编写，未测试）

- 技能列表/读取、本地与 Git 预览、安装预览/创建、启停/卸载、快照文件读取八项接口接入共享参数及返回契约，覆盖代码工具声明、摘要、文件列表、来源和工作区；前端技能映射改用 API 类型。
- 修正 skill.read/file 接受 expectedContentHash/expectedWorkspaceRoot 却忽略的问题：读取仓库现在校验预期状态，再返回对应快照，避免项目技能切换或覆盖后继续使用旧展示操作。调用方不传预期条件时保留原读取行为。
- 本轮没有获取 Git、安装或执行技能，未生成测试代码，也未运行生成、构建、类型检查或测试。内置技能发现/配置及其余业务协议仍待推进。

## B6：自唤醒记录契约与恢复审计（已编写，未测试）

- 自唤醒列表、执行记录和动作恢复三项接入共享契约；运行与日记结构明确来源快照、状态、数值时间戳及分页调度信息，前端映射使用 API 类型并接受数字时间。未修改用户保留的自唤醒上下文、页面及测试文件。
- 动作恢复的状态更新与 self_awake.action_resumed 事件在同一事务内提交，提交后广播；保留仅失败/中断动作且会话活动才能恢复的限制，并使用原状态条件更新。
- 当前路由没有独立自唤醒配置接口，不能把此次记录迁移称作配置迁移完成；配置/触发仍由既有业务入口负责。未触发自唤醒、重试动作、调用模型、运行生成/构建/测试。

## B6：子智能体管理契约与详情事件接线（已编写，未测试）

- 创建/列表/读取/发送/继续/中断六项 RPC 接入共享契约，线程明确子会话、父线程、路径、预算使用、截止时间与数值时间戳；agent-client 和子任务面板改用 API 类型，移除面板重复声明。
- 修正详情页从父会话按 agentId 过滤而无法看到子会话执行事件的问题：先读取选中会话树并确认目标，再读取该线程 childSessionId 的持久事件，按显示上限裁剪。
- 现有任务深度、并发、预算与审批限制保留；精细预算、创建幂等键 payload 一致性、恢复等仍属业务待办。未创建或运行子智能体，未执行生成、构建或测试。

## B6：导演、记忆候选与工具目录契约（已编写，未测试）

- director.list 复用已存在的 DirectorRun schema；记忆候选分页/恢复接入已有候选契约及明确 accepted 返回结构，服务端统一参数和结果校验。
- 工具目录接入工具元数据 schema；前端导演及工具类型改用 API，记忆候选移除临时 RPC 声明，宿主名称从 rust-host 改为 typescript-host。
- 此次只覆盖现有路由，并不代表全部旧导演/记忆业务迁移完成；协议生成、旧方法逐项差异及产品完整性仍需继续处理。未运行模型、记忆恢复、工具、生成器、构建或测试。

## B6：通用操作账本与未知结果人工处理（已编写，未测试）

- 对照旧 RPC 确认 operation.list/resolve 缺少实现，新增世界内操作仓库、共享契约和认证路由，支持会话/状态筛选与数量限制，读取真实工具账本及相关权限资源。
- M52 为 tool_operations 增加请求和错误 JSON，新工具调用以明确列名插入参数，结束时保存失败结果。已有记录默认 request=null，未伪造历史参数。
- unknown 操作人工决定仅允许 retry/abandon，条件更新为 failed 并记录 user_resolved_unknown 与持久事件；沿用旧实现语义，不立即重放工具，也不宣称外部副作用已回滚或成功。
- 待补产品页中的未知操作审阅入口、历史请求回填策略及分域账本关联。未打开生产数据库、执行迁移或操作决定，未运行生成/构建/测试。

## B6：操作结果审阅产品入口（已编写，未测试）

- 会话新增独立 OperationPanel，默认筛选结果未知，可切换最近 100 条记录，展示工具、资源、时间、请求/结果/错误；长内容仅截断展示，不改持久记录。
- retry/abandon 按钮明确为记录决定，不执行工具、不回滚外部变化；提交后更新列表，失败提示刷新核对。世界/会话切换隔离旧响应，读请求序号避免并发轮询覆盖人工处理结果。
- 面板只在展开时轮询，可见页面五秒刷新，重复提交加锁；沿用按世界固定的 RPC 传输。无实际页面操作、工具请求或人工决定，未运行构建、生成或测试。

## B6：初始化和实时通知共享契约（已编写，未测试）

- initialize 的请求/结果纳入共享方法表，宿主返回结构校验后才标记初始化；前端连接时校验版本、世界与能力字段。
- session.event 和 server.warning 增加共享通知 schema；实时事件进入前端序号跟踪前先校验，不合法输入关闭连接并走已有恢复流程。TS 生成模板采用共享通知映射和初始化结果类型，旧生成文件仍未运行生成器改写。
- contractHandler 区分请求参数错误和服务端返回契约错误，后者返回内部错误 -32603，不再把返回结构不符误报成用户参数错误。
- 未连接实际服务、触发重连、运行生成、构建或测试；完整协议生成产物及恢复行为仍待授权后的验证。

## B3：创建幂等一致性与后代授权判断（已编写，未测试）

- M53 新增 spawn_request_hash，按规范化默认值及固定字段顺序保存父会话、任务名、角色、消息、回合数和超时摘要。服务入口和仓库创建事务均核对同键摘要；重复相同请求返回原线程，变更任务内容不再悄悄返回旧任务。
- 已有无摘要记录不能可靠还原原始 timeout 参数，因此拒绝作为新请求的幂等确认，提示先查看已有任务，未伪造回填摘要或重新启动任务。
- 后代权限判断从 agentPath 字符串前缀改为沿 parent_id/parent_session_id 关系，限制四层并检测环，避免同名路径混淆可操作线程范围。
- 未创建/发送/执行子任务，未运行数据库迁移、构建或测试。精细预算和其他恢复边界继续实现。

## B3：消息与续接请求幂等（已编写，未测试）

- agent.send/followup 支持可选 idempotencyKey，消息转交原持久邮箱的一致性检查；面板在编辑消息或请求成功后更新键，失败保留键。
- M54 subagent_followups 记录续接键与消息，同一事务内写入记录、扣减预算和排队作业。相同请求返回已有线程状态，不再重复扣减回合；同键内容不同拒绝。仓库自身也拒绝已有活动任务的新增续接。
- 模型 followup_task 使用 turnId/callId 稳定键，审批与后代关系检查保留；旧无键调用仍按每次新请求处理。
- 未创建实际消息、续接任务或迁移数据库，未运行构建/测试。更细粒度模型/工具预算、进程恢复和其他业务缺口继续推进。

## B3：持久输入终态和子任务结果归档（已编写，未测试）

- 作业结算保留 cancelled，失败/中断携带对应 turn 错误或输入终态说明；成功清除旧错误，不再把取消全部归为无说明失败。
- 子任务结算提取到独立 settlement 模块，先同步已终止的持久输入状态，事务内只读取 assistant 的 message_end 作为答复，避免工具或用户消息成为任务结果。
- completed 输入无持久助手消息时明确失败说明；unknown 提醒检查操作记录。线程更新同时绑定 latest_job_id 和活动状态，避免覆盖另一次续接。
- 未执行作业或子任务、未写测试代码、未运行构建或测试。恢复边界和精细预算继续推进。

## B3：持久模型/工具调用预算（已编写，未测试）

- M55 增加子线程累计模型请求与工具调用计数，默认每子树 128 次模型请求、256 次工具调用；续接不清零，后代同时扣减祖先预算，路径按持久父关系并检查环/缺失祖先。
- runtimeCallbacks 在真实模型请求/工具执行前与对应持久记录同事务扣减，任一祖先超预算则整体回滚并拒绝请求；当前线程需活动且自身/祖先截止时间未到。祖先任务本轮答复结束不单独禁止其已创建子任务继续，既有父会话关闭规则仍有效。
- 共享线程详情和子任务面板增加预算上限及使用量。现有无计数历史默认零，历史消耗回填及用户可配置上限仍待后续完善。
- 未调用模型/工具，未执行迁移、构建或测试。

## B3：创建时配置模型/工具预算（已编写，未测试）

- 创建 schema 增加 maxModelRequests（1–128）与 maxToolCalls（1–256），默认保持既有上限；模型工具复用创建 schema，前端新增对应输入并随编辑更新幂等键。
- M56 保存每线程上限，执行前预算读取持久配置，祖先预算逐层扣减规则保持；线程详情返回实际配置。非默认预算进入创建请求摘要，默认值省略以兼容此前固定默认预算的已存摘要。
- 预算只在创建时设置，不新增模型提高已有预算的入口。历史使用量回填、跨平台及其他业务缺口仍待处理。未创建任务或运行迁移、构建、测试。

## B7：旧数据库只读快照导出入口（已编写，未执行）

- 新增 exportLegacySnapshot 与 Script/Project/export_legacy.mjs，以只读连接和 SQLite 读事务导出所有非内部表，包含未知附加表，保留表 schema 及字段类型。整数按十进制字符串带类型标记、Blob 按 base64 带类型标记，避免 bigint 精度损失。
- 目标目录必须不存在，目录 0700/文件 0600；逐表 NDJSON、行数/字节数/SHA-256 清单，单行超 32 MiB 拒绝。失败目录保留 INCOMPLETE，不能作为可导入快照；manifest 写完同步后才删除该标记。
- 要求旧 sessions/session_events 及明确 runtime_origin，混合世界或缺失世界列拒绝，待专门转换处理。仅数据库值，不包含 Blob 外部文件；字段转换、附件复制、幂等导入、回退及完整迁移报告仍待实现，不能将导出称作迁移完成。
- 本轮只编写源码，未读取生产数据库、创建实际快照、执行导出入口、构建或测试。

## B7：迁移快照读取与完整性边界（已编写，未执行）

- 新增 snapshot-format 和 LegacySnapshotReader，校验格式/世界、唯一表名与文件名、数字元数据、文件名限制及完整标记；拒绝文件符号链接与非普通文件，清单最大 16 MiB、单行最大 32 MiB。
- 流式读取每表并核对行数、字节数、SHA-256、文件修改信息和完整标记；整数恢复为 SQLite 64 位 bigint、Blob 使用 canonical base64，UTF-8 非法字节拒绝。清单解析后冻结，避免消费方修改校验元数据。
- scan 回调只用于尚未提交的暂存转换，必须等整表校验成功后才能提交；不会执行快照内 schema SQL。快照摘要证明传输完整性，不代表可信签名或数据已完成领域迁移。
- 尚待分域转换、所有表处置报告和外部附件；本轮未读取真实快照、生产数据库或执行任何迁移、构建、测试。

## B7：会话领域暂存转换（已编写，未执行）

- stageLegacySessions 创建全新私有目标目录和 v2 数据库，只从验证快照转换 sessions；保留 UUID、标题、状态、时间、参与者与环境，元数据使用稳定事件 ID，并记录旧到新 ID 映射。
- 每张快照表写入摘要/行数/待处理状态，sessions 在整表扫描校验完成后事务提交；报告明确其余表与附件尚未转换。快照 schema 不执行。
- realm_meta 增加 legacy_import_state=incomplete，普通 EdenDatabase 打开已有暂存库时拒绝启动；只有后续完整转换流程能够完成所有域并解除门禁。当前模块不提供解除或自动启动入口。
- 原始事件序号与元数据合并、断点恢复、所有其他表域和附件尚待实现。未打开生产数据库、创建暂存目录、执行导入、构建或测试。

## B7：历史事件与增量载荷转换（已编写，未执行）

- 对照归档 event_storage.rs，实现 Replace/Append/Object/Array 外部标签补丁解码，不把普通 JSON 当补丁；对象使用无原型容器，数组索引及递归深度/展开大小受限。
- 事件表先完整扫描暂存并校验摘要，再按会话和序号转换。原 ID 保留、序号统一加一；基事件需存在且早于当前事件。权威会话元数据移至历史末尾，避免旧创建/元数据事件覆盖导入会话当前状态。
- 转换事务同时提交历史事件、映射及表处置状态，失败回滚；原始 schema 不执行。当前仅会话/事件已接入，尚不包含 pi checkpoint 语义转换，不能据此宣称可续聊历史运行状态。
- 未读取实际快照、执行转换、构建或测试，其余领域与附件继续实现。

## B7：Blob 外部文件转换（已编写，未执行）

- 暂存导入支持明确传入旧 Blob 根目录，保留 ID/MIME/创建时间，按新宿主 blobs/<摘要前两位>/<摘要> 写入；源/目标要求独立，源相对路径禁止穿越与符号链接，目标不覆盖已有文件。
- 64 KiB 流式复制，核对长度、SHA-256 和源文件修改信息；默认遵循宿主 32 MiB Blob 上限，模块参数可明确指定不超过 1 GiB。文件和 POSIX 目录同步后提交元数据/映射及整表状态。
- 失败回滚 Blob 表事务，已写文件留在不可启动的暂存目录，等待后续恢复/清理流程；不回写旧文件。未传根目录时 blobs 仍为待处理，报告不宣称附件完成。
- 本轮未读取、复制真实附件，未执行导入、构建或测试；其余表域及断点恢复继续实现。

## B7：审批、问答与长期记忆转换（已编写，未执行）

- 审批和问答按表事务转换，保留原 ID、所属会话/回合、原请求与已结束状态；旧 pending 转为 interrupted，避免等待旧执行器。原审批决定、解决时间等完整原始行保存在 legacy_conversion_records，不自动创建持久授权或重放动作。
- memories 保留整数 ID、内容、类型、范围、来源会话、元数据及时间，同时记录 ID 映射和原始行。超出新 API 安全整数范围显式拒绝；不触发记忆提取或远端同步。
- 每表完整性扫描成功后才提交数据和 converted 状态。暂存报告明确只有列出的领域完成转换，宿主启动门禁仍保持 incomplete。备忘、作业、运行续接及其余领域、恢复和最终报告仍待实现。
- 本轮只读取源码、编辑实现和文档，未读取真实 Data、执行导入、构建、类型检查或测试；仍按先全部业务、后测试代码、用户指定后才执行测试的长期目标推进。

## B7/B1：备忘迁移与独立延后调度（已编写，未执行）

- 对照旧 host.rs 的 COALESCE(snoozed_until, remind_at, due_at)，M57 添加独立延后时间及来源；共享备忘契约、创建/修改/读取、待触发查询、调度恢复与模型工具同步接入。通知发送后清除延后，重复时间仍按原 remindAt/dueAt 推进。
- 前端稍后提醒写 snoozedUntil 并恢复 active，视图映射保留 source、snoozed_until 和实际 trigger_at。现有调用方仍可省略新字段，创建 schema 默认 null。
- 暂存导入保留备忘整数 ID、内容、状态、关联会话、重复规则、全部时间、来源及原始行，校验完成后同事务提交映射和表状态；不直接排队提醒。完整导入激活后由已有恢复调度入口接管，旧作业迁移仍待去重衔接。
- 旧重复规则超出新宿主支持范围、旧内容超出当前 API 长度限制等兼容处理仍需在最终转换门禁前补齐，不能将表复制当作运行兼容验收。
- 未读取真实 Data、执行 SQL 迁移、运行提醒、构建、类型检查或测试。

## B7：作业历史与提醒恢复去重（已编写，未执行）

- 新增 job-conversion，保留原作业 ID、类型、时间、次数及失败信息，旧幂等键/租约/载荷等完整原始行写入转换记录。completed/failed/cancelled 保留，claimed 映射 unknown，不进入自动恢复队列。
- scheduled memo.reminder 从已转换备忘补齐 revision/occurrence，使用与 recoverSchedules 相同的 memo:<id>:<revision> 键和载荷；失效提醒标为 cancelled。同一发生时间的 claimed 提醒占用该键以阻止恢复重复派发，已变更时间不占用新提醒键。
- 非提醒 scheduled 作业暂时明确报错、回滚整张 jobs 表，保留暂存库 incomplete；自唤醒及其他调度载荷仍需对应领域转换，不能静默丢弃或把旧载荷交给新执行器。人工处理 unknown 的产品入口和转换后的跨域完成门禁继续实现。
- 未读取实际 Data、执行转换、运行作业、构建、类型检查或测试。

## B7：自唤醒定时作业转换（已编写，未执行）

- 新增 self-awake-job 领域转换，scheduled self_awake 已接入作业导入。保留旧幂等键和 eventId（缺省回退旧键），将 prompt 兼容为 scheduled 原因，触发资料沿用归档 build_request 的八字段白名单与 4000 字符限额；完整旧载荷仍保存在原始转换记录。
- 会话必须存在，关闭或删除会话的待调度任务转 cancelled；claimed 仍保留 unknown、不自动执行。scheduler/userId 只保留为路由数据，不创建提交所有权；Mon 提交归属以及运行/日记历史转换仍待补齐。
- 同时修正前批提醒转换：缺少当前发生时间的 claimed 提醒仍保持 unknown，不能因已清除提醒时间而推断旧外部结果已取消。
- 不读取生产数据，不执行导入、作业、模型、构建或测试。其他未知 scheduled 类型仍明确阻止该表提交，后续继续按旧域转换。

## B7：自唤醒运行记录与日记（已编写，未执行）

- 按 jobs → self_awake_runs → self_awake_diaries 顺序转换，保留原 ID、请求、作者、决定、次数、错误与时间，日记保留原作者/角色、标题、正文、心情和元数据，原始行及 ID 映射同事务提交。
- 显式校验作业类型、运行会话及日记所属会话，拒绝跨会话关联。completed/failed 保留，running 转 interrupted；pending 仅在作业 queued 时映射 preparing，其余 interrupted。历史运行已结束或运行中时同步停止错误保留 queued 的旧作业，阻止重复动作。
- 本批不创建旧回合或 input 的虚构关系；历史执行事件关联、通知状态、提交归属及其余领域继续实现。未执行导入、业务、构建或测试，暂存启动门禁仍保持 incomplete。

## B7：自唤醒通知历史可读迁移（已编写，未执行）

- M58 添加 self_awake_notification_history，引用已转换运行，保存旧请求渠道、原始/当前状态、载荷、回执、次数、错误与时间；原始行和 ID 映射随整表校验提交。
- delivered/failed/suppressed 原样保留；pending 标 unknown 并保留 originalState，不交给新外发调度。旧 delivered 仅代表旧宿主回执，不能视为用户已读或回复。
- self_awake.execution 增加 notificationHistory，供现有执行记录查看入口读取。历史回执不覆盖新 actionResult，不新增自动重发；待处理通知的人工决定、提交归属及其他领域继续实现。
- 未读取实际 Data、执行数据库迁移、发送通知、构建或测试。

## B7：分域进度与失败报告（已编写，未执行）

- 报告从 legacy_conversion_tables 的已提交状态生成，包含阶段、结果、表摘要/行数、已转换和待处理表；初始化及各领域提交后写入，失败时记录当前阶段和错误。交互/历史组中部分表已提交后另一表失败也能准确反映。
- 私有临时文件写入并同步后原子替换 migration-report.json，POSIX 同步目录；报告写入本身失败时聚合原转换错误与报告错误，不把失败伪装为完成。
- 仍固定 incomplete，未增加宿主绕过入口。崩溃时磁盘报告可能落后最后一次数据库提交，后续恢复必须以数据库状态为准；断点恢复、提交归属及全部领域完成门禁继续实现。未运行导入、构建或测试。

## B7：显式断点恢复入口（已编写，未执行）

- 提取 conversion-runner，创建和恢复共用逐域转换及报告逻辑。resumeLegacyConversion 只接受同世界 incomplete 暂存库，核对快照表集合、SHA-256、行数和合法进度后才升级暂存 schema；普通 EdenDatabase 启动门禁不变。
- 已提交表跳过，交互/自唤醒历史组也按表恢复；未提交 Blob 根目录改名为 blobs-incomplete-<UUID> 保留，再在独立新目录复制，不删除旧附件或半成品。
- 新建/恢复持有 .conversion.lock 排除并发导入，正常退出释放；崩溃残留锁不自动猜测失效，需确认原进程已停止。数据库已提交进度是恢复事实来源，报告不是跳过依据。
- 未执行实际导入或恢复，未运行构建/测试。剩余领域、完整性最终门禁、CLI 和回退继续实现；仍不可启动暂存库。

## B7：迁移 CLI 与只读状态（已编写，未执行）

- 新增 import_legacy.mjs 的 stage/resume/status 与根 npm 入口；使用现有 tsx 加载 TypeScript，export 使用说明同步。参数错误退出 2、失败退出 1、暂存未完成退出 3，当前不提供成功启用生产迁移入口。
- readLegacyConversionStatus 只读连接/事务核对世界与 incomplete 标记后返回数据库已提交表进度，不升级 schema、不信任旧 JSON 报告、不触发模型或调度。聚合转换/报告错误在 CLI 分别输出。
- 新增数据迁移操作.md，说明双世界、可选 Blob、退出码、残留锁、半成品保留及当前未完成范围。所有示例仅为文档，未执行 CLI、导入、构建或测试。

## B7：旧操作账本接入审阅（已编写，未执行）

- operation_journal 转换到 tool_operations，保留原操作 ID、会话/回合、工具调用 ID、能力/资源和请求/结果/错误/时间；原始行和 ID 映射同事务提交，支持已转换表跳过。
- M59 添加显式 tool_call_id/capability/resource，操作读取优先使用保存字段，现有新操作仍回退原推导逻辑，避免旧操作 ID 格式不同导致工具调用 ID 错误。
- started 转 unknown 进入现有人工处理列表，planned/authorized 转 interrupted，committed/failed/unknown 原样保留；不把旧授权转成新自动执行许可，不重放动作。
- 未执行真实迁移、外部动作、构建或测试；其余领域和最终门禁继续实现。

## B7：历史语音分段与附件关联（已编写，未执行）

- Blob 领域提交后转换 voice_speech_segments；保留原整数 ID、消息/会话、分段组/次序、文本摘要/长度和音频时长/格式，按原 Blob ID 建立历史缓存键 legacy:voice:<id>，不参与正常合成缓存命中。
- M60 保存 external_audio_asset_id，列表优先返回旧外部资产标识；重新合成覆盖分段时清除旧外部标识，避免新音频仍冒用旧资产。完整旧行（含更新时间）和 ID 映射同事务保留。
- 缺少已转换附件时语音表保持待处理；引用缺失 Blob 则报错回滚整表。不重新合成、不联网获取旧音频，不自动播放。未执行迁移、播放、构建或测试。

## B7：屏幕与摄像头请求迁移（已编写，未执行）

- 对照旧 media_requests 表，将 screen/camera 请求、结果、错误、时间和原 ID 导入新表；answered 映射 resolved，pending 映射 cancelled 并标注迁移中断，rejected/expired 保留。
- 原始行和映射随完整表扫描提交，支持恢复跳过；不伪造旧 resolved_at，也不重新弹出采集请求。结果载荷原样保留，附件完整性仍由 Blob 与最终跨域门禁负责。
- 未执行真实导入、屏幕/摄像头捕获、构建或测试；其他领域继续实现。

## B7：连接器身份与事件历史（已编写，未执行）

- 连接器身份保留原 ID、类型、身份键、显示名、配置、期望连接状态和时间；生成新 generation，连接状态重置为 disconnected 或等待权限/凭据的 error，不声称旧 worker 仍在线，不复制新代次授权。
- 事件保留 ID、外部去重标识、类型与载荷，旧状态写入 legacy_* suppression，claimed 标 legacy_unknown；原 operation_id/lease/更新时间随完整原始行留档。旧表没有权威会话/作业关联，不拿当前绑定冒充历史归属。
- 已转换记录可由连接器事件读取入口查看；pending/unknown 事件的人工恢复、旧设置兼容和凭据来源仍待接齐，不将历史可读等同恢复闭环。未启动 worker、执行迁移、构建或测试。

## B7：历史输入与回合关联（已编写，未执行）

- session_inputs 保留原输入/回合 ID、文本、压缩类型及完整载荷（包含附件/作业信息），创建对应持久 turns；原 claimed/completed 时间与字段也存入原始记录。
- queued 暂为 held，claimed 为 interrupted，completed/interrupted 保留；此为等待运行上下文转换的暂存状态，尚未实现原排队输入接续，不能据此宣称队列迁移完成。
- 原载荷含 jobId 时要求作业存在、会话一致且无冲突关联，持久 input_id 并避免旧作业再次投递；完成输入对应完成作业，其余保留 unknown。整表失败回滚输入、回合和关联。
- 未读取真实 Data、运行输入/作业、执行迁移、构建或测试。桌面提醒的回合外键及自唤醒历史执行关联可继续使用这些原回合记录接线。

## B7：桌面提醒历史（已编写，未执行）

- M61 将桌面提醒回合外键改为可空，导入不虚构旧表缺失的回合/显示时间/关闭时间；关联自唤醒运行时校验会话并保留作者快照，其余作者为空。旧原始行与幂等键仍完整留档。
- launching 转 unknown，open 转 displayed，closed/failed/unknown 保留；共享契约扩展对应状态。默认弹窗列表仅返回 pending，includeClosed 查询保留完整历史；历史无回合或不确定记录不能通过实时显示回执恢复执行。
- 历史窗口不自动重新打开，独立人工处理和原始更新时间展示仍待后续产品接线。未执行迁移、弹窗、构建或测试。

## B7：历史执行关联补全（已编写，未执行）

- 新增可重复执行的 execution-links，按迁移映射限定旧运行，从作业持久 input_id 找到原输入/回合，并逐层核对会话和既有非空关联；缺少输入时保持未知，冲突则回滚。
- 桌面提醒按完整原始行的 run_id 关联对应运行回合，不从时间或作者猜测。先前已转换表也能在 resume 后补齐关系，自唤醒执行详情可按回合读取已转换事件。
- 关联步骤不修改业务状态、不触发动作；同时修正实时提醒关闭操作只更新 closed_at，未展示就关闭不再产生虚假的 displayed_at。
- 未执行迁移、弹窗、构建或测试。历史输入恢复及其他领域继续实现。

## B7：模型实体选择保留（已编写，未执行）

- M62 新增私有 legacy_model_selections，转换会话级及角色级模型实体、视觉实体、运行信息和时间，原始行与复合 ID 映射同事务保留。
- 全部标 refresh_required，报告单列待刷新数量。表 converted 仅表示历史选择已保存，不表示可执行绑定恢复；Core 刷新、角色名册核对及未确认选择隔离仍待接齐，不从旧 runtime_info 猜测凭据或模型协议。
- status 对尚未升级到 M62 的已有暂存库保持只读兼容。未调用 Core、模型、数据库迁移、构建或测试。

## B7/B6：模型刷新与历史选择核对（已编写，未执行）

- 当前模型绑定经过 schema/角色名册验证并保存后，同事务核对 legacy_model_selections；M63 保存核对结果和时间，保留历史原值。主/视觉实体一致标 refreshed，当前实体不同标 replaced，不伪称恢复了原模型。
- 角色不匹配、没有有效主模型时仍待刷新；单模式缺视觉实体 ID 时不能用模型名称推断一致，继续 refresh_required。原凭据不进入核对记录或公共事件。
- 本批接入现有绑定保存流程，不自动调用 Core，也未解决完整迁移启动前的模型刷新入口；该入口及单角色视觉实体保留继续实现。未执行模型请求、数据库迁移、构建或测试。

## B6/B7：单角色视觉实体持久化（已编写，未执行）

- 单模式绑定快照增加可选 visionEntityId，Core 目录装载保存已验证实体 ID；模型服务恢复、子任务继承和无持久绑定分支同步传递。没有视觉模型却有实体 ID 的快照拒绝。
- 旧快照缺字段仍可读取，不猜测身份；历史选择核对现在按明确视觉 ID 比较，缺失时仍待刷新，当前无视觉模型则明确按 null 比较。
- 未调用 Core/模型、运行迁移、构建或测试；迁移激活及其余领域继续实现。

## B7：工作区选择迁移（已编写，未执行）

- workspace_state 转换保存当前路径和原始记录；本平台绝对路径写入 workspace.root 并标 selected_unvalidated，空路径/异平台路径不解释为相对当前目录，标 reselection_required。
- 原 pending_path/session/requested_at 留在私有迁移设置，待切换请求标 interrupted，不执行旧切换。实际读写仍由 WorkspaceService.root 校验路径存在、规范路径一致和私有数据边界。用户后续通过正常切换入口选择目录时更新迁移状态为 reselected。
- 保存路径不代表已通过文件系统验证；跨平台重选提示及完整启用报告继续实现。未读取旧工作区、执行迁移、构建或测试。

## B7：业务恢复摘要（已编写，未执行）

- conversion-report 接入 recovery-summary，逐域提供未决数量、最多 20 个标识和处理方向，工作区提供选择状态；数据库已提交的 converted 表和业务仍待恢复分别展示。
- SQL 表名/条件来自源码固定清单，报告不包含请求、模型凭据或文件内容；缺少后续新增表的旧暂存库保持兼容。摘要明确跨域记录可能重叠，零计数不能替代运行兼容和最终完成门禁。
- 更新操作文档；本轮只读源码并编辑实现，未运行报告查询、真实迁移、构建或测试。

## B7：Core 身份与同步历史保留（已编写，未执行）

- M64 保存旧 Core 会话身份、主体键、凭据引用及 outbox ID/幂等键/载荷/时间/次数/错误，原始行和映射同事务保留。queued 暂 held，claimed 为 unknown，completed 保留。
- 身份标 rebind_required，不把旧 credential_ref 当令牌或连接配置；会话投影前检查未核对身份和未决投递，禁止自动同步。恢复摘要列出两域计数。
- 该门禁等待后续身份核对、旧投递格式/幂等键恢复入口，不代表同步迁移闭环已完成；未访问 Core、执行投递、数据库迁移、构建或测试。

## B7/B5：Core 身份重绑验证（已编写，未执行）

- 按归档 resolve_principal 的 /api/users/me/profile/ 与 id/user.id/username 顺序核对 user: 主体；先匹配规范端点，credential: 回退主体要求完整旧 SHA-256 凭据引用与前缀一致，令牌不输出。
- 模型目录装载及模型选择 PATCH 前先验证历史身份，目录成功后保存连接/模型的同一事务标记 rebound，并核对原行未改变。已 rebound 的历史会话换凭据仍重新核对主体。
- 旧用户身份无法确认、端点变化或只有回退身份且换令牌时明确拒绝，后续需显式账号核对流程；未决 outbox 门禁保持。
- 仅编写网络调用代码，没有实际访问 Core、修改远端模型、执行迁移、构建或测试。

## B5/B6/B7：旧同步历史查询与页面（已编写，未执行）

- mon.sync.status 共享契约新增 legacy 分支和独立 legacyBefore 游标，提供原账号核对状态、同步阻断标识、投递状态计数及分页记录，不返回凭据引用、主体键或请求载荷。
- MonSyncPanel 显示旧同步状态、记录与独立翻页按钮，切换会话清除旧记录和游标；状态查询仍沿当前会话/Mon 世界验证。
- 仅接入可读审阅，历史未决投递的处理入口仍待实现。未执行 Core 请求、页面、迁移、构建或测试。

## B5/B6/B7：历史同步人工决定（已编写，未执行）

- 新增 mon.sync.legacy.resolve 共享协议/路由，只处理当前 Mon 会话内 held/unknown 记录；要求非空说明，支持 confirm_completed/abandon，分别标 completed/abandoned。
- M65 保存原状态、决定、说明及时间，与状态变更及审计事件同事务。相同决定/说明可幂等返回，不允许后续不同决定覆盖历史记录。原载荷、错误与时间保留。
- 本操作不重投旧请求；未决计数归零并且身份核对后，现有同步可继续。前端人工入口和真正旧协议重投继续实现，不把放弃当作恢复成功。未调用 RPC、Core、迁移、构建或测试。

## B5/B6/B7：同步人工处理页面（已编写，未执行）

- 新增 LegacySyncReview，只在 held/unknown 记录提供必填说明后的确认完成/放弃操作，忙碌锁阻止重复提交、失败保留输入、成功刷新。文字明确不重发旧请求、处理完未决项后正常同步可以继续。
- 历史查询共享契约增加 review，列表显示人工决定及说明，区分用户记录与旧运输回执。MonSyncPanel 按会话 key 重建内部状态，操作固定 Mon 世界并在提交前核对当前世界。
- 未运行 UI、RPC、真实迁移、构建或测试；旧协议重新投递和其他迁移域继续实现。

## B5/B7：旧投影协议适配（已编写，未执行）

- 对照归档 core-sync deliver/会话参与者更新，新增 legacyProjectionPlan 与 deliverLegacyProjection，支持 session/message/director 三类原载荷，保留 source、external_session_id、external_message_id/external_plan_id 和正文。
- 第一次请求前校验会话/嵌套元数据归属、全部消息标识及参与者列表；远端 ID 限定可编码路径，阶段间检查取消和调用方身份/意图仍有效。Core 消息 sync_status=failed 明确失败，不把已存原文当作投影成功。
- 本模块尚未开放重投路由，调用者必须先持久化授权重投意图并核对主体；notification 渠道需要独立适配，不套用投影重试。未实际请求 Core、运行迁移、构建或测试。

## B5/B7：历史重投持久意图（已编写，未执行）

- M66 新增 legacy_core_replays，单投递最多一个 running 意图，稳定请求键绑定会话/投递/说明/原载荷摘要；重复键只返回既有记录，不重复执行。
- 开始/结束与旧投递状态、审计事件同事务，成功 completed、响应不确定 unknown；恢复函数将中断 running 转 unknown，普通同步门禁也覆盖 running。原始历史时间/错误保持。
- 本批仅完成 repository，恢复函数及传输适配调用仍需服务生命周期接线，不宣称入口可用。未运行 SQL 迁移、Core 请求、构建或测试。

## B5/B6/B7：历史重投服务与 RPC（已编写，未执行）

- mon.sync.legacy.replay 要求 confirm=true、说明及稳定 UUID 请求键，接入会话空闲配置锁与服务 AbortController/关闭等待。启动调用账本恢复，运行中断转 unknown，不自动重发。
- 只支持已适配的会话/消息/导演投影；核对持久 rebound 身份与旧投递 credential_ref 一致，再用当前凭据重验账号，阶段间检查会话/连接/身份/意图仍有效。没有对应原身份的历史投递不猜测归属。
- 校验后先保存意图，重复请求键返回旧记录；实际发送结果不明确返回 unknown 审阅状态，不吞错为成功。前端重投展示和通知渠道适配继续实现。未调用实际 RPC/Core、运行迁移、构建或测试。
