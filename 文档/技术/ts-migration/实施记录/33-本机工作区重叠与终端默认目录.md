# 本机工作区重叠与终端默认目录

2026-09-10。选择项目目录后报 Workspace overlaps private runtime data，实际未完成选择；终端查询硬件随后因没有工作区而失败。

在用户已授权全部本机执行的策略下，取消 workspaceRoot 对运行数据目录及其祖先/子目录的重叠拒绝，保留 realpath 和目录类型验证。旧规则保存在沙箱暂停归档，恢复须开发者审阅。编辑器文件工具仍以所选目录为范围，不自动改变用户选择。

终端新增 commandRoot：已有工作区使用选中目录，未选择时使用 OS 用户主目录。保留审批与串行执行队列，审批后目录变化会拒绝旧请求。选择失败的前端错误卡新增重新选择文件夹按钮。

工作区读取、写入、终端默认目录、策略和并发相关 6 项实际测试通过。类型检查两端通过。日志 `/tmp/eden-workspace-host-fix.log`。服务端需重启后生效；未自动重放此前失败命令或修改用户工作区设置。
