# 130 移除角色打算机制

2026-09-21，用户要求去掉打算机制。

- 删除 intentions 业务模块、工具注册、API schema 与导出；移除 list_intentions、read_intention、create_intention、update_intention 默认工具项与模型描述。
- 删除 eden-intentions 内置技能及自醒技能关联工具。自醒提示保持仅写日记与设置时钟。
- 追加数据库迁移，先删除 character_intention_entries 再删除 character_intentions；保持历史迁移编号，支持现有数据库升级。移除旧业务测试，改为内存库升级删除验证。
- 历史审计解析器中的旧提示原文仅用于解释旧记录，保留；没有运行时打算工具或技能。没有删除长期记忆、已有日记、聊天历史或改写既有时钟。

验证：11 项迁移、自醒写日记、时钟上下文和提示词测试通过，服务端类型检查通过。重启前当前账号有 6 项打算、22 条进展记录；空闲重启宿主 PID 448325 后，只读确认当前账号、Mon 根库、local 库均已不存在打算两表，双世界服务正常监听。本次未额外触发自醒。

限制：旧聊天、日记和长期记忆里提及的事项仍可能被角色读取；移除打算机制不等于抹除这些历史内容。
